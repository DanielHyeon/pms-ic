# 01. 대시보드 화면설계

> 작성일: 2026-02-08
> 버전: v2.0
> 라우트: `/dashboard`
> 필요 Capability: `view_dashboard`
> 기본 Preset: 역할에 따라 자동 결정
> 노드 역할: **Hub** (허브 노드 — 진입점·요약·판단 전용)

---

## 1. 화면 개요

### 1.1 목적

대시보드는 **"지금 전체 상황이 어떤가?"**에 대한 단일 진입점이다.
기존 3종 분리 대시보드(PMO용/PM용/공통용)를 **1개 화면 + 5개 Preset 분기**로 통합한다.

> **v2.0 변경**: AUDIT_EVIDENCE Preset은 대시보드에서 제거.
> 외부 감사의 진입점은 `/audit/evidence`이며, 대시보드 메뉴 자체가 숨김 처리된다 (00_총괄 §3 매트릭스 참조).

### 1.2 설계 원칙

| 원칙 | 설명 |
|------|------|
| **단일 화면** | `/dashboard` 하나로 모든 역할이 진입 |
| **Preset 분기** | 역할에 따라 정보 밀도·위젯·CTA가 달라짐 |
| **정책 기반 조립** | 5개 Sub-Page가 아니라, **DashboardShell + WidgetKey 정책**으로 위젯을 조립 |
| **판단 우선** | 상단 "판단 요약"(KPI/Health), 중앙 "상세", 우측 "다음 행동" |
| **AI CTA 통합** | AI Insight Widget이 진단+근거+다음행동 3단 구조로 네비게이션 유도 |
| **Hub 페널티** | AI 스코어링에서 행동/목록 질문에는 허브 노드 페널티 적용 (§8.3 참조) |

### 1.3 핵심 질문 → 위젯 매핑

| 사용자 질문 | 위젯 | Preset |
|-----------|------|--------|
| "프로젝트 건강 상태는?" | Project Health Card | ALL |
| "리스크가 뭐가 있지?" | Risk Summary | EXEC, PMO, PM |
| "승인 대기 건이 있나?" | Pending Approvals | PMO, PM, CUSTOMER |
| "내 할 일은?" | My Action Items | PM, DEV |
| "단계별 진행률은?" | Phase Progress Timeline | ALL (밀도 다름) |
| "예산 소진율은?" | Budget Burn Card | EXEC, PMO |
| "이번 스프린트 상태는?" | Sprint Summary | PM, DEV |

---

## 2. MenuOntology 노드

### 2.1 타입 확정 (v2.0 변경사항)

v1.0 대비 변경된 필드 규격:

| 항목 | v1.0 | v2.0 (확정) | 변경 사유 |
|------|------|------------|----------|
| `entities` | 소문자 `"project"` | PascalCase `"Project"` | EntityType enum 정규화 |
| `ui.highlightMetrics` | 단일 배열 | `ui.highlight.metricKeys` + `ui.highlight.widgetKeys` | 데이터 키 vs UI 위젯 키 분리 |
| `suggestedActions.capability` | 단일 `Capability` | `requiredCaps?: Capability[]` + `route?: string` | 복수 권한 + 목적지 명시 |
| `deepLinks.pattern` | `{phaseId}` 형태 | `:phaseId` 형태 | 라우트 템플릿 표준 통일 |
| `scopeHints` | 추상+구체 혼합 | `scopeKeys` (추상) + `scopeParams` (구체) 2계층 분리 | 스코프 해석 안정성 |
| `nodeRole` | 없음 | `"hub" \| "detail" \| "action"` 신규 | AI 스코어링 페널티 기준 |
| AUDIT preset | 존재 | 제거 | 감사는 대시보드 접근 불가 |

> **참고**: EntityType PascalCase 변경은 00_총괄 §6.2 타입 정의에도 반영 필요.
> 본 문서에서 먼저 확정하고, 전체 타입 마이그레이션은 별도 PR로 처리한다.

### 2.2 확장 타입 정의

```typescript
// ─── v2.0 확장 타입 (00_총괄 §6.2 기반) ────────────────────

/** 노드 역할 — AI 스코어링 페널티 기준 */
export type NodeRole = "hub" | "detail" | "action";

/** Preset UI 정책 — highlight 분리 */
export interface PresetUiPolicy {
  density: "compact" | "standard" | "detailed";
  defaultRightPanel: "closed" | "open";
  defaultFilters?: Record<string, string>;
  hiddenColumns?: string[];
  highlight?: {
    metricKeys?: string[];   // 데이터 KPI 키 (API 응답 필드명)
    widgetKeys?: WidgetKey[];  // UI 위젯 키 (강조 표시 대상)
  };
}

/** Preset 추천 액션 — 복수 권한 + 목적지 */
export interface SuggestedAction {
  key: string;
  label: string;
  requiredCaps?: Capability[];   // 복수 (AND 조건)
  route?: string;                // 이동 목적지 (없으면 현재 화면 내 액션)
  targetNodeId?: string;         // AI resolve용 nodeId (route 직접 지정 대신)
}

/** Preset 정책 (v2.0) */
export interface PresetPolicyV2 {
  preset: ViewModePreset;
  ui: PresetUiPolicy;
  suggestedActions?: SuggestedAction[];
}

/** Scope 2계층 분리 */
export interface ScopeDefinition {
  keys: ScopeKey[];          // 추상 ("project" | "part" | "team" | "mine")
  params: string[];          // 구체 ("projectId" | "partId" | "userId")
}

/** MenuOntologyNode 확장 (v2.0) */
export interface MenuOntologyNodeV2 extends Omit<MenuOntologyNode, 'scopeHints' | 'presetPolicies'> {
  nodeRole: NodeRole;
  scope: ScopeDefinition;
  presetPolicies: PresetPolicyV2[];
  virtualNodes?: VirtualNode[];   // 하위 가상 노드 (딥링크 승격)
}

/** 가상 노드 — 딥링크를 온톨로지 2계층 노드로 승격 */
export interface VirtualNode {
  virtualId: string;             // e.g., "dashboard.phase"
  label: string;
  routeTemplate: string;         // e.g., "/dashboard?view=phase&phaseId=:phaseId"
  requiredParams: string[];
  intents: IntentTag[];          // 이 가상 노드에 특화된 인텐트
  description: string;
}
```

### 2.3 Dashboard 노드 (확정본)

```typescript
const dashboardNode: MenuOntologyNodeV2 = {
  nodeId: "dashboard",
  label: "대시보드",
  route: "/dashboard",
  icon: "DashboardRounded",
  domain: "overview",
  nodeRole: "hub",               // ★ 허브 노드 — 행동/목록 질문에 스코어 페널티

  requiredCaps: ["view_dashboard"],

  intents: [
    "ask_overall_status",
    "ask_progress",
    "ask_risk",
    "ask_decision_pending",
    "ask_health_score",
    "ask_bottleneck",
  ],
  canonicalQuestions: [
    "지금 전체 상황이 어떤가?",
    "프로젝트 건강 상태 알려줘",
    "오늘 주요 리스크는?",
    "승인 대기 건이 몇 개야?",
    "이번 주 핵심 지표 요약해줘",
    "전체 진행률이 얼마야?",
  ],
  entities: ["Project", "Phase", "Risk", "Decision", "Sprint"],
  keywords: [
    "대시보드", "현황", "요약", "상태", "건강", "KPI",
    "dashboard", "overview", "summary", "health", "status",
  ],
  metrics: [
    "overall_progress",
    "health_score",
    "risk_count",
    "pending_approvals",
    "budget_burn_rate",
    "sprint_velocity",
    "open_issues_count",
  ],

  defaultPreset: "PM_WORK",
  presetPolicies: [
    {
      preset: "EXEC_SUMMARY",
      ui: {
        density: "compact",
        defaultRightPanel: "closed",
        highlight: {
          metricKeys: ["health_score", "budget_burn_rate", "risk_count"],
          widgetKeys: ["KPI_HEALTH", "KPI_BUDGET", "PHASE_TIMELINE"],
        },
      },
      suggestedActions: [
        {
          key: "view_report",
          label: "리포트 보기",
          requiredCaps: ["view_reports"],
          targetNodeId: "reports",
        },
      ],
    },
    {
      preset: "PMO_CONTROL",
      ui: {
        density: "standard",
        defaultRightPanel: "open",
        highlight: {
          metricKeys: ["health_score", "pending_approvals", "phase_deviation"],
          widgetKeys: ["PORTFOLIO_TABLE", "PHASE_DEVIATION_CHART"],
        },
      },
      suggestedActions: [
        {
          key: "export_report",
          label: "리포트 Export",
          requiredCaps: ["export_reports"],
          targetNodeId: "reports",
        },
        {
          key: "view_health",
          label: "Health Matrix",
          requiredCaps: ["view_pmo"],
          targetNodeId: "pmo-health",
        },
      ],
    },
    {
      preset: "PM_WORK",
      ui: {
        density: "detailed",
        defaultRightPanel: "open",
        highlight: {
          metricKeys: ["overall_progress", "sprint_velocity", "open_issues_count"],
          widgetKeys: ["PHASE_HEALTH", "SPRINT_BURNDOWN_MINI", "AI_INSIGHT"],
        },
      },
      suggestedActions: [
        {
          key: "go_kanban",
          label: "칸반 보드",
          requiredCaps: ["view_kanban"],
          targetNodeId: "kanban",
        },
        {
          key: "go_issues",
          label: "이슈 관리",
          requiredCaps: ["view_issues"],
          targetNodeId: "issues",
        },
      ],
    },
    {
      preset: "DEV_EXECUTION",
      ui: {
        density: "standard",
        defaultRightPanel: "closed",
        highlight: {
          metricKeys: ["my_tasks_count", "sprint_remaining"],
          widgetKeys: ["MY_WORK_PREVIEW", "MY_ISSUES"],
        },
      },
      suggestedActions: [
        {
          key: "go_my_work",
          label: "내 작업",
          requiredCaps: ["view_my_work"],
          targetNodeId: "my-work",
        },
        {
          key: "go_kanban",
          label: "칸반 보드",
          requiredCaps: ["view_kanban"],
          targetNodeId: "kanban",
        },
      ],
    },
    {
      preset: "CUSTOMER_APPROVAL",
      ui: {
        density: "compact",
        defaultRightPanel: "open",
        highlight: {
          metricKeys: ["pending_approvals", "overall_progress"],
          widgetKeys: ["APPROVAL_LIST", "APPROVAL_DETAIL"],
        },
      },
      suggestedActions: [
        {
          key: "go_deliverables",
          label: "산출물 승인",
          requiredCaps: ["view_deliverables"],
          targetNodeId: "deliverables",
        },
        {
          key: "go_decisions",
          label: "결정 사항",
          requiredCaps: ["view_decisions"],
          targetNodeId: "decisions",
        },
      ],
    },
    // ★ AUDIT_EVIDENCE 제거 — 감사는 대시보드 접근 불가
  ],

  // ─── DeepLink: routeTemplate 표준 (:param 형태) ───
  deepLinks: [
    {
      pattern: "/dashboard?view=phase&phaseId=:phaseId",
      description: "단계별 필터 대시보드",
      requiredParams: ["phaseId"],
    },
    {
      pattern: "/dashboard?view=part&partId=:partId",
      description: "파트별 필터 대시보드",
      requiredParams: ["partId"],
    },
    {
      pattern: "/dashboard?preset=:presetId",
      description: "Preset 직접 지정 진입",
      requiredParams: ["presetId"],
    },
  ],

  // ─── 가상 노드: 딥링크를 온톨로지 2계층으로 승격 ───
  virtualNodes: [
    {
      virtualId: "dashboard.phase",
      label: "단계별 편차 뷰",
      routeTemplate: "/dashboard?view=phase&phaseId=:phaseId",
      requiredParams: ["phaseId"],
      intents: ["ask_phase_status", "ask_progress"],
      description: "특정 단계의 편차/원인/조치를 대시보드에서 바로 확인",
    },
    {
      virtualId: "dashboard.portfolio",
      label: "포트폴리오 뷰",
      routeTemplate: "/dashboard?preset=PMO_CONTROL",
      requiredParams: [],
      intents: ["ask_overall_status"],
      description: "PMO 포트폴리오 대시보드 직접 진입",
    },
    {
      virtualId: "dashboard.my",
      label: "내 대시보드",
      routeTemplate: "/dashboard?preset=DEV_EXECUTION",
      requiredParams: [],
      intents: ["ask_my_work"],
      description: "DEV 개인 대시보드 직접 진입",
    },
    {
      virtualId: "dashboard.approvals",
      label: "승인 대시보드",
      routeTemplate: "/dashboard?preset=CUSTOMER_APPROVAL",
      requiredParams: [],
      intents: ["ask_decision_pending"],
      description: "승인 대기 목록 대시보드 직접 진입",
    },
  ],

  // ─── 스코프 2계층 분리 ───
  scope: {
    keys: ["project"],                     // 추상 ScopeKey
    params: ["projectId"],                 // 구체 ScopeParam
  },

  priority: 1,
};
```

---

## 3. Preset별 와이어프레임

### 3.1 EXEC_SUMMARY (Sponsor)

> **목표**: "30초 안에 프로젝트 상태를 판단하고, 필요한 결정을 내린다"

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  대시보드  [ 프로젝트: AI 보험금지급심사 구축 ▼ ]              │
│  asOf: 2026-02-08 09:00       [ Preset: EXEC ▼ ] [🔄 새로고침]│
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ Health  │  │ Progress│  │  Risk   │  │ Budget  │        │
│  │  Score  │  │   72%   │  │  3건    │  │  Burn   │        │
│  │  B+ 🟡  │  │  ████░░ │  │ High: 2 │  │  58%    │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│                                                              │
│  ┌────────────────────────────────────────────────────┐      │
│  │  Phase Status Timeline                              │      │
│  │  ─── Step1 ✅ ── Step2 🔄 ── Step3 ⏳ ── Step4 ──  │      │
│  │         완료      진행중(지연)   대기       대기      │      │
│  └────────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────────┐      │
│  │  Key Decisions        │  │  AI Insight               │      │
│  │                       │  │                          │      │
│  │  ✅ 결정-001 승인완료  │  │  ⚠ Step2 인프라 지연으로 │      │
│  │  ⏳ 결정-004 승인대기  │  │    전체 일정 영향 예상   │      │
│  │  ⏳ 결정-005 승인대기  │  │                          │      │
│  │                       │  │  근거: Phase Progress     │      │
│  │                       │  │  -28% 편차 (인프라 파트)  │      │
│  │                       │  │                          │      │
│  │                       │  │  1차: [▶ 단계별 관리]     │      │
│  │                       │  │  2차: [▶ 이슈 관리]       │      │
│  └──────────────────────┘  └──────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────┐                    │
│  │  [ 리포트 보기 ]  (Primary CTA)      │                    │
│  └──────────────────────────────────────┘                    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**위젯 구성**:

| 위젯 | WidgetKey | 위치 | 데이터 |
|------|-----------|------|--------|
| Health Score Card | `KPI_HEALTH` | 상단 KPI Row | health_score (A~F 등급) |
| Progress Card | `KPI_PROGRESS` | 상단 KPI Row | overall_progress (%) |
| Risk Count Card | `KPI_RISK` | 상단 KPI Row | open_risk_count + severity 분포 |
| Budget Burn Card | `KPI_BUDGET` | 상단 KPI Row | budget_burn_rate (%) |
| Phase Timeline | `PHASE_TIMELINE` | 중앙 상단 | phases[] + status + delay flag |
| Key Decisions | `DECISION_SUMMARY` | 중앙 좌 | decisions[].status (Approved/Pending) |
| AI Insight | `AI_INSIGHT` | 중앙 우 | AI 진단 + 근거 + 다음행동 (§8.1) |

**CTA**: `리포트 보기` → nodeId: `reports` (Primary)

---

### 3.2 PMO_CONTROL (PMO)

> **목표**: "포트폴리오 전체를 통제하고, 편차를 발견하고, 승인을 처리한다"

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  대시보드  [ 전체 프로젝트 ▼ ]  [ Export ⬇ ]                  │
│  [ Preset: PMO ▼ ] [🔄 새로고침]                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 프로젝트│  │ Health  │  │ 승인대기│  │ 편차   │        │
│  │  Count  │  │ Avg B+  │  │   5건   │  │ Alert  │        │
│  │   3개   │  │ 🟡      │  │ ⚠      │  │ 2건    │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│                                                              │
│  ┌──────────────────────────────────────────────┬───────────┐│
│  │  Portfolio Health Table                       │ Details   ││
│  │                                               │           ││
│  │  프로젝트명  | Health | Progress | Risk | 편차 │ 선택한    ││
│  │  ──────────────────────────────────────────── │ 프로젝트  ││
│  │  AI보험심사  | 🟡 B+  |  72%    |  3  | -28  │ 상세 정보 ││
│  │  차세대Core  | 🟢 A   |  85%    |  1  |  +5  │           ││
│  │  디지털채널  | 🔴 D   |  45%    |  5  | -42  │ Health    ││
│  │                                               │ Breakdown ││
│  │  ▸ 행 클릭 → 프로젝트 선택                    │           ││
│  ├───────────────────────────────────────────────│ Phase     ││
│  │  Phase/WBS Deviation Chart                    │ Deviation ││
│  │                                               │           ││
│  │  ██████░░░░ Step1 (80%)                       │ Pending   ││
│  │  ████░░░░░░ Step2 (42%) ⚠ -28%              │ Approvals ││
│  │  █████░░░░░ Step3 (50%)                       │ ・결정-004││
│  │                                               │ ・산출물-7││
│  └──────────────────────────────────────────────┴───────────┘│
│                                                              │
│  ┌──────────────────────────────────────┐                    │
│  │  [ 리포트 Export ]  (Primary CTA)    │                    │
│  └──────────────────────────────────────┘                    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**위젯 구성**:

| 위젯 | WidgetKey | 위치 | 데이터 |
|------|-----------|------|--------|
| Project Count Card | `KPI_PROJECT_COUNT` | 상단 KPI Row | 담당 프로젝트 수 |
| Health Average Card | `KPI_HEALTH` | 상단 KPI Row | health_score 평균 |
| Pending Approvals Card | `KPI_PENDING` | 상단 KPI Row | pending_count |
| Deviation Alert Card | `KPI_DEVIATION` | 상단 KPI Row | deviation_alert_count |
| Portfolio Health Table | `PORTFOLIO_TABLE` | 중앙 좌 Main | projects[] + health/progress/risk/deviation |
| Phase/WBS Deviation Chart | `PHASE_DEVIATION_CHART` | 중앙 좌 하단 | phases[] + plan vs actual |
| Detail Panel | `PMO_DETAIL_PANEL` | 우측 Panel | 선택된 프로젝트 상세 + Pending Approvals 목록 |

**CTA**: `리포트 Export` → export 실행 (Primary)
**Right Panel**: 기본 `open` — 프로젝트 행 클릭 시 상세 패널 갱신

---

### 3.3 PM_WORK (PM)

> **목표**: "오늘 어디가 막혔는지 파악하고, 즉시 조치로 이어간다"

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  대시보드  [ AI 보험금지급심사 구축 ]  [ 필터: 전체 ▼ ]        │
│  [ Preset: PM ▼ ] [🔄 새로고침]                               │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ Progress│  │  이슈   │  │ Sprint  │  │ 산출물  │        │
│  │   72%   │  │ Open 8  │  │ Day 7/14│  │ 승인2건 │        │
│  │  ████░░ │  │ Blk: 2  │  │ Vel: 24 │  │ 대기    │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│                                                              │
│  ┌───────────────────────────────┬──────────────────────────┐│
│  │  Phase Health Summary          │  Action Items            ││
│  │                                │                          ││
│  │  S1 ✅  S2 🔄⚠  S3 ⏳  S4 ⏳  │  ⚡ 우선 조치 필요       ││
│  │                                │  1. ISS-042 블로커       ││
│  │  ▸ S2: 계획70%, 실적42%       │     → 칸반에서 확인      ││
│  │    편차: -28% (인프라)         │  2. DLV-007 승인 대기    ││
│  │    원인: QA 리소스 부족        │     → 산출물 승인        ││
│  │    조치: 리소스 재배치 검토    │  3. DEC-004 결정 필요    ││
│  │                                │     → 결정/리스크 확인   ││
│  ├────────────────────────────────│                          ││
│  │  Sprint Burndown (Mini)        │  ──────────────────────  ││
│  │                                │  이번 주 마감            ││
│  │  Ideal ╲                       │  ・TSK-155 코드리뷰      ││
│  │  Actual  ╲___╲___             │  ・TSK-161 테스트작성    ││
│  │                 ╲ → 지연      │  ・TSK-170 문서작성      ││
│  │                                │                          ││
│  ├────────────────────────────────│                          ││
│  │  AI Insight                    │                          ││
│  │  ⚠ 인프라 파트 병목 감지       │                          ││
│  │  근거: Phase -28%, ISS 2건     │                          ││
│  │  1차: [▶ 칸반 보드]            │                          ││
│  │  2차: [▶ 이슈 관리]            │                          ││
│  └────────────────────────────────┴──────────────────────────┘│
│                                                              │
│  ┌─────────────────────┐  ┌──────────────────────┐           │
│  │ [ 칸반 보드 ]        │  │ [ 이슈 관리 ]         │           │
│  │   Primary CTA        │  │   Secondary CTA      │           │
│  └─────────────────────┘  └──────────────────────┘           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**위젯 구성**:

| 위젯 | WidgetKey | 위치 | 데이터 |
|------|-----------|------|--------|
| Progress Card | `KPI_PROGRESS` | 상단 KPI Row | overall_progress (%) |
| Issue Summary Card | `KPI_ISSUE` | 상단 KPI Row | open_count + blocker_count |
| Sprint Status Card | `KPI_SPRINT` | 상단 KPI Row | sprint_day / sprint_length + velocity |
| Deliverable Pending Card | `KPI_PENDING` | 상단 KPI Row | pending_approval_count |
| Phase Health Summary | `PHASE_HEALTH` | 중앙 좌 상단 | phases[] + plan/actual/deviation/cause/action |
| Sprint Burndown (Mini) | `SPRINT_BURNDOWN_MINI` | 중앙 좌 하단 | sprint burndown data (ideal vs actual) |
| AI Insight | `AI_INSIGHT` | 중앙 좌 최하단 | AI 진단 + 근거 + 다음행동 |
| Action Items | `ACTION_ITEMS_PANEL` | 우측 Panel | 우선순위별 조치 항목 + deep link |

**CTA**: `칸반 보드` → nodeId: `kanban` (Primary), `이슈 관리` → nodeId: `issues` (Secondary)
**Right Panel**: 기본 `open` — Action Items 고정 표시

---

### 3.4 DEV_EXECUTION (DEV / DevReader)

> **목표**: "내 할 일을 빠르게 파악하고, 바로 작업으로 들어간다"

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  대시보드  [ AI 보험금지급심사 구축 ]                          │
│  [ Preset: DEV ▼ ] [🔄 새로고침]                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                      │
│  │ 내 태스크│  │ Sprint  │  │ 완료율  │                      │
│  │  진행 3  │  │ Day 7/14│  │ 이번주  │                      │
│  │  대기 5  │  │         │  │  60%   │                      │
│  └─────────┘  └─────────┘  └─────────┘                      │
│                                                              │
│  ┌────────────────────────────────────────────────────┐      │
│  │  내 작업 요약 (My Work Preview)                     │      │
│  │                                                    │      │
│  │  In Progress (3)                                   │      │
│  │  ├─ TSK-155  코드리뷰 대기    Sprint-5  D-3       │      │
│  │  ├─ TSK-161  단위테스트 작성  Sprint-5  D-5       │      │
│  │  └─ TSK-170  API 문서 작성    Sprint-5  D-7       │      │
│  │                                                    │      │
│  │  Todo (5)                                          │      │
│  │  ├─ TSK-180  로깅 모듈 구현   Sprint-6  -         │      │
│  │  ├─ TSK-181  에러핸들링 개선  Sprint-6  -         │      │
│  │  └─ ... 3건 더                                     │      │
│  │                                                    │      │
│  │  Done This Week (4)                                │      │
│  │  └─ TSK-148, TSK-149, TSK-151, TSK-153            │      │
│  └────────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────────┐      │
│  │  최근 이슈 (내 관련)  │  │  AI 도움                  │      │
│  │                       │  │                          │      │
│  │  ISS-042 블로커      │  │  ℹ TSK-155 리뷰 요청이   │      │
│  │  → 내 태스크 블로킹   │  │    2일째 대기 중          │      │
│  │                       │  │                          │      │
│  │  ISS-038 일반         │  │  근거: review_wait_days=2│      │
│  │  → 코멘트 요청       │  │                          │      │
│  │                       │  │  1차: [▶ 리뷰어에게 알림]│      │
│  │                       │  │  2차: [▶ 칸반에서 확인]  │      │
│  └──────────────────────┘  └──────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────┐                    │
│  │  [ 내 작업으로 이동 ] (Primary CTA)  │                    │
│  └──────────────────────────────────────┘                    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**위젯 구성**:

| 위젯 | WidgetKey | 위치 | 데이터 |
|------|-----------|------|--------|
| My Tasks Card | `KPI_MY_TASKS` | 상단 KPI Row | in_progress_count + todo_count |
| Sprint Day Card | `KPI_SPRINT` | 상단 KPI Row | current_sprint.day / length |
| Weekly Completion Card | `KPI_WEEKLY_DONE` | 상단 KPI Row | completed_this_week / total |
| My Work Preview | `MY_WORK_PREVIEW` | 중앙 Main | my_tasks[] grouped by status |
| Recent Issues (Mine) | `MY_ISSUES` | 하단 좌 | issues[].assignee = me |
| AI Help | `AI_INSIGHT` | 하단 우 | AI 개인 맥락 분석 + 추천 액션 |

**CTA**: `내 작업으로 이동` → nodeId: `my-work` (Primary)
**Right Panel**: `closed` (DEV는 Right Panel 불필요)

---

### 3.5 CUSTOMER_APPROVAL (Customer PM)

> **목표**: "승인 대기 건을 확인하고, 즉시 승인/반려한다"

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  대시보드  [ AI 보험금지급심사 구축 ]                          │
│  [ Preset: CUSTOMER ▼ ] [🔄 새로고침]                         │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                      │
│  │ 전체    │  │ 승인대기│  │ 이번주  │                      │
│  │ Progress│  │   4건   │  │ 처리   │                      │
│  │   72%   │  │ 긴급: 1 │  │   2건   │                      │
│  └─────────┘  └─────────┘  └─────────┘                      │
│                                                              │
│  ┌────────────────────────────────────┬──────────────────────┐│
│  │  승인 대기 목록                     │  상세 / 승인          ││
│  │                                    │                      ││
│  │  [긴급]                            │  DLV-007 상세        ││
│  │  ├─ DLV-007 설계서_v3.pdf         │                      ││
│  │  │  산출물 | 승인요청 2/6          │  유형: 산출물         ││
│  │  │                                │  요청자: 박승현 (PM) ││
│  │  [일반]                            │  요청일: 2026-02-06  ││
│  │  ├─ DEC-004 아키텍처 변경 결정     │                      ││
│  │  │  결정 | 승인요청 2/5            │  첨부: 설계서_v3.pdf ││
│  │  ├─ REQ-012 요구사항 변경          │  변경사항: 3건       ││
│  │  │  요구사항 | 검토요청 2/7        │                      ││
│  │  ├─ DLV-009 테스트결과서.xlsx      │  ┌──────┐ ┌──────┐  ││
│  │  │  산출물 | 승인요청 2/8          │  │ 승인 │ │ 반려 │  ││
│  │  │                                │  └──────┘ └──────┘  ││
│  └────────────────────────────────────┴──────────────────────┘│
│                                                              │
│  ┌────────────────────────────────────────────────────┐      │
│  │  Phase Progress (요약)                              │      │
│  │  Step1 ✅ — Step2 🔄 (72%) — Step3 ⏳ — Step4 ⏳   │      │
│  └────────────────────────────────────────────────────┘      │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**위젯 구성**:

| 위젯 | WidgetKey | 위치 | 데이터 |
|------|-----------|------|--------|
| Progress Card | `KPI_PROGRESS` | 상단 KPI Row | overall_progress |
| Pending Approval Card | `KPI_PENDING` | 상단 KPI Row | pending_count + urgent_count |
| Weekly Processed Card | `KPI_WEEKLY_DONE` | 상단 KPI Row | processed_this_week |
| Approval List | `APPROVAL_LIST` | 중앙 좌 | pending_items[] (deliverable/decision/requirement) |
| Approval Detail + Actions | `APPROVAL_DETAIL` | 우측 Panel | 선택된 항목 상세 + 승인/반려 버튼 |
| Phase Progress (Mini) | `PHASE_TIMELINE` | 하단 | phase timeline (compact) |

**CTA**: 우측 Panel 내 `승인` / `반려` 버튼 (항목별)
**Right Panel**: 기본 `open` — 승인 상세 패널

---

## 4. 공통 상단 KPI Row 규격

### 4.1 KPI Card 표준

```
┌──────────────────┐
│  라벨 (12px, 회색) │
│  수치 (28px, Bold) │
│  상태 아이콘/색상   │
│  트렌드 (↑↓→)     │
└──────────────────┘
```

| 속성 | 값 |
|------|-----|
| 너비 | Grid 3열 (4개 카드 = 12열) |
| 높이 | 96px |
| 배경 | White, border-radius: 8px |
| 상태 색상 | 🟢 정상(green-500), 🟡 주의(amber-500), 🔴 위험(red-500) |
| 트렌드 | 전주 대비 delta 표시 (optional) |

### 4.2 상태 임계값 (Health Score)

| 등급 | 점수 범위 | 색상 | 의미 |
|------|---------|------|------|
| A | 90~100 | 🟢 | 정상 |
| B+ | 75~89 | 🟡 | 주의 |
| C | 60~74 | 🟠 | 경고 |
| D | 40~59 | 🔴 | 위험 |
| F | 0~39 | ⚫ | 심각 |

### 4.3 Health Score 계산 공식 (v2.0 강화)

```
Health Score =
    (Phase Progress Adherence × 0.30)
  + (Risk Severity Index × 0.25)
  + (Issue Resolution Rate × 0.20)
  + (Deliverable Approval Rate × 0.15)
  + (Budget Adherence × 0.10)
```

#### 4.3.1 Factor별 정의

| Factor | Weight | 입력 데이터 | 계산 방식 | 비고 |
|--------|--------|-----------|----------|------|
| **Phase Progress Adherence** | 30% | `phases[].planProgress`, `actualProgress` | `max(0, 100 - abs(avg(actual - plan)))` | 편차 0 = 100점, 편차 30% = 70점 |
| **Risk Severity Index** | 25% | `risks[].severity` (Critical/High/Medium/Low) | `100 - (critical×40 + high×20 + medium×5) / max(totalRisks, 1)` | severity 가중치 적용 |
| **Issue Resolution Rate** | 20% | `issues.resolved`, `issues.total` | `resolved / max(total, 1) × 100` | total=0이면 100점 (이슈 없음) |
| **Deliverable Approval Rate** | 15% | `deliverables.approved`, `deliverables.total` | `approved / max(total, 1) × 100` | total=0이면 100점 |
| **Budget Adherence** | 10% | `budget.planned`, `budget.actual` | `max(0, 100 - abs((actual/planned - 1) × 100))` | 초과·미소진 모두 감점 |

#### 4.3.2 결측(Missing) 데이터 처리 규칙

> **핵심**: 데이터 없으면 **0점이 아니라 missing 처리 + 가중치 재정규화(renormalize)**.

| 상황 | 처리 | 결과 |
|------|------|------|
| Factor 데이터 정상 | 정상 계산 | 정상 점수 |
| Factor 데이터 결측 | 해당 factor 제외, 나머지 weight 합이 1.0이 되도록 renormalize | 점수는 있는 데이터 기반 |
| 2개 이상 factor 결측 | renormalize + **등급 상한 제한** (B+ 이상 불가) | "데이터 부족" 배지 표시 |
| 전체 결측 | Health Score = `null` | "N/A" 표시, 등급 미부여 |

```typescript
interface HealthScoreFactor {
  key: string;
  weight: number;
  score: number | null;        // null = missing
  source: "db" | "cache" | "derived";
  refreshedAt: string;
}

interface HealthScoreResult {
  score: number | null;
  grade: "A" | "B+" | "C" | "D" | "F" | null;
  factors: HealthScoreFactor[];
  missingFactors: string[];    // 결측된 factor key 목록
  gradeCapReason?: string;     // "2+ factors missing" 등
  confidence: "full" | "partial" | "insufficient";
}
```

---

## 5. 데이터 구조 / API

### 5.1 Dashboard Summary API (v2.0 — 신뢰성 강화)

```
GET /api/dashboard/summary?projectId={projectId}
```

```typescript
// ─── 데이터 신뢰성 3종 (completeness / warnings / provenance) ───

interface DataProvenance {
  source: "db" | "cache" | "derived";
  refreshedAt: string;          // ISO datetime
}

type CompletenessStatus = "ok" | "partial" | "missing";

interface DashboardSummary {
  project: {
    id: string;
    name: string;
    client: string;
    period: { start: string; end: string };
    overallProgress: number;      // 0~100
    healthScore: HealthScoreResult;  // ★ 단순 number → 구조체
    budgetBurnRate: number;       // 0~100
  };

  phases: {
    id: string;
    name: string;
    order: number;
    status: "completed" | "in_progress" | "delayed" | "waiting";
    planProgress: number;
    actualProgress: number;
    deviation: number;            // actual - plan (음수 = 지연)
    delayCause?: string;
    action?: string;
  }[];

  risks: {
    total: number;
    critical: number;             // ★ high → critical/high 분리
    high: number;
    medium: number;
    low: number;
    topRisks: { id: string; title: string; severity: string }[];
  };

  issues: {
    total: number;
    open: number;
    blockers: number;
    resolvedThisWeek: number;
  };

  decisions: {
    total: number;
    pending: number;
    approvedThisWeek: number;
    items: { id: string; title: string; status: string; requestDate: string }[];
  };

  deliverables: {
    total: number;
    pendingApproval: number;
    approvedThisWeek: number;
  };

  sprint?: {
    id: string;
    name: string;
    day: number;
    totalDays: number;
    velocity: number;
    burndownData: { date: string; ideal: number; actual: number }[];
  };

  // ─── 신뢰성 3종 (v2.0 신규) ───

  asOf: string;  // ISO datetime

  completeness: {
    phases: CompletenessStatus;
    risks: CompletenessStatus;
    issues: CompletenessStatus;
    sprint: CompletenessStatus;
    budget: CompletenessStatus;
  };

  warnings: {
    code: string;               // e.g., "BUDGET_DATA_STALE"
    message: string;            // e.g., "예산 데이터가 3일 이상 갱신되지 않았습니다"
    severity: "info" | "warning" | "error";
  }[];

  provenance: {
    phases: DataProvenance;
    risks: DataProvenance;
    issues: DataProvenance;
    sprint: DataProvenance;
    budget: DataProvenance;
  };
}
```

> **의도**: `completeness` + `warnings` + `provenance`가 있으면 "예산 소진율 0%" 같은
> 치명적 착시를 방지한다. UI는 `completeness !== "ok"`인 KPI에 "데이터 부족" 배지를 표시.

### 5.2 My Dashboard API (DEV용)

```
GET /api/dashboard/my?projectId={projectId}&userId={userId}
```

```typescript
interface MyDashboard {
  myTasks: {
    inProgress: TaskSummary[];
    todo: TaskSummary[];
    doneThisWeek: TaskSummary[];
  };
  myIssues: IssueSummary[];
  sprint: SprintSummary;
  weeklyCompletion: { completed: number; total: number };
  asOf: string;
}

interface TaskSummary {
  id: string;
  title: string;
  status: string;
  sprintName: string;
  dueDate?: string;
  daysRemaining?: number;
}

interface IssueSummary {
  id: string;
  title: string;
  severity: "blocker" | "critical" | "major" | "minor";
  relation: "assignee" | "reporter" | "watcher";
}
```

### 5.3 Portfolio Dashboard API (PMO용)

```
GET /api/dashboard/portfolio
```

```typescript
interface PortfolioDashboard {
  projects: {
    id: string;
    name: string;
    healthScore: number;
    healthGrade: string;
    progress: number;
    riskCount: number;
    deviation: number;
    pendingApprovals: number;
  }[];
  aggregated: {
    totalProjects: number;
    avgHealthScore: number;
    totalPendingApprovals: number;
    deviationAlerts: number;
  };
  asOf: string;
}
```

### 5.4 Pending Approvals API (Customer PM용)

```
GET /api/dashboard/pending-approvals?projectId={projectId}
```

```typescript
interface PendingApprovals {
  items: {
    id: string;
    type: "deliverable" | "decision" | "requirement";
    title: string;
    requestedBy: string;
    requestDate: string;
    urgency: "urgent" | "normal";
    attachments?: { name: string; url: string }[];
    changeSummary?: string;
  }[];
  stats: {
    total: number;
    urgent: number;
    processedThisWeek: number;
  };
}
```

---

## 6. 위젯 조립 아키텍처 (v2.0 신규)

### 6.1 설계 철학: "5개 Sub-Page" → "1개 Shell + 정책 기반 조립"

> **문제**: v1.0의 `switch(viewMode) → ExecDashboard/PmoDashboard/...` 구조는
> 초기에 빠르지만, Preset 추가·위젯 변경·부분 렌즈 전환 시 중복 코드가 폭증한다.
> 동일 `PhaseTimeline` 위젯이 5곳에 복사되면 UI/데이터 불일치가 필연적이다.
>
> **해결**: 대시보드는 **조립식**으로 설계한다.
> - `DashboardShell` = 공통 레이아웃 (KPI Row + Main + Right Panel)
> - `DashboardPresetLayout` = "어떤 위젯을 어디에 넣을지" 정책 정의
> - 각 위젯은 재사용 가능한 독립 컴포넌트

### 6.2 WidgetKey 정의

```typescript
type WidgetKey =
  // KPI Cards
  | "KPI_HEALTH"
  | "KPI_PROGRESS"
  | "KPI_RISK"
  | "KPI_BUDGET"
  | "KPI_ISSUE"
  | "KPI_SPRINT"
  | "KPI_PENDING"
  | "KPI_DEVIATION"
  | "KPI_PROJECT_COUNT"
  | "KPI_MY_TASKS"
  | "KPI_WEEKLY_DONE"
  // Phase
  | "PHASE_TIMELINE"
  | "PHASE_HEALTH"
  | "PHASE_DEVIATION_CHART"
  // Widgets
  | "RISK_SUMMARY"
  | "DECISION_SUMMARY"
  | "SPRINT_BURNDOWN_MINI"
  | "ACTION_ITEMS_PANEL"
  | "AI_INSIGHT"
  // PMO
  | "PORTFOLIO_TABLE"
  | "PMO_DETAIL_PANEL"
  // DEV
  | "MY_WORK_PREVIEW"
  | "MY_ISSUES"
  // Customer
  | "APPROVAL_LIST"
  | "APPROVAL_DETAIL";
```

### 6.3 DashboardPresetLayout

```typescript
interface DashboardPresetLayout {
  preset: ViewModePreset;
  slots: {
    kpiRow: WidgetKey[];        // 상단 KPI 카드 (3~4개)
    main: WidgetKey[];          // 중앙 메인 영역 (위에서 아래로 순서)
    rightPanel?: WidgetKey[];   // 우측 패널 (없으면 closed)
    footer?: WidgetKey[];       // 하단 축소 영역
  };
  ui: {
    density: "compact" | "standard" | "detailed";
    defaultRightPanel: "open" | "closed";
  };
}

const presetLayouts: Record<ViewModePreset, DashboardPresetLayout> = {
  EXEC_SUMMARY: {
    preset: "EXEC_SUMMARY",
    slots: {
      kpiRow: ["KPI_HEALTH", "KPI_PROGRESS", "KPI_RISK", "KPI_BUDGET"],
      main: ["PHASE_TIMELINE", "DECISION_SUMMARY", "AI_INSIGHT"],
    },
    ui: { density: "compact", defaultRightPanel: "closed" },
  },

  PMO_CONTROL: {
    preset: "PMO_CONTROL",
    slots: {
      kpiRow: ["KPI_PROJECT_COUNT", "KPI_HEALTH", "KPI_PENDING", "KPI_DEVIATION"],
      main: ["PORTFOLIO_TABLE", "PHASE_DEVIATION_CHART"],
      rightPanel: ["PMO_DETAIL_PANEL"],
    },
    ui: { density: "standard", defaultRightPanel: "open" },
  },

  PM_WORK: {
    preset: "PM_WORK",
    slots: {
      kpiRow: ["KPI_PROGRESS", "KPI_ISSUE", "KPI_SPRINT", "KPI_PENDING"],
      main: ["PHASE_HEALTH", "SPRINT_BURNDOWN_MINI", "AI_INSIGHT"],
      rightPanel: ["ACTION_ITEMS_PANEL"],
    },
    ui: { density: "detailed", defaultRightPanel: "open" },
  },

  DEV_EXECUTION: {
    preset: "DEV_EXECUTION",
    slots: {
      kpiRow: ["KPI_MY_TASKS", "KPI_SPRINT", "KPI_WEEKLY_DONE"],
      main: ["MY_WORK_PREVIEW", "MY_ISSUES", "AI_INSIGHT"],
    },
    ui: { density: "standard", defaultRightPanel: "closed" },
  },

  CUSTOMER_APPROVAL: {
    preset: "CUSTOMER_APPROVAL",
    slots: {
      kpiRow: ["KPI_PROGRESS", "KPI_PENDING", "KPI_WEEKLY_DONE"],
      main: ["APPROVAL_LIST", "PHASE_TIMELINE"],
      rightPanel: ["APPROVAL_DETAIL"],
    },
    ui: { density: "compact", defaultRightPanel: "open" },
  },

  // AUDIT_EVIDENCE: 대시보드에서 제거 — 감사는 /audit/evidence로 직접 진입
};
```

### 6.4 DashboardShell 렌더링

```typescript
function DashboardPage() {
  const { viewMode, effectivePreset } = useAccessContext();
  const { isTemporarySwitch, readOnlyOverride } = usePresetSwitch();

  // effectivePreset: 역할 기반 자동 결정 or 사용자 임시 전환
  const layout = presetLayouts[effectivePreset];
  if (!layout) return <Navigate to="/unauthorized" />;

  return (
    <DashboardShell
      layout={layout}
      readOnly={readOnlyOverride}       // 상향 전환 시 true
      isTemporarySwitch={isTemporarySwitch}
    >
      {/* KPI Row */}
      <KpiSlot widgets={layout.slots.kpiRow} />

      {/* Main */}
      <MainSlot widgets={layout.slots.main} />

      {/* Right Panel */}
      {layout.slots.rightPanel && (
        <RightPanelSlot
          widgets={layout.slots.rightPanel}
          defaultOpen={layout.ui.defaultRightPanel === "open"}
        />
      )}
    </DashboardShell>
  );
}
```

> **장점**:
> - 동일 위젯은 1번만 구현, 배치만 정책으로 바꿈
> - Preset 추가 시 layout 객체만 추가 (컴포넌트 신규 생성 불필요)
> - 임시 Preset 전환이 `effectivePreset` 교체만으로 동작
> - 테스트: layout fixture만 있으면 렌더 테스트 가능

---

## 7. 컴포넌트 분해

### 7.1 폴더 구조

```
features/
  dashboard/
    pages/
      DashboardPage.tsx           ← DashboardShell 기반 조립
    layout/
      DashboardShell.tsx          ← 공통 레이아웃 (KPI + Main + RightPanel)
      KpiSlot.tsx                 ← KPI Row 슬롯 렌더러
      MainSlot.tsx                ← Main 영역 슬롯 렌더러
      RightPanelSlot.tsx          ← Right Panel 슬롯 렌더러
      PresetSwitcher.tsx          ← Preset 임시 전환 UI (Page Header 내)
      ReadOnlyBadge.tsx           ← 상향 전환 시 읽기 전용 배지
    components/
      // KPI Cards
      KpiCardRow.tsx              ← 상단 KPI 카드 Row
      KpiCard.tsx                 ← 단일 KPI 카드 (generic)
      HealthScoreCard.tsx         ← Health Score 전용 (등급 + 결측 배지)

      // Phase
      PhaseTimeline.tsx           ← 단계 진행 타임라인
      PhaseHealthSummary.tsx      ← 단계별 계획/실적/편차/원인/조치

      // Widgets
      RiskSummaryWidget.tsx       ← 리스크 요약
      DecisionSummaryWidget.tsx   ← 결정 사항 (Approved/Pending)
      SprintBurndownMini.tsx      ← 스프린트 번다운 미니 차트
      ActionItemsPanel.tsx        ← 우선 조치 항목 (Right Panel)
      AiInsightWidget.tsx         ← AI 진단 + 근거 + 다음행동 (3단 구조)
      DataIncompleteBadge.tsx     ← completeness !== "ok" 시 표시되는 배지

      // PMO Specific
      PortfolioHealthTable.tsx    ← 포트폴리오 테이블
      PhaseDeviationChart.tsx     ← 단계 편차 바 차트
      PmoDetailPanel.tsx          ← PMO Right Panel (선택 프로젝트 상세)

      // DEV Specific
      MyWorkPreview.tsx           ← 내 작업 요약 리스트
      MyIssuesWidget.tsx          ← 내 관련 이슈

      // Customer Specific
      ApprovalListWidget.tsx      ← 승인 대기 목록
      ApprovalDetailPanel.tsx     ← 승인 상세 + 승인/반려 버튼

    config/
      presetLayouts.ts            ← DashboardPresetLayout 정의 (§6.3)
      widgetRegistry.ts           ← WidgetKey → React Component 매핑
    api/
      dashboardApi.ts             ← API 호출 함수
    hooks/
      useDashboardData.ts         ← TanStack Query 래퍼
      useMyDashboard.ts           ← DEV용 Query
      usePortfolioDashboard.ts    ← PMO용 Query
      usePresetSwitch.ts          ← Preset 임시 전환 상태 관리
```

### 7.2 widgetRegistry 패턴

```typescript
// config/widgetRegistry.ts
import { lazy } from "react";

const widgetRegistry: Record<WidgetKey, React.LazyExoticComponent<any>> = {
  KPI_HEALTH: lazy(() => import("../components/HealthScoreCard")),
  KPI_PROGRESS: lazy(() => import("../components/KpiCard")),
  PHASE_TIMELINE: lazy(() => import("../components/PhaseTimeline")),
  AI_INSIGHT: lazy(() => import("../components/AiInsightWidget")),
  // ... 모든 WidgetKey에 대한 매핑
};
```

---

## 8. AI Navigation 연동 (v2.0 — nodeId 기반)

### 8.1 AI Insight Widget: 진단 + 근거 + 다음행동 3단 구조

> **v1.0 문제**: AI 요약만 있으면 "그럴듯한 말"이 되고, 다음 행동이 약하다.
> **v2.0**: drivers(근거)가 있어야 PMO/PM이 "왜 그런지" 납득한다.
> nextActions를 Top-2 제공하여, 1순위가 권한/데이터로 막힐 때 2순위가 fallback.

```typescript
interface AiDashboardInsight {
  asOf: string;
  summary: string;               // "Step2 인프라 지연으로 전체 일정 영향 예상"
  severity: "info" | "warning" | "critical";

  // ─── 근거 (설명책임) ───
  drivers: {
    type: "metric" | "entity" | "event";
    label: string;               // "Phase Progress Deviation"
    value?: string;              // "-28%"
    link?: {
      targetNodeId: string;      // "phases" — route가 아니라 nodeId
      label: string;             // "단계별 관리에서 확인"
    };
  }[];

  // ─── 다음 행동 (네비게이션 + 액션) ───
  nextActions: {
    label: string;               // "칸반 보드에서 병목 확인"
    targetNodeId: string;        // "kanban" — ★ route 문자열 아님
    deepLinkParams?: Record<string, string>;  // { status: "blocked" }
    suggestedPreset?: ViewModePreset;
    requiredCaps?: Capability[];
    reason: string;              // "블로킹 태스크 2건 확인 필요"
  }[];
}
```

**렌더링 예시**:

```
┌──────────────────────────────────────────┐
│  AI Insight                      ⚠ warn  │
│                                          │
│  Step2 인프라 지연으로 전체 일정 영향 예상 │
│                                          │
│  근거:                                   │
│  ・Phase Progress: -28% 편차             │
│  ・Blocking Issues: 2건                  │
│                                          │
│  추천 행동:                              │
│  1차: [▶ 칸반 보드에서 병목 확인]         │
│  2차: [▶ 이슈 관리에서 블로커 해결]       │
└──────────────────────────────────────────┘
```

### 8.2 AI 네비게이션 규칙: nodeId만 반환, route 직접 생성 금지

> **핵심**: AI가 route 문자열을 직접 생성하면 hallucination 위험이 있다.
> AI는 `targetNodeId`만 반환하고, UI(또는 BFF)가 resolve한다.

```
AI 출력 (내부):
  targetNodeId: "kanban"
  deepLinkParams: { status: "blocked", sprintId: "sprint-5" }
  suggestedPreset: "PM_WORK"

UI resolve 과정:
  1. MenuOntology에서 nodeId="kanban" 노드 조회
  2. deepLinks에서 매칭되는 template 선택
  3. template에 params 치환: /kanban?status=blocked&sprintId=sprint-5
  4. requiredCaps 확인 → 권한 없으면 fallback 표시 ("권한 필요" 안내)
  5. suggestedPreset → deriveViewMode 결과와 비교 → 적용
```

```typescript
// AI Navigation Contract (v2.0)
interface AiNavigationResponse {
  answer: string;
  navigation: {
    primary: {
      targetNodeId: string;              // ★ route가 아닌 nodeId
      deepLinkParams?: Record<string, string>;
    };
    secondary?: {
      targetNodeId: string;
      deepLinkParams?: Record<string, string>;
    };
  };
  viewMode: {
    suggested: ViewModePreset;
    reason: string;
  };
  scopeHints?: Record<string, string>;   // { projectId: "PRJ-001" }
}
```

### 8.3 Hub 노드 페널티: "대시보드 만능 추천" 방지

> **문제**: 대시보드는 거의 모든 질문의 "첫 후보"가 되기 쉽다.
> "승인 대기 보여줘"는 대시보드가 아니라 `/deliverables`가 더 적절하다.

**스코어링 조정 규칙**:

```
Intent→Node 스코어링 (00_총괄 §9 기반):

기본 점수 계산 후, nodeRole에 따라 보정:

IF 질문에 행동/목록 동사 포함 ("보여줘", "목록", "열어줘", "이동", "승인", "등록", "생성"):
    IF node.nodeRole === "hub":
        score -= 20   // Hub 페널티
    IF node.nodeRole === "action" || node.nodeRole === "detail":
        score += 10   // Action/Detail 보너스

IF 질문이 요약/상태/판단 성격 ("어떤 상태야?", "전체 요약", "건강 점수는?"):
    IF node.nodeRole === "hub":
        score += 15   // Hub 보너스
```

**예시**:

| 질문 | Dashboard (hub) | Deliverables (action) | 최종 추천 |
|------|----------------|---------------------|----------|
| "승인 대기 건 보여줘" | 65 - 20 = **45** | 70 + 10 = **80** | `/deliverables` |
| "전체 상태 알려줘" | 80 + 15 = **95** | 40 = **40** | `/dashboard` |
| "리스크 목록 열어줘" | 60 - 20 = **40** | decisions: 75 + 10 = **85** | `/decisions?tab=risks` |

### 8.4 대시보드에서 AI 질문 시 Navigation 예시 (v2.0 수정)

| 질문 | AI 추천 nodeId | deepLinkParams | Preset | 비고 |
|------|---------------|---------------|--------|------|
| "병목이 어디야?" | `kanban` | `{ status: "blocked" }` | PM_WORK | action 노드 보너스 |
| "이번 스프린트 속도는?" | `sprints` | `{ sprintId: "current" }` | PM_WORK | detail 노드 |
| "승인 대기 건 보여줘" | `dashboard.approvals` (1차) | - | CUSTOMER_APPROVAL | **승인 허브 우선** |
| | `deliverables` (2차) | `{ filter: "pending" }` | CUSTOMER_APPROVAL | fallback |
| "리스크 목록 보여줘" | `decisions` | `{ tab: "risks" }` | PM_WORK | **탭 딥링크 명시** |
| "감사 증빙 준비 상태는?" | `audit-evidence` | - | AUDIT_EVIDENCE | 전용 화면 |
| "전체 진행률 알려줘" | `dashboard` | - | (현재 유지) | **hub 보너스** |

---

## 9. Preset 동적 전환 (v2.0 신규)

### 9.1 2단계 Preset 결정

```
기본 Preset (자동):
  Role → computeCapabilities() → deriveViewMode() → defaultPreset

임시 Preset (사용자 전환):
  Page Header의 [ Preset: PM ▼ ] 드롭다운 클릭
  → 선택 가능 Preset 목록 표시 (현재 역할 기준)
  → 선택 시 effectivePreset 변경
  → 세션 스토리지에 저장 ("이번 프로젝트에서만 기억")
```

### 9.2 전환 규칙 (안전 + 만족도 균형)

| 전환 방향 | 허용 | 제약 |
|----------|------|------|
| **하향** (PM → DEV_EXECUTION) | 허용 | 없음 (자기 권한으로 축소) |
| **동등** (PM → PM_WORK) | 허용 | 없음 |
| **상향** (DEV → PM_WORK) | 허용 (읽기 전용) | `ReadOnlyBadge` 강제, 모든 CTA 비활성 |
| **AUDIT_EVIDENCE → 다른** | **차단** | 감사 Preset은 locked (전환 불가) |
| **다른 → AUDIT_EVIDENCE** | **차단** | 감사 Preset은 진입 불가 (감사 역할만) |

### 9.3 ReadOnly Override 로직

```typescript
function usePresetSwitch() {
  const { viewMode: roleDefault } = useAccessContext();
  const [tempPreset, setTempPreset] = useSessionStorage<ViewModePreset | null>(
    `dashboard-preset-${projectId}`,
    null
  );

  const effectivePreset = tempPreset ?? roleDefault;

  // Preset 순위 정의 (높을수록 상위)
  const presetRank: Record<ViewModePreset, number> = {
    EXEC_SUMMARY: 5,
    PMO_CONTROL: 4,
    PM_WORK: 3,
    DEV_EXECUTION: 2,
    CUSTOMER_APPROVAL: 1,
    AUDIT_EVIDENCE: 0,  // locked
  };

  const isTemporarySwitch = tempPreset !== null && tempPreset !== roleDefault;
  const readOnlyOverride = isTemporarySwitch
    && presetRank[effectivePreset] > presetRank[roleDefault];

  return {
    effectivePreset,
    isTemporarySwitch,
    readOnlyOverride,
    switchPreset: (preset: ViewModePreset) => { ... },
    resetPreset: () => setTempPreset(null),
  };
}
```

### 9.4 Preset 전환 UI

```
Page Header:
┌───────────────────────────────────────────────────────┐
│  대시보드  [ AI 보험금지급심사 ▼ ]  [ Preset: PM ▼ ]   │
│                                     ┌─────────────┐   │
│                                     │ EXEC (읽기)  │   │
│                                     │ PMO (읽기)   │   │
│                                     │ ● PM         │   │ ← 현재
│                                     │ DEV          │   │
│                                     │ CUSTOMER     │   │
│                                     └─────────────┘   │
└───────────────────────────────────────────────────────┘

상향 전환 시:
┌───────────────────────────────────────────────────────┐
│  대시보드  [ Preset: EXEC ▼ ] [읽기 전용] [원래로 복귀]│
│  ⓘ 현재 읽기 전용 모드입니다 (EXEC 렌즈 임시 적용)     │
└───────────────────────────────────────────────────────┘
```

---

## 10. 인터랙션 상세

### 10.1 Phase Timeline 클릭

```
Phase Timeline의 단계 노드 클릭
  → Drawer 열림 (420px)
  → Phase Drilldown (해당 단계 상세)
  → 편차/원인/조치/관련이슈 표시
  → "단계별 관리에서 상세 보기" 링크 → nodeId: "phases", params: { phaseId }
```

### 10.2 KPI Card 클릭 (v2.0 — 탭 딥링크 명시)

```
Risk Count Card 클릭
  → nodeId: "decisions", deepLinkParams: { tab: "risks" }
  → resolve: /decisions?tab=risks

Issue Card 클릭
  → nodeId: "issues", deepLinkParams: { filter: "open" }
  → resolve: /issues?filter=open

Deliverable Pending Card 클릭
  → nodeId: "deliverables", deepLinkParams: { filter: "pending" }
  → resolve: /deliverables?filter=pending

Health Score Card 클릭 (PMO)
  → nodeId: "pmo-health"
  → resolve: /pmo/health
```

### 10.3 Portfolio Table 행 클릭 (PMO)

```
프로젝트 행 클릭
  → Right Panel 갱신 (해당 프로젝트 상세)
  → Panel 내 "프로젝트로 전환" 버튼 → 프로젝트 context 변경 + /dashboard 재로딩
```

### 10.4 데이터 새로고침

| 항목 | 주기 |
|------|------|
| KPI Cards | 5분 자동 갱신 (TanStack Query `refetchInterval`) |
| Phase Timeline | 페이지 진입 시 + 수동 새로고침 |
| AI Insight | 페이지 진입 시 1회 (캐시 10분) |
| 전체 | 수동 새로고침 버튼 (Page Header) |

### 10.5 Completeness Badge 인터랙션

```
KPI Card에 completeness !== "ok" 표시 시:
  → KPI 값 옆에 "데이터 부족" 배지 (amber)
  → 배지 hover → tooltip: warnings[].message 표시
  → 배지 click → 새로고침 시도 or 관리자 문의 안내
```

---

## 11. 반응형 대응

### 11.1 브레이크포인트별 레이아웃

| 너비 | KPI Row | Main Content | Right Panel |
|------|---------|-------------|-------------|
| ≥1440px | 4열 가로 배치 | 70% | 30% (visible) |
| 1280px | 4열 가로 배치 | 100% | Drawer (toggle) |
| ≤1024px | 2x2 그리드 | 100% | Drawer (overlay) |
| ≤768px | 1열 세로 스택 | 100% | Full Modal |

### 11.2 위젯 축소 규칙

- **≤1280px**: Phase Timeline → 축약 (아이콘 + 진행률만)
- **≤1024px**: Sprint Burndown Mini → 숨김
- **≤768px**: AI Insight → 접이식 (펼치기 버튼)

---

## 12. 접근성

| 항목 | 대응 |
|------|------|
| 색상 대비 | WCAG AA 기준 (4.5:1 이상) |
| 스크린 리더 | KPI Card에 `aria-label` ("전체 진척률 72%") |
| 키보드 | Tab으로 KPI → Timeline → Widget 순회, Enter로 상세 진입 |
| 상태 색상 | 색상 + 텍스트 라벨 병행 (색맹 대응) |
| Completeness Badge | `aria-live="polite"` (데이터 갱신 시 알림) |

---

## 13. 기존 화면 vs 개선 화면 비교

| 항목 | 기존 (엑셀 기반) | v1.0 | v2.0 (본 설계) |
|------|----------------|------|-------------|
| **화면 수** | 3종 (PMO/PM/공통) | 1개 + Preset 6종 | 1개 + Preset **5종** (AUDIT 제거) |
| **렌더링 방식** | 역할별 하드코딩 | switch 분기 (5개 Sub-Page) | **정책 기반 위젯 조립** (DashboardShell) |
| **정보 밀도** | 동일 | Preset 자동 조절 | Preset 자동 + **임시 전환** 지원 |
| **판단 지원** | "얼마나 했는지" | "무엇을 해야 하는지" | "왜 그런지" + "무엇을 해야 하는지" (AI 3단 구조) |
| **AI 연동** | 없음 | AI 요약 + CTA | AI **진단+근거+다음행동** + nodeId 기반 네비게이션 |
| **Health Score** | 없음 | 5-factor weighted | 5-factor + **결측 처리** + confidence level |
| **데이터 신뢰성** | 없음 | asOf만 | **completeness + warnings + provenance** |
| **승인 흐름** | 별도 화면 | Customer Preset 내 승인/반려 | 동일 + **승인 허브 우선 추천** |
| **Preset 전환** | N/A | 역할 기반 자동 | 자동 + **사용자 임시 전환** (상향 = ReadOnly) |
| **AI 추천 정확도** | N/A | route 직접 반환 | **nodeId 반환 + Hub 페널티 + 탭 딥링크** |

---

## 14. DoD (본 화면 완료 기준)

- [ ] 단일 `/dashboard` 라우트에서 **DashboardShell + 위젯 조립** 방식으로 5개 Preset 렌더링
- [ ] Health Score 자동 계산 + 등급(A~F) + **결측 처리 + confidence 레벨**
- [ ] KPI Card 클릭 시 **nodeId 기반 딥링크** 이동 (탭 파라미터 포함)
- [ ] Phase Timeline에서 단계 클릭 → Drawer 상세 표시
- [ ] PMO Preset: Portfolio Table + 행 클릭 → Right Panel 상세
- [ ] PM Preset: Action Items Panel + Sprint Burndown Mini
- [ ] DEV Preset: My Work Preview + 내 이슈 표시
- [ ] Customer Preset: 승인 대기 목록 + 즉시 승인/반려 가능
- [ ] **AI Insight Widget이 진단+근거+다음행동 3단 구조로 표시**
- [ ] **AI 네비게이션은 nodeId만 반환 → UI resolve → capability 필터링**
- [ ] **Preset 임시 전환**: 드롭다운으로 전환, 상향 시 ReadOnly 강제
- [ ] **completeness/warnings 배지**: 데이터 부족 시 KPI에 배지 표시
- [ ] 5분 자동 갱신 + 수동 새로고침
- [ ] 반응형 레이아웃 (≥1440 / 1280 / ≤1024 / ≤768)
- [ ] WCAG AA 접근성 준수
- [ ] **AUDIT_EVIDENCE Preset은 대시보드에서 제거** (감사 → `/audit/evidence` 직접 진입)
- [ ] **Hub 페널티 스코어링 적용**: 행동/목록 질문 시 대시보드 추천 억제

---

## 부록 A. v1.0 → v2.0 변경 이력

| 섹션 | 변경 | 사유 |
|------|------|------|
| §1.1 | "6개 Preset" → "5개 Preset" | AUDIT 제거 |
| §1.2 | "정책 기반 조립" 원칙 추가 | Sub-Page 분기 → 위젯 조립 |
| §2 전체 | MenuOntologyNodeV2 타입 확정 | 타입 불일치 해소 |
| §2.3 | entities PascalCase, highlight 분리, suggestedActions 확장, scope 2계층, virtualNodes | 피드백 7개 항목 반영 |
| §3.6 | AUDIT_EVIDENCE 와이어프레임 삭제 | 감사는 대시보드 접근 불가 |
| §4.3 | Health Score factor별 정의 + 결측 처리 | "0점 착시" 방지 |
| §5.1 | completeness/warnings/provenance 추가 | 데이터 신뢰성 3종 |
| §6 전체 | 위젯 조립 아키텍처 (신규) | switch 분기 → 정책 기반 |
| §7 | 폴더 구조 reorganize (layout/, config/) | Shell + Registry 패턴 |
| §8 전체 | AI Insight 3단 구조, nodeId 기반 Nav, Hub 페널티 | AI 추천 정확도 향상 |
| §9 전체 | Preset 동적 전환 규칙 (신규) | 임시 전환 + ReadOnly |
| §10.2 | KPI 딥링크에 tab 파라미터 추가 | Decision/Risk 혼선 해소 |
