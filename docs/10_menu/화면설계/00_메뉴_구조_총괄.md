# AIMAX AI-PMS 메뉴 구조 총괄 설계서

> 작성일: 2026-02-08
> 버전: v2.0
> 범위: 전체 좌측 메뉴(SideNav) 구조, 역할별 노출 규칙, IA(정보 구조) 맵, **MenuOntology**, **AI Navigation Contract**, **Preset 동적 전환**, **Intent→Node 스코어링**

---

## 1. 설계 원칙

### 1.1 핵심 철학 — AI ↔ Menu ↔ Preset 삼각 연결

```
┌────────────────────────────────────────────────────────┐
│                   AI (Navigator)                        │
│   "사용자 질문을 해석하고 최적 화면+렌즈로 안내한다"    │
│                                                        │
│        Intent 분류 → Node 추천 → Preset 제안           │
│              ▲                        │                │
│              │                        ▼                │
│   ┌──────────┴───────┐   ┌──────────────────────┐      │
│   │  Menu (Ontology) │   │  Preset (Lens)       │      │
│   │  "도메인 지도"    │◄──│  "UI 렌더링 정책"    │      │
│   │                  │   │                      │      │
│   │  nodeId, route,  │   │  density, panels,    │      │
│   │  intents,        │   │  suggestedActions,   │      │
│   │  entities,       │   │  defaultRightPanel   │      │
│   │  keywords,       │   │                      │      │
│   │  requiredCaps    │   │  ≠ Capability        │      │
│   └──────────────────┘   └──────────────────────┘      │
│                                                        │
│   "메뉴 = 단순 링크 목록 ❌  →  도메인 온톨로지 ✅"    │
│   "Preset = Role ❌  →  렌즈(시각 정책) ✅"             │
│   "AI = 단순 답변 ❌  →  화면 네비게이터 ✅"            │
└────────────────────────────────────────────────────────┘
```

**3가지 핵심 원칙:**

1. **메뉴는 도메인 온톨로지다** — `menuConfig.ts`는 단순 라우트 배열이 아니라, 각 노드가 intent·entity·keyword·metric을 보유하는 **도메인 지도(MenuOntologyNode)**
2. **Preset은 렌즈(Lens)이지 역할(Role)이 아니다** — Preset은 UI 밀도·패널·필터·추천 액션 등 **시각 정책**을 결정하고, 실제 실행 권한은 Capability가 결정한다. "렌즈는 바뀌어도 권한은 안 오른다"
3. **AI는 네비게이터다** — AI 응답에는 항상 `navigation.primary.route` + `viewMode.suggested`가 포함되어, 사용자를 **최적 화면+최적 렌즈**로 안내한다

### 1.2 메뉴 노출 규칙
- 권한 없는 메뉴는 **숨김** (비활성화가 아닌 완전 미노출)
- `MenuOntologyNode.requiredCaps`를 기준으로 `computeCapabilities()` 결과와 비교하여 필터링
- 동일한 Capability 기준이 메뉴 노출 / 라우트 가드 / 버튼 활성화에 일관 적용

### 1.3 Preset(ViewMode) 정의 — "렌즈" 모델

| Preset | 의미 | 대표 대상 | UI 밀도 | 기본 Right Panel |
|--------|------|---------|---------|-----------------|
| `EXEC_SUMMARY` | 요약/결정 중심 | Sponsor | `compact` | `closed` |
| `PMO_CONTROL` | 통제/리포트/승인 | PMO | `standard` | `open` |
| `PM_WORK` | 계획/조정/관리 | PM | `detailed` | `open` |
| `DEV_EXECUTION` | 실행/흐름/내작업 | DEV / DevReader | `standard` | `closed` |
| `CUSTOMER_APPROVAL` | 승인/반려 전용 | Customer PM | `compact` | `open` |
| `AUDIT_EVIDENCE` | 증빙 Export 전용 | External Auditor | `compact` | `closed` |

> **핵심**: 동일 화면(`/kanban`)이라도 Preset에 따라 보이는 컬럼, 필터, 추천 액션이 달라진다.
> Preset 전환은 자유이나, **상위 Preset으로 전환 시 읽기 전용**으로 강제된다 (→ §8 참조).

---

## 2. 최종 메뉴 구조 (확정안)

```
AIMAX AI-PMS
│
├─ 1. Overview (개요)
│   └─ 대시보드                          /dashboard
│
├─ 2. Plan & Structure (계획/구조)
│   ├─ 요구사항                           /requirements
│   ├─ 백로그(EPIC)                       /backlog
│   ├─ WBS                               /wbs
│   └─ 단계(Phase)                       /phases
│
├─ 3. Execution (실행)
│   ├─ 칸반 보드                          /kanban
│   ├─ 스프린트                           /sprints
│   └─ 내 작업                            /my-work
│
├─ 4. Control (통제/품질)
│   ├─ 이슈 관리                          /issues
│   ├─ 테스트 관리                        /tests
│   ├─ 산출물 관리                        /deliverables
│   └─ 결정/리스크 보드                   /decisions
│
├─ 5. Traceability (추적)
│   └─ Lineage & History                 /lineage
│
├─ 6. Reports (보고)
│   ├─ 프로젝트 리포트                    /reports
│   └─ 통계 대시보드                      /stats
│
├─ 7. PMO / Governance (거버넌스)
│   ├─ PMO 대시보드                       /pmo
│   ├─ Health Matrix                     /pmo/health
│   ├─ 프로젝트 관리 (책임 주체)          /project-management
│   └─ 역할 및 권한 관리                  /role-permission
│
├─ 8. Audit (감사)
│   └─ Evidence Export                   /audit/evidence
│
├─ 9. Collaboration (협업)
│   ├─ 회의 관리                          /meetings
│   └─ 공지사항                           /notices
│
├─ 10. Tools (도구)
│    ├─ AI 어시스턴트                     /ai-assistant
│    └─ 교육 관리                         /education
│
└─ 11. Admin (관리)
     ├─ 프로젝트 설정                     /admin/project
     └─ 시스템 설정                       /admin/system
```

---

## 3. 역할별 메뉴 노출 매트릭스

> `O` = 노출, `-` = 숨김, `(조건)` = 특정 조건 충족 시 노출

| 메뉴 | Sponsor | PMO | PM | DevReader | DEV | Customer PM | 외부 감사 | 시스템 운영자 |
|------|---------|-----|----|-----------|-----|-------------|---------|------------|
| **대시보드** | O | O | O | O | O(요약) | O | - | O |
| **요구사항** | O(요약) | O | O | O | O | O(요청/검토) | - | - |
| **백로그(EPIC)** | O | O | O | O(팀범위) | O | O(요약) | - | - |
| **WBS** | O | O | O | O(팀범위) | O | O | - | - |
| **단계(Phase)** | O | O | O | O | O | O | - | - |
| **칸반 보드** | O(요약) | O(병목) | O | O | O | O(읽기) | - | - |
| **스프린트** | O(요약) | O | O | O | O | O | - | - |
| **내 작업** | - | - | - | - | O | - | - | - |
| **이슈 관리** | O(요약) | O | O | O | O(등록/코멘트) | O(등록/요청) | - | - |
| **테스트 관리** | O(요약) | O | O | O | O(QA=RW) | O | - | - |
| **산출물 관리** | O | O | O | O | O(업로드/조회) | O(다운로드제한) | - | - |
| **결정/리스크** | O(승인) | O | O | O | O | O(승인/요청) | - | - |
| **Lineage** | O | O | O(R/W) | O | O | O(제한) | O | - |
| **프로젝트 리포트** | O | O(Export) | O(R/W) | O | - | O | O(R/W Export) | - |
| **통계 대시보드** | O | O | O | O | - | O | O | - |
| **PMO 대시보드** | O(요약) | O | - | - | - | O(요약) | O | - |
| **Health Matrix** | - | O | - | - | - | - | - | - |
| **프로젝트 관리 (책임 주체)** | - | O | O | - | - | - | - | O |
| **파트 관리** | - | O | O | - | - | - | - | O |
| **역할 및 권한 관리** | - | O | O | - | - | - | - | O |
| **Evidence Export** | - | - | - | - | - | - | O | - |
| **회의 관리** | O | O | O | O | O | O | - | - |
| **공지사항** | O | O | O | O | O | O | - | O |
| **AI 어시스턴트** | - | - | O | O | O | - | - | - |
| **교육 관리** | - | O | O | O | O | - | - | O |
| **프로젝트 설정** | - | O(제한) | O | - | - | - | - | - |
| **시스템 설정** | - | - | - | - | - | - | - | O |

---

## 4. 역할별 사이드바 실제 노출 요약

### 4.1 Sponsor
```
대시보드
├─ PMO 대시보드 / Reports / 결정·리스크 / Dashboard만 노출
```
- 의사결정과 보고서 열람에 집중
- Kanban/Backlog은 "Drill-down 읽기"로만 노출(선택)

### 4.2 PMO
```
대시보드
├─ PMO 대시보드 / Health Matrix / Reports
├─ 프로젝트 관리(책임 주체) / 파트 관리 / 역할 및 권한 관리
├─ Lineage / Issues(통제)
├─ Kanban/Backlog은 "Drill-down 읽기"로만 노출(선택)
```

### 4.3 PM
```
Overview + Plan & Structure + Control + Reports 중심
├─ 대시보드 / 요구사항 / 백로그 / WBS / 단계
├─ 칸반 / 스프린트
├─ 이슈 / 테스트 / 산출물 / 결정·리스크
├─ Lineage / Reports
├─ 프로젝트 관리(책임 주체) / 파트 관리 / 역할 및 권한 관리
├─ 프로젝트 설정
```

### 4.4 DevReader(파트장)
```
Execution + Control + 일부 Plan(WBS/Backlog 읽기) 중심
├─ 대시보드 / 백로그(팀범위) / WBS(팀범위) / 단계
├─ 칸반 / 스프린트
├─ 이슈 / 테스트 / 산출물 / 결정·리스크
├─ Lineage
```

### 4.5 DEV
```
Execution(칸반/내작업) + Issues(등록) + Deliverables(업로드) 중심
├─ 대시보드 / 백로그(읽기) / WBS(읽기) / 단계(읽기)
├─ 칸반 / 스프린트 / 내 작업
├─ 이슈(등록/코멘트) / 테스트(QA만 RW) / 산출물(업로드)
├─ AI 어시스턴트
```

### 4.6 Customer PM
```
Overview + Roadmap/Backlog 요약 + Issues/Decision(승인/요청) + Reports
├─ 대시보드 / 요구사항(요청/검토)
├─ 백로그(요약) / WBS / 단계
├─ 칸반(읽기) / 스프린트
├─ 이슈(등록/요청) / 산출물(다운로드제한) / 결정(승인/요청)
├─ Lineage(제한) / Reports
```

### 4.7 외부 감사
```
Reports + Lineage + Deliverables(읽기/다운로드 제한) 중심
├─ Lineage & History
├─ 프로젝트 리포트(Export) / 통계
├─ PMO 대시보드(요약)
├─ Evidence Export
```

### 4.8 시스템 운영자
```
Admin(System) + Admin(Project) + 모든 화면(운영목적)
├─ 프로젝트 설정 / 시스템 설정
├─ 프로젝트 관리(책임 주체) / 파트 관리 / 역할 및 권한 관리
├─ (운영 필요 시 전체 화면 접근 가능)
```

---

## 5. IA(정보 구조) 맵 — 질문 흐름 기반

```
┌──────────────────────────────────────────────────────────────┐
│  "지금 전체 상황이 어떤가?"                                    │
│  → 대시보드 (Overview)                                        │
│     ├─ Sponsor: 요약 KPI + Health                            │
│     ├─ PM: 프로젝트 상세 + Risk + Action Items               │
│     └─ DEV: 내 할 일 요약                                    │
├──────────────────────────────────────────────────────────────┤
│  "무엇을 할 것인가?"                                          │
│  → Plan & Structure                                          │
│     ├─ 요구사항: 고객 RFP → 요구사항 분류/추적                │
│     ├─ 백로그: Epic/Feature/Story 구조 편집                   │
│     ├─ WBS: 작업분해/일정 관리                                │
│     └─ 단계: 프로젝트 단계별 진행/상태 관리                   │
├──────────────────────────────────────────────────────────────┤
│  "지금 어떻게 흐르는가?"                                      │
│  → Execution                                                 │
│     ├─ 칸반: 흐름/병목 시각화                                 │
│     ├─ 스프린트: 스프린트 운영                                │
│     └─ 내 작업: 개인 집중 (DEV 전용)                          │
├──────────────────────────────────────────────────────────────┤
│  "뭘 문제로 결정하나?"                                        │
│  → Control                                                   │
│     ├─ 이슈: 이슈/장애/리스크 등록·처리                       │
│     ├─ 테스트: 품질/테스트 실행·결과                          │
│     ├─ 산출물: 산출물 업로드/승인                             │
│     └─ 결정/리스크: 결정·승인·에스컬레이션                    │
├──────────────────────────────────────────────────────────────┤
│  "영향/변경 추적"                                              │
│  → Traceability                                               │
│     └─ Lineage & History: 관계/변경/영향 분석                 │
├──────────────────────────────────────────────────────────────┤
│  "공유/보고"                                                   │
│  → Reports                                                    │
│     ├─ 프로젝트 리포트                                        │
│     └─ 통계 대시보드                                          │
├──────────────────────────────────────────────────────────────┤
│  "프로젝트 밖에서 프로젝트들을 본다"                           │
│  → PMO / Governance                                           │
│     ├─ PMO 대시보드: 포트폴리오 요약                          │
│     ├─ Health Matrix: 프로젝트 건강도 매트릭스                │
│     ├─ 프로젝트 관리: PM/Co-PM/Sponsor 책임 주체 관리        │
│     └─ 역할 및 권한 관리: 위임/역할/거버넌스 감사            │
├──────────────────────────────────────────────────────────────┤
│  "감사 증빙 제출"                                              │
│  → Audit                                                      │
│     └─ Evidence Export: 증빙 패키지 생성/다운로드              │
├──────────────────────────────────────────────────────────────┤
│  "협업/소통"                                                   │
│  → Collaboration                                              │
│     ├─ 회의 관리: 발표/주간보고/비정기 회의                   │
│     └─ 공지사항                                               │
├──────────────────────────────────────────────────────────────┤
│  "도구/학습"                                                   │
│  → Tools                                                      │
│     ├─ AI 어시스턴트                                          │
│     └─ 교육 관리: 업무대상 교육 / IT대상 교육                 │
├──────────────────────────────────────────────────────────────┤
│  "시스템 설정"                                                 │
│  → Admin                                                      │
│     ├─ 프로젝트 설정                                          │
│     └─ 시스템 설정                                            │
└──────────────────────────────────────────────────────────────┘
```

---

## 6. MenuOntology 스키마 정의

### 6.1 개념 — menuConfig.ts의 진화

기존 `menuConfig.ts`는 `{ label, route, icon, requiredCaps }` 수준의 **라우트 배열**이었다.
이를 **도메인 온톨로지**로 업그레이드하여, 각 메뉴 노드가 AI Intent·Entity·Keyword·Metric·DeepLink·PresetPolicy를 보유하는 **지식 그래프 노드**로 확장한다.

```
기존 menuConfig         →    MenuOntology
────────────────────         ────────────────────
label, route, icon           + intents[]
requiredCaps[]               + canonicalQuestions[]
                             + entities[]
                             + keywords[]
                             + metrics[]
                             + deepLinks[]
                             + presetPolicies[]
                             + defaultPreset
                             + domain
                             + priority
                             + scopeHints[]
```

### 6.2 TypeScript 타입 정의

```typescript
// ─── 기본 타입 ───────────────────────────────────────────

export type MenuDomain =
  | "overview"
  | "plan"
  | "execution"
  | "control"
  | "trace"
  | "reports"
  | "governance"
  | "audit"
  | "collaboration"
  | "tools"
  | "admin";

export type ViewModePreset =
  | "EXEC_SUMMARY"
  | "PMO_CONTROL"
  | "PM_WORK"
  | "DEV_EXECUTION"
  | "CUSTOMER_APPROVAL"
  | "AUDIT_EVIDENCE";

export type Capability =
  | "view_dashboard"
  | "view_backlog"
  | "manage_backlog"
  | "view_kanban"
  | "manage_kanban"
  | "view_issues"
  | "manage_issues"
  | "view_tests"
  | "manage_tests"
  | "view_deliverables"
  | "manage_deliverables"
  | "approve_deliverable"
  | "view_decisions"
  | "manage_decisions"
  | "view_lineage"
  | "view_reports"
  | "export_reports"
  | "view_pmo"
  | "view_phases"
  | "manage_phases"
  | "view_requirements"
  | "manage_requirements"
  | "view_wbs"
  | "manage_wbs"
  | "view_sprints"
  | "manage_sprints"
  | "view_my_work"
  | "export_audit_evidence"
  | "view_meetings"
  | "view_notices"
  | "view_ai_assistant"
  | "view_education"
  | "view_stats"
  | "admin_project"
  | "admin_system"
  // Governance / Authority (Screens #20, #21, #22)
  | "view_projects"
  | "manage_projects"
  | "view_roles"
  | "manage_roles"
  | "view_parts"
  | "manage_parts"
  | "manage_part_members"
  | "view_users"
  | "manage_users"
  | "view_rfp"
  | "manage_rfp"
  | "edit_project_accountability"
  | "view_role_permission"
  | "manage_delegations"
  | "manage_capabilities"
  | "audit_governance";

export type EntityType =
  | "project"
  | "phase"
  | "epic"
  | "feature"
  | "story"
  | "task"
  | "issue"
  | "risk"
  | "decision"
  | "deliverable"
  | "test_case"
  | "sprint"
  | "requirement"
  | "wbs_item"
  | "meeting"
  | "user";

export type IntentTag =
  // Ask 계열 (조회/질문)
  | "ask_overall_status"
  | "ask_progress"
  | "ask_bottleneck"
  | "ask_risk"
  | "ask_decision_pending"
  | "ask_my_work"
  | "ask_trace_impact"
  | "ask_report_export"
  | "ask_phase_status"
  | "ask_sprint_velocity"
  | "ask_test_coverage"
  | "ask_deliverable_status"
  | "ask_requirement_status"
  | "ask_wbs_status"
  | "ask_health_score"
  | "ask_audit_readiness"
  // Do 계열 (실행/변경)
  | "do_create_issue"
  | "do_approve_deliverable"
  | "do_export_evidence"
  | "do_move_task"
  | "do_create_sprint"
  | "do_assign_task"
  | "do_escalate_risk"
  | "do_update_phase"
  | "do_upload_deliverable"
  | "do_create_meeting"
  | "do_export_report";

export type ScopeKey =
  | "projectId"
  | "phaseId"
  | "partId"
  | "sprintId"
  | "epicId";

// ─── DeepLink 템플릿 ────────────────────────────────────

export interface DeepLinkTemplate {
  pattern: string;        // e.g., "/kanban?status={status}&assignee={userId}"
  description: string;    // e.g., "상태별 칸반 필터"
  requiredParams: string[];
}

// ─── Preset 정책 ─────────────────────────────────────────

export interface PresetPolicy {
  preset: ViewModePreset;
  ui: {
    density: "compact" | "standard" | "detailed";
    defaultRightPanel: "closed" | "open";
    defaultFilters?: Record<string, string>;
    hiddenColumns?: string[];
    highlightMetrics?: string[];
  };
  suggestedActions?: {
    key: string;
    label: string;
    capability?: Capability;
  }[];
}

// ─── MenuOntologyNode (핵심) ─────────────────────────────

export interface MenuOntologyNode {
  // 기본 식별
  nodeId: string;                    // e.g., "kanban", "audit-evidence"
  label: string;                     // 한글 라벨
  route: string;                     // React Router 경로
  icon: string;                      // 아이콘 이름
  domain: MenuDomain;                // 소속 도메인

  // 권한
  requiredCaps: Capability[];        // 메뉴 노출에 필요한 Capability

  // AI 연결 — Intent / Entity / Keyword / Metric
  intents: IntentTag[];              // 이 노드와 연관된 인텐트 목록
  canonicalQuestions: string[];      // 대표 질문 (AI 학습/매칭용)
  entities: EntityType[];            // 관련 엔티티 타입
  keywords: string[];                // 한글/영문 키워드 (fuzzy match용)
  metrics?: string[];                // 관련 지표명 (KPI 매칭용)

  // Preset 정책
  defaultPreset: ViewModePreset;     // 기본 Preset
  presetPolicies: PresetPolicy[];    // Preset별 UI 정책

  // DeepLink
  deepLinks?: DeepLinkTemplate[];    // 파라미터화된 딥링크 템플릿

  // 스코어링 보조
  priority?: number;                 // 동점 시 우선순위 (낮을수록 우선)
  scopeHints?: ScopeKey[];           // 필요한 스코프 키
}
```

### 6.3 필드별 역할 설명

| 필드 | 역할 | 사용처 |
|------|------|--------|
| `intents` | 이 화면이 대답할 수 있는 사용자 인텐트 목록 | AI Intent→Node 매칭 (§9) |
| `canonicalQuestions` | 대표 질문 텍스트 (임베딩/키워드 매칭 대상) | AI가 "비슷한 질문"을 이 노드에 매핑 |
| `entities` | 이 화면이 다루는 엔티티 타입 | Entity mention 감지 시 스코어 가산 |
| `keywords` | 한글/영문 키워드 (정규화 후 매칭) | Korean normalizer + fuzzy match 연동 |
| `metrics` | 관련 KPI/지표명 | "번다운" → Sprint, "커버리지" → Tests |
| `deepLinks` | 파라미터화된 URL 템플릿 | AI CTA 버튼에 직접 링크 생성 |
| `presetPolicies` | Preset별 UI 렌더링 정책 | ViewMode 전환 시 UI 분기 |
| `defaultPreset` | 사용자 역할 기반 기본 Preset | 최초 진입 시 적용할 렌즈 |
| `priority` | 동점 시 노드 우선순위 | 여러 노드가 동일 점수일 때 tiebreaker |
| `scopeHints` | 필요한 스코프 파라미터 | AI가 응답에 projectId, phaseId 등 포함 |

### 6.4 샘플 Ontology 노드

#### 칸반 보드 (`kanban`)

```typescript
const kanbanNode: MenuOntologyNode = {
  nodeId: "kanban",
  label: "칸반 보드",
  route: "/kanban",
  icon: "ViewKanban",
  domain: "execution",

  requiredCaps: ["view_kanban"],

  intents: [
    "ask_bottleneck",
    "ask_progress",
    "ask_my_work",
    "do_move_task",
    "do_assign_task",
  ],
  canonicalQuestions: [
    "지금 병목이 어디야?",
    "In Progress에 머물러 있는 태스크가 몇 개야?",
    "이번 주 완료된 작업은?",
    "내 태스크를 Done으로 옮기고 싶어",
  ],
  entities: ["task", "sprint", "user"],
  keywords: ["칸반", "보드", "병목", "WIP", "흐름", "kanban", "bottleneck", "board"],
  metrics: ["wip_count", "cycle_time", "throughput", "blocked_count"],

  defaultPreset: "PM_WORK",
  presetPolicies: [
    {
      preset: "PM_WORK",
      ui: {
        density: "detailed",
        defaultRightPanel: "open",
        highlightMetrics: ["cycle_time", "blocked_count"],
      },
      suggestedActions: [
        { key: "assign", label: "담당자 배정", capability: "manage_kanban" },
        { key: "escalate", label: "이슈 에스컬레이션", capability: "manage_issues" },
      ],
    },
    {
      preset: "PMO_CONTROL",
      ui: {
        density: "standard",
        defaultRightPanel: "open",
        defaultFilters: { view: "bottleneck" },
        highlightMetrics: ["wip_count", "throughput"],
      },
      suggestedActions: [
        { key: "export", label: "병목 리포트 Export", capability: "export_reports" },
      ],
    },
    {
      preset: "DEV_EXECUTION",
      ui: {
        density: "standard",
        defaultRightPanel: "closed",
        defaultFilters: { assignee: "me" },
      },
      suggestedActions: [
        { key: "move", label: "태스크 이동", capability: "manage_kanban" },
      ],
    },
    {
      preset: "EXEC_SUMMARY",
      ui: {
        density: "compact",
        defaultRightPanel: "closed",
        hiddenColumns: ["assignee", "estimate"],
        highlightMetrics: ["throughput"],
      },
    },
  ],

  deepLinks: [
    {
      pattern: "/kanban?status={status}",
      description: "상태별 칸반 필터",
      requiredParams: ["status"],
    },
    {
      pattern: "/kanban?assignee={userId}",
      description: "담당자별 칸반 필터",
      requiredParams: ["userId"],
    },
    {
      pattern: "/kanban?sprint={sprintId}",
      description: "스프린트별 칸반 필터",
      requiredParams: ["sprintId"],
    },
  ],

  priority: 3,
  scopeHints: ["projectId", "sprintId"],
};
```

#### 감사 증빙 (`audit-evidence`)

```typescript
const auditEvidenceNode: MenuOntologyNode = {
  nodeId: "audit-evidence",
  label: "Evidence Export",
  route: "/audit/evidence",
  icon: "GavelRounded",
  domain: "audit",

  requiredCaps: ["export_audit_evidence"],

  intents: [
    "ask_audit_readiness",
    "do_export_evidence",
  ],
  canonicalQuestions: [
    "감사 증빙 자료를 내보내야 해",
    "현재 증빙 준비 상태는?",
    "이번 감사에 필요한 산출물은 다 올라왔어?",
  ],
  entities: ["deliverable", "decision", "test_case"],
  keywords: ["감사", "증빙", "export", "evidence", "audit", "패키지"],
  metrics: ["evidence_coverage", "pending_approvals"],

  defaultPreset: "AUDIT_EVIDENCE",
  presetPolicies: [
    {
      preset: "AUDIT_EVIDENCE",
      ui: {
        density: "compact",
        defaultRightPanel: "closed",
      },
      suggestedActions: [
        { key: "export_all", label: "전체 증빙 패키지 생성", capability: "export_audit_evidence" },
        { key: "export_phase", label: "단계별 증빙 Export", capability: "export_audit_evidence" },
      ],
    },
  ],

  deepLinks: [
    {
      pattern: "/audit/evidence?phase={phaseId}",
      description: "단계별 증빙 필터",
      requiredParams: ["phaseId"],
    },
  ],

  priority: 10,
  scopeHints: ["projectId", "phaseId"],
};
```

---

## 7. AI Navigation Contract

### 7.1 개념

AI 응답은 단순 텍스트가 아니라, **표준 Navigation Contract**를 포함한다.
사용자가 AI에게 질문하면, AI는 답변과 함께 **"지금 어디로 가면 좋겠다"**는 추천을 항상 반환한다.

```
┌─────────────────────────────────────────────┐
│  사용자: "병목이 어디야?"                     │
│                                             │
│  AI 응답:                                    │
│  ├─ answer: 텍스트 답변 (요약/하이라이트)    │
│  ├─ navigation:                              │
│  │   ├─ primary: /kanban (병목 확인)         │
│  │   └─ secondary: /issues (블로커 등록)     │
│  ├─ viewMode: PMO_CONTROL (conf=0.78)       │
│  ├─ requiredCaps: [view_kanban, view_issues] │
│  └─ scopeHints: { projectId, partId }       │
└─────────────────────────────────────────────┘
```

### 7.2 응답 스키마

```typescript
export interface AiNavigationResponse {
  // 답변 본문
  answer: {
    summary: string;                  // 핵심 요약 (1-2문장)
    highlights: string[];             // 강조 포인트 목록
    warnings?: string[];              // 주의 사항
  };

  // 화면 추천
  navigation: {
    primary: {
      route: string;                  // e.g., "/kanban"
      label: string;                  // e.g., "칸반 보드"
      reason: string;                 // e.g., "병목 확인"
    };
    secondary?: {
      route: string;
      label: string;
      reason: string;
    }[];
  };

  // Preset 추천
  viewMode: {
    suggested: ViewModePreset;        // 추천 Preset
    confidence: number;               // 0.0 ~ 1.0
    why: string[];                    // 추천 이유 (디버깅/UX용)
  };

  // 권한 요구
  requiredCaps: Capability[];         // 이 추천을 실행하기 위해 필요한 Capability

  // 스코프 힌트
  scopeHints?: Record<ScopeKey, string>;  // e.g., { projectId: "p-001", partId: "part-002" }
}
```

### 7.3 응답 예시 (JSON)

```json
{
  "answer": {
    "summary": "현재 '인수테스트' 단계에서 3건의 태스크가 5일 이상 In Review 상태에 머물러 있습니다.",
    "highlights": [
      "Review 병목 태스크: TSK-142, TSK-155, TSK-161",
      "공통 원인: QA 리소스 부족 (현재 1명 배정)"
    ],
    "warnings": [
      "이번 스프린트 종료까지 3일 남음 — 미해결 시 스프린트 목표 미달 가능"
    ]
  },
  "navigation": {
    "primary": {
      "route": "/kanban",
      "label": "칸반 보드",
      "reason": "병목 태스크 직접 확인 및 리소스 재배치"
    },
    "secondary": [
      {
        "route": "/issues",
        "label": "이슈 관리",
        "reason": "블로커 이슈 등록 및 에스컬레이션"
      },
      {
        "route": "/sprints",
        "label": "스프린트",
        "reason": "스프린트 범위 조정 검토"
      }
    ]
  },
  "viewMode": {
    "suggested": "PMO_CONTROL",
    "confidence": 0.78,
    "why": [
      "질문이 '통제/병목/지표' 중심",
      "3건 이상 정체 태스크 → 관리자 시점 필요"
    ]
  },
  "requiredCaps": ["view_kanban", "view_issues"],
  "scopeHints": {
    "projectId": "p-001",
    "partId": "part-002"
  }
}
```

### 7.4 프론트엔드 렌더링 규칙

1. **CTA 버튼 생성**: `navigation.primary`를 기반으로 메인 CTA 버튼을 렌더링
2. **권한 검사**: `requiredCaps`를 현재 사용자의 Capability와 대조 → 권한 없으면 CTA 비활성화 + 안내 텍스트
3. **Preset 적용**: CTA 클릭 시 `viewMode.suggested`로 Preset 전환 (단, 상위 전환 시 Read-only 적용)
4. **Secondary 추천**: 접이식 "관련 화면" 섹션으로 노출

```
┌─ AI 응답 카드 ───────────────────────────────┐
│                                               │
│  💬 "인수테스트 단계에서 3건의 태스크가..."    │
│                                               │
│  ▸ TSK-142, TSK-155, TSK-161                  │
│  ▸ QA 리소스 부족 (현재 1명 배정)             │
│                                               │
│  ⚠ 스프린트 종료까지 3일                       │
│                                               │
│  ┌──────────────────────┐                     │
│  │  ▶ 칸반 보드에서 확인 │  ← Primary CTA     │
│  └──────────────────────┘                     │
│  📋 관련: 이슈 관리 · 스프린트  ← Secondary   │
│                                               │
│  🔍 PMO_CONTROL 렌즈 추천 (78%)              │
└───────────────────────────────────────────────┘
```

### 7.5 Guardrails (안전장치)

| 규칙 | 설명 |
|------|------|
| **Capability 미충족 시** | CTA 비활성화 + "이 화면에 접근 권한이 없습니다" 표시 |
| **confidence < 0.5** | viewMode 추천을 숨기고 기본 Preset 유지 |
| **navigation 없음** | 답변만 표시, CTA 없음 (Casual 응답) |
| **route 유효성** | 존재하지 않는 route면 CTA 미생성 + 로그 기록 |
| **scopeHints 불완전** | 필수 scope(projectId) 누락 시 AI가 재질문 |

---

## 8. Preset 동적 전환 정책

### 8.1 핵심 원칙: Lens ≠ Capability

```
┌───────────────────────────────────────────────────┐
│                                                   │
│   Preset (Lens)        ≠    Capability (Power)    │
│   ─────────────              ─────────────        │
│   "무엇을 보여줄까"          "무엇을 할 수 있나"   │
│                                                   │
│   UI 밀도, 패널,              create, update,      │
│   필터, 추천 액션             delete, approve,     │
│   하이라이트 지표             export               │
│                                                   │
│   🔄 자유 전환 가능           🔒 역할 기반 고정    │
│   (단, 상위 전환 = R/O)                           │
│                                                   │
│   "렌즈는 바뀌어도 권한은 안 오른다"               │
│                                                   │
└───────────────────────────────────────────────────┘
```

### 8.2 Preset 계층 (권한 높이)

```
높음  ← EXEC_SUMMARY (Sponsor 시점)
      ← PMO_CONTROL  (PMO 시점)
      ← PM_WORK      (PM 시점)
      ← DEV_EXECUTION (DEV 시점)
      ← CUSTOMER_APPROVAL (Customer 시점)
낮음  ← AUDIT_EVIDENCE (Auditor 시점)
```

### 8.3 전환 정책 매트릭스

| 현재 역할 기본 Preset | 전환 대상 | 허용 | 모드 |
|---------------------|----------|------|------|
| DEV_EXECUTION | PM_WORK | ✅ | **Read-only** (모든 Write 액션 비활성) |
| DEV_EXECUTION | PMO_CONTROL | ✅ | **Read-only** |
| DEV_EXECUTION | EXEC_SUMMARY | ✅ | **Read-only** |
| PM_WORK | PMO_CONTROL | ✅ | **Read-only** |
| PM_WORK | DEV_EXECUTION | ✅ | Full (자기 Capability 범위 내) |
| PMO_CONTROL | PM_WORK | ✅ | Full (자기 Capability 범위 내) |
| PMO_CONTROL | EXEC_SUMMARY | ✅ | **Read-only** |
| EXEC_SUMMARY | 하위 전부 | ✅ | **Read-only** (Sponsor는 본래 조회 전용) |
| AUDIT_EVIDENCE | 모든 다른 | ❌ | **차단** (감사 전용 Preset에서는 전환 불가) |

### 8.4 전환 시 UI 동작

```typescript
function applyPresetTransition(
  currentPreset: ViewModePreset,
  targetPreset: ViewModePreset,
  userCaps: Capability[]
): PresetTransitionResult {
  const currentLevel = PRESET_HIERARCHY[currentPreset];
  const targetLevel = PRESET_HIERARCHY[targetPreset];

  // 감사 Preset은 전환 차단
  if (currentPreset === "AUDIT_EVIDENCE") {
    return { allowed: false, reason: "AUDIT_EVIDENCE preset is locked" };
  }

  // 상위 전환 = Read-only 강제
  const isUpward = targetLevel > currentLevel;

  return {
    allowed: true,
    readOnly: isUpward,
    activeActions: isUpward
      ? []  // 모든 Write 액션 비활성
      : filterByCapability(targetPreset.suggestedActions, userCaps),
    notification: isUpward
      ? `${targetPreset} 렌즈로 전환합니다 (읽기 전용)`
      : `${targetPreset} 렌즈로 전환합니다`,
  };
}
```

### 8.5 Preset 전환 UI

```
┌─ SideNav Footer 또는 TopBar ────────────────┐
│                                              │
│  🔍 현재 렌즈: PM_WORK  [전환 ▼]            │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │  ⬜ EXEC_SUMMARY   (읽기 전용)  🔒   │    │
│  │  ⬜ PMO_CONTROL    (읽기 전용)  🔒   │    │
│  │  ✅ PM_WORK        (현재)       ✏️   │    │
│  │  ⬜ DEV_EXECUTION  (전체 기능)  ✏️   │    │
│  │  ⬜ CUSTOMER_APPR. (전체 기능)  ✏️   │    │
│  │  ── AUDIT_EVIDENCE (전환 불가)  🚫   │    │
│  └──────────────────────────────────────┘    │
│                                              │
└──────────────────────────────────────────────┘
```

---

## 9. Intent → Node 추천 스코어링

### 9.1 개요

사용자 질문을 받으면 AI는 **어떤 MenuOntologyNode로 안내할지** 결정해야 한다.
이를 위해 **Rule-based 1차 스코어링 + Model Rerank 2차**의 하이브리드 방식을 사용한다.

```
┌─────────────────────────────────────────────────────────────┐
│  사용자 질문: "QA 리소스 때문에 칸반 보드가 막혀있어"         │
│                                                             │
│  Step 1: Rule-based Scoring (Top-5 후보)                    │
│  ├─ Intent hit (40%): ask_bottleneck → kanban ✅            │
│  ├─ Entity hit (25%): task → kanban ✅, backlog, issues     │
│  ├─ Keyword hit (15%): "칸반", "보드" → kanban ✅✅         │
│  ├─ Domain alignment (10%): execution → kanban ✅           │
│  └─ Context bonus (10%): 이전 화면 = sprint → execution ✅  │
│                                                             │
│  Step 2: Model Rerank (Top-5 → Top-1)                      │
│  └─ LLM이 질문+후보 노드를 비교하여 최종 순위 결정          │
│                                                             │
│  결과: kanban (0.92) >> issues (0.61) > sprints (0.45)      │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 Rule-based 스코어 가중치

| Factor | Weight | 설명 |
|--------|--------|------|
| **Intent hit** | 40% | 분류된 IntentTag가 노드의 `intents[]`에 포함되는지 |
| **Entity hit** | 25% | 추출된 Entity가 노드의 `entities[]`에 포함되는지 |
| **Keyword hit** | 15% | 정규화된 키워드가 노드의 `keywords[]`에 매칭되는지 |
| **Domain alignment** | 10% | 분류된 도메인이 노드의 `domain`과 일치하는지 |
| **Context bonus** | 10% | 이전 화면/대화 맥락과의 연관성 |

### 9.3 스코어링 알고리즘 (의사 코드)

```typescript
interface ScoringContext {
  classifiedIntents: IntentTag[];
  extractedEntities: EntityType[];
  normalizedKeywords: string[];
  inferredDomain?: MenuDomain;
  previousRoute?: string;
  conversationHistory?: string[];
}

function scoreNode(
  node: MenuOntologyNode,
  ctx: ScoringContext
): number {
  let score = 0;

  // 1) Intent hit (40%)
  const intentOverlap = ctx.classifiedIntents
    .filter(i => node.intents.includes(i)).length;
  const intentScore = intentOverlap > 0
    ? 0.4 * Math.min(intentOverlap / ctx.classifiedIntents.length, 1.0)
    : 0;

  // 2) Entity hit (25%)
  const entityOverlap = ctx.extractedEntities
    .filter(e => node.entities.includes(e)).length;
  const entityScore = entityOverlap > 0
    ? 0.25 * Math.min(entityOverlap / ctx.extractedEntities.length, 1.0)
    : 0;

  // 3) Keyword hit (15%)
  const kwOverlap = ctx.normalizedKeywords
    .filter(kw => node.keywords.some(nkw =>
      nkw.includes(kw) || kw.includes(nkw)
    )).length;
  const keywordScore = kwOverlap > 0
    ? 0.15 * Math.min(kwOverlap / ctx.normalizedKeywords.length, 1.0)
    : 0;

  // 4) Domain alignment (10%)
  const domainScore = ctx.inferredDomain === node.domain ? 0.10 : 0;

  // 5) Context bonus (10%)
  const contextScore = computeContextBonus(node, ctx.previousRoute);

  score = intentScore + entityScore + keywordScore + domainScore + contextScore;

  return score;
}

function computeContextBonus(
  node: MenuOntologyNode,
  previousRoute?: string
): number {
  if (!previousRoute) return 0;
  // 같은 도메인 내 이동 시 보너스
  const prevNode = ontologyNodes.find(n => n.route === previousRoute);
  if (prevNode && prevNode.domain === node.domain) return 0.07;
  // 인접 도메인 이동 시 약간의 보너스
  if (prevNode && ADJACENT_DOMAINS[prevNode.domain]?.includes(node.domain)) return 0.03;
  return 0;
}
```

### 9.4 Model Rerank (2차)

Rule-based 스코어링으로 **Top-5 후보**를 선별한 후, LLM에 다음 프롬프트를 전달하여 최종 순위를 결정한다:

```
Given the user question: "{질문}"
And the following candidate menu nodes:
1. {nodeId}: {label} — intents: {intents}, keywords: {keywords}
2. ...

Rank these nodes by relevance. Return a JSON array of
[{nodeId, score, reason}] sorted by score descending.
```

- **Fast path**: Rule 1위 점수 ≥ 0.75 → Rerank 생략, 바로 1위 사용
- **Rerank threshold**: Rule 1위 < 0.75 → LLM Rerank 실행
- **Fallback**: Rerank 실패 시 Rule 순위 그대로 사용

### 9.5 스코어→Navigation 변환

```typescript
function buildNavigation(
  rankedNodes: { node: MenuOntologyNode; score: number }[],
  userPreset: ViewModePreset
): AiNavigationResponse["navigation"] {
  const [primary, ...rest] = rankedNodes;

  return {
    primary: {
      route: primary.node.route,
      label: primary.node.label,
      reason: `${primary.node.intents[0]} 관련 화면 (score: ${primary.score.toFixed(2)})`,
    },
    secondary: rest.slice(0, 2).map(r => ({
      route: r.node.route,
      label: r.node.label,
      reason: `보조 추천 (score: ${r.score.toFixed(2)})`,
    })),
  };
}
```

---

## 10. 전체 와이어프레임 레이아웃 규격

### 10.1 Desktop 기준
```
┌──────────────────────────────────────────────────────────────┐
│  Top Bar (56px)                                               │
│  [ 로고 ]  [ 프로젝트 선택 ▼ ]  [ 검색 ]  [ 알림 🔔 ] [ 👤 ] │
├────────┬─────────────────────────────────────────────────────┤
│        │  Page Header (48px)                                  │
│  Side  │  [ 제목 ]  [ 필터 ▼ ]  [ + Primary CTA ]            │
│  Nav   ├─────────────────────────────────┬───────────────────┤
│ (240px)│                                 │ Right Panel       │
│        │  Main Content (70%)             │  (30%, 선택)      │
│  메뉴  │                                 │                   │
│  항목  │  - 테이블 / 카드 / 트리 / 보드  │  - 상세 정보      │
│  목록  │  - 역할별 Preset 분기           │  - Quick Action   │
│        │                                 │  - Context        │
│ ─────  │                                 │                   │
│ 렌즈   │                                 │                   │
│ 전환기 │                                 │                   │
├────────┴─────────────────────────────────┴───────────────────┤
│  (Drawer 420px — 선택적 오버레이, 모바일에서는 Full Modal)     │
└──────────────────────────────────────────────────────────────┘
```

> **변경**: SideNav 하단에 **렌즈 전환기(Preset Switcher)** 영역 추가

### 10.2 그리드 시스템
- **Grid**: 12 columns / margin 24px / gutter 16px
- **Left Rail**: SideNav 240px (고정, 접기 가능)
- **Top Bar**: 56px (고정)
- **Content**: `calc(100% - 240px)`

### 10.3 반응형 브레이크포인트
| 너비 | 적용 |
|------|------|
| ≥1440px | 풀 레이아웃 (SideNav + 2열 Content) |
| 1280px | 테이블 컬럼 일부 축약 |
| ≤1024px | SideNav 접기 + Drawer → Full Modal |
| ≤768px | 모바일 대응 (필요 시) |

---

## 11. 공통 컴포넌트 라이브러리

### 11.1 레이아웃 컴포넌트
| 컴포넌트 | 용도 |
|----------|------|
| `AppShell` | 전체 레이아웃 (SideNav + TopBar + Outlet) |
| `SideNav` | 좌측 메뉴 (Capability 기반 필터링 + Preset Switcher) |
| `TopBar` | 상단바 (프로젝트 선택/검색/알림) |
| `PageLayout` | 페이지 템플릿 (Header + Main + RightPanel) |
| `RightDrawer` | 우측 슬라이드 패널 (420px) |
| `PresetSwitcher` | Preset 전환 UI (SideNav 하단) |

### 11.2 데이터 표시 컴포넌트
| 컴포넌트 | 용도 |
|----------|------|
| `StatusBadge` | 상태 표시 (Normal/Warning/Critical/Paused) |
| `KpiCard` | KPI 수치 카드 |
| `DataTable` | 정렬/필터 가능 테이블 |
| `TreeView` | 계층 구조 (Epic → Feature → Story) |
| `DataBadge` | 데이터 기준 시각 (asOf) 표시 |

### 11.3 액션 컴포넌트
| 컴포넌트 | 용도 |
|----------|------|
| `Can` | 권한 기반 조건부 렌더링 래퍼 |
| `ApprovalBox` | 승인/반려 패널 |
| `ConfirmDialog` | 확인 다이얼로그 |
| `ExportButton` | Export 버튼 (PMO/감사) |

### 11.4 상태 컴포넌트
| 컴포넌트 | 용도 |
|----------|------|
| `EmptyState` | 데이터 없음 상태 |
| `LoadingSkeleton` | 로딩 중 스켈레톤 |
| `ErrorState` | 오류 + 재시도 |
| `NoPermission` | 권한 없음 안내 |

### 11.5 AI 연동 컴포넌트 (신규)
| 컴포넌트 | 용도 |
|----------|------|
| `AiResponseCard` | AI 응답 카드 (answer + CTA + secondary) |
| `NavigationCta` | AI 추천 화면 이동 버튼 |
| `PresetBadge` | 추천 Preset 표시 + 전환 버튼 |
| `ScopeChip` | scopeHints 시각화 칩 |

---

## 12. 권한 4중 방어 구조

```
┌─────────────────────────────────────────────┐
│  Layer 1: Menu/Navigation                    │
│  → 권한 없는 메뉴 자체를 숨김               │
│  → MenuOntologyNode.requiredCaps 기반        │
│  → SideNav 렌더링 시 computeCapabilities()   │
│    결과로 필터링                             │
├─────────────────────────────────────────────┤
│  Layer 2: Route Guard                        │
│  → URL 직접 접근 시 차단                     │
│  → ProtectedRoute + requiredCapabilities     │
│  → /unauthorized 또는 /dashboard 리다이렉트  │
├─────────────────────────────────────────────┤
│  Layer 3: Action Guard                       │
│  → 버튼/드래그/승인 등 상호작용 차단         │
│  → <Can required={[...]}>                    │
│  → Preset Read-only 모드 시 모든 Write 비활성│
│  → manage_kanban 없으면 DragDrop 비활성      │
├─────────────────────────────────────────────┤
│  Layer 4: Backend Enforcement (최종 보안)     │
│  → API 레벨에서 Capability + Scope 강제      │
│  → 프론트는 UX/오작동 방지 목적              │
│  → Preset 정보는 프론트 전용, 백엔드 무관    │
└─────────────────────────────────────────────┘
```

### 12.1 Capability 계산 공식

```typescript
function computeCapabilities(
  orgRole: OrgRole,
  projectRole: ProjectRole,
  discipline: Discipline
): Capability[] {
  const baseCaps = ORG_ROLE_CAPS[orgRole];            // 시스템 역할 기반
  const projectCaps = PROJECT_ROLE_CAPS[projectRole];  // 프로젝트 역할 기반
  const disciplineCaps = DISCIPLINE_CAPS[discipline];  // 직무 기반 (QA, BA 등)

  return [...new Set([...baseCaps, ...projectCaps, ...disciplineCaps])];
}
```

### 12.2 AI CTA 권한 검사

```typescript
// AI 추천 CTA 렌더링 시
function renderAiCta(
  navigation: AiNavigationResponse["navigation"],
  requiredCaps: Capability[],
  userCaps: Capability[]
) {
  const hasAccess = requiredCaps.every(cap => userCaps.includes(cap));

  if (!hasAccess) {
    return <CtaDisabled reason="이 화면에 접근 권한이 없습니다" />;
  }
  return <NavigationCta target={navigation.primary} />;
}
```

---

## 13. 라우트 전체 목록 (React Router)

### 13.1 Public Routes
```
/login
/logout
/unauthorized
```

### 13.2 App Routes
```
/dashboard                                  → view_dashboard
/requirements                               → view_requirements
/requirements/:reqId                        → view_requirements
/backlog                                    → view_backlog
/backlog/epics/:epicId                      → view_backlog
/wbs                                        → view_wbs
/phases                                     → view_phases
/phases/:phaseId                            → view_phases (Drawer open)
/phases/settings                            → manage_phases (Admin)
/kanban                                     → view_kanban
/sprints                                    → view_sprints
/sprints/:sprintId                          → view_sprints
/my-work                                    → view_my_work
/issues                                     → view_issues
/issues/:issueId                            → view_issues
/tests                                      → view_tests
/deliverables                               → view_deliverables
/deliverables/:deliverableId                → view_deliverables
/decisions                                  → view_decisions
/lineage                                    → view_lineage
/lineage/:entityType/:entityId              → view_lineage
/reports                                    → view_reports
/stats                                      → view_stats
/pmo                                        → view_pmo
/pmo/health                                 → view_pmo
/project-management                         → edit_project_accountability
/parts                                      → view_parts, manage_parts, manage_part_members
/role-permission                            → view_role_permission, manage_delegations, manage_capabilities, audit_governance
/audit/evidence                             → export_audit_evidence
/meetings                                   → view_meetings (TBD)
/notices                                    → view_notices (TBD)
/ai-assistant                               → view_ai_assistant ✅
/rfp                                        → view_rfp, manage_rfp ✅
/education                                  → view_education (TBD)
/admin/project                              → admin_project
/admin/system                               → admin_system
```

---

## 14. 기존 화면 vs 개선 화면 핵심 차이

| 항목 | 기존 (엑셀 기반) | 개선 (본 설계) |
|------|----------------|-------------|
| **메뉴 기준** | 기능 나열 (6개 대분류) | 사용자 질문 기반 (11개 섹션) |
| **메뉴 구조** | 단순 라우트 배열 | **MenuOntology** (intent/entity/keyword 포함) |
| **권한 모델** | Role 직접 매핑 | Capability + Scope 조합 |
| **대시보드** | 3종 버전 (PMO/PM/공통) | 1개 화면 + Preset 분기 |
| **Preset** | Role = 화면 | **Lens 모델** (렌즈 전환, 권한 불변) |
| **AI 연동** | 별도 챗봇 화면 | **Navigation Contract** (답변 + CTA + Preset 추천) |
| **화면 추천** | 수동 메뉴 탐색 | **Intent→Node 스코어링** (Rule 40% + Rerank) |
| **단계별 관리** | 보고서 나열 | 조정 테이블 (상태→원인→조치) |
| **이슈 관리** | CRUD 리스트 | 맥락(이슈=맥락+영향+결정) |
| **산출물** | 목록 관리 | 승인 흐름 (Upload→Review→Approve→Locked) |
| **감사 증빙** | Lineage 화면에서 겸용 | 전용 Export 화면 분리 |
| **교육 관리** | 일정 관리 | 단계 연계 교육 + 성과 추적 |

---

## 15. 적용 순서 (Implementation Phases)

### Phase A — MenuOntology 스키마 확립

| 항목 | 설명 |
|------|------|
| **범위** | `menuConfig.ts`를 `MenuOntologyNode[]`로 마이그레이션 |
| **작업** | 1. TypeScript 타입 정의 (`types/menu-ontology.ts`) |
|          | 2. 기존 menuConfig를 ontology 형식으로 변환 |
|          | 3. 각 노드에 intents, entities, keywords, canonicalQuestions 매핑 |
|          | 4. presetPolicies (최소 defaultPreset + 1~2개 정책) 작성 |
| **산출물** | `menuOntology.ts` + 단위 테스트 |
| **의존성** | 없음 (독립 실행 가능) |

### Phase B — AI Navigation CTA

| 항목 | 설명 |
|------|------|
| **범위** | AI 응답에 Navigation Contract 포함 + 프론트 CTA 렌더링 |
| **작업** | 1. AI 응답 스키마에 `navigation`, `viewMode`, `requiredCaps` 추가 |
|          | 2. `AiResponseCard`, `NavigationCta`, `PresetBadge` 컴포넌트 구현 |
|          | 3. CTA 클릭 → React Router navigate + Preset 적용 |
|          | 4. 권한 검사 (requiredCaps vs userCaps) |
| **산출물** | AI 응답 스키마 v2 + 프론트 컴포넌트 + E2E 테스트 |
| **의존성** | Phase A (MenuOntology에서 route/caps 정보 참조) |

### Phase C — Preset 동적 전환

| 항목 | 설명 |
|------|------|
| **범위** | Preset Switcher UI + 전환 정책 엔진 |
| **작업** | 1. `PresetSwitcher` 컴포넌트 (SideNav 하단) |
|          | 2. 전환 정책 매트릭스 구현 (상위 = Read-only) |
|          | 3. 각 화면에 `presetPolicies`에 따른 UI 분기 적용 |
|          | 4. Read-only 모드 시 Action Guard 연동 |
| **산출물** | PresetSwitcher + 정책 엔진 + 화면별 ViewMode 적용 |
| **의존성** | Phase A (presetPolicies 정의), Phase B (AI가 Preset 추천) |

### Phase D — Intent→Node 스코어링 품질

| 항목 | 설명 |
|------|------|
| **범위** | 스코어링 알고리즘 + Model Rerank + 품질 측정 |
| **작업** | 1. Rule-based scoring 함수 구현 |
|          | 2. Model Rerank 프롬프트 + 호출 로직 |
|          | 3. 골든셋 테스트 (질문→기대노드 매핑 100건+) |
|          | 4. A/B 테스트 (Rule only vs Rule+Rerank) |
|          | 5. 한국어 키워드 정규화 연동 (korean_normalizer.py) |
| **산출물** | 스코어링 엔진 + 골든셋 + A/B 리포트 |
| **의존성** | Phase A (ontology 데이터), Phase B (Navigation 응답) |

### 전체 흐름

```
Phase A ──────────────┐
(MenuOntology)        │
                      ├──→ Phase B ──────────────┐
                      │    (AI CTA)               │
                      │                           ├──→ Phase D
                      ├──→ Phase C                │    (Scoring Quality)
                      │    (Preset 전환)  ────────┘
                      │
  ※ Phase A는 독립 실행 가능
  ※ Phase B, C는 A 완료 후 병렬 가능
  ※ Phase D는 B 완료 후 시작
```

---

## 16. 문서 목차 (이후 작성 예정)

| 번호 | 파일명 | 화면 |
|------|--------|------|
| 00 | `00_메뉴_구조_총괄.md` | 메뉴 구조 총괄 (본 문서) |
| 01 | `01_대시보드_화면설계.md` | 대시보드 |
| 02 | `02_요구사항관리_화면설계.md` | 요구사항 관리 |
| 03 | `03_백로그_화면설계.md` | 백로그 (Epic/Feature/Story) |
| 04 | `04_WBS_화면설계.md` | WBS |
| 05 | `05_단계별관리_화면설계.md` | 단계별 관리 |
| 06 | `06_칸반보드_화면설계.md` | 칸반 보드 |
| 07 | `07_스프린트_화면설계.md` | 스프린트 |
| 08 | `08_내작업_화면설계.md` | 내 작업 (DEV 전용) |
| 09 | `09_이슈관리_화면설계.md` | 이슈 관리 |
| 10 | `10_테스트관리_화면설계.md` | 테스트 관리 |
| 11 | `11_산출물관리_화면설계.md` | 산출물 관리 |
| 12 | `12_결정리스크_화면설계.md` | 결정/리스크 보드 |
| 13 | `13_리니지_화면설계.md` | Lineage & History |
| 14 | `14_리포트_화면설계.md` | 리포트/통계 |
| 15 | `15_PMO거버넌스_화면설계.md` | PMO 대시보드 / Health Matrix |
| 16 | `16_감사증빙_화면설계.md` | Audit Evidence Export |
| 17 | `17_협업도구_화면설계.md` | 회의 관리 / 공지사항 |
| 18 | `18_교육관리_화면설계.md` | 교육 관리 |
| 19 | `19_관리자_화면설계.md` | 프로젝트 설정 / 시스템 설정 |
| 20 | `20_프로젝트관리_화면설계.md` | 프로젝트 관리 (책임 주체 — Authority Anchor) |
| 21 | `21_파트관리_화면설계.md` | 파트 관리 (조직/멤버십 — Assignment Layer) |
| 22 | `22_역할권한관리_화면설계.md` | 역할 및 권한 관리 (위임/거버넌스 — Authority Orchestration) |
| 23 | `23_AI어시스턴트_화면설계.md` | AI 어시스턴트 (AI Decision Console — Briefing/Insight/Action) ✅ |
| 24 | `24_RFP_화면설계.md` | RFP 관리 (RFP Origin Console — Upload/Parse/Score/Diff) ✅ |

---

## 17. DoD (본 문서 완료 기준)

- [x] 좌측 메뉴 전체 구조가 확정 라우트와 1:1 매핑
- [x] 역할별 메뉴 노출 매트릭스 작성 완료
- [x] Preset(ViewMode) 6종 정의 완료
- [x] IA 맵이 "사용자 질문" 기준으로 정렬됨
- [x] 공통 컴포넌트 라이브러리 정의
- [x] 권한 4중 방어 구조 명시
- [x] 전체 라우트 + 필요 Capability 매핑 완료
- [x] **MenuOntology 스키마 정의** (TypeScript 타입 + 필드 설명 + 샘플 노드)
- [x] **AI Navigation Contract** (응답 스키마 + JSON 예시 + Guardrails)
- [x] **Preset 동적 전환 정책** (Lens vs Capability 분리 + 전환 매트릭스 + UI)
- [x] **Intent→Node 스코어링** (Rule 가중치 + Rerank + 의사 코드)
- [x] **적용 순서** (Phase A→D 로드맵 + 의존성 다이어그램)
