# 09. 이슈관리 화면설계

> 작성일: 2026-02-08
> 버전: v2.1
> 라우트: `/issues`, `/issues/:issueId`
> 필요 Capability: `view_issues` (조회), `manage_issues` (등록/처리/에스컬레이션)
> 기본 Preset: 역할에 따라 자동 결정
> 노드 역할: **Detail** (상세 노드 — 필터·조회·처리 전용)

> **v2.0 변경**: Capability 세분화 (`manage_issues` 내 triage/assign/resolve/escalate 행위 구분),
> FilterSpec 온톨로지 내장, 이슈 생명주기 상태 전이 서버 트랜잭션 확정,
> Right Panel panelMode 5종 정의, 에스컬레이션 경로(Issue→Risk→Decision) 확정,
> Resolution 액션 4종 + 서버 검증, AI 딥링크 FilterSpec 기반 전환,
> Comment Thread 실시간 구독, 관련 엔티티(Task/Story/Epic) 링크 양방향 추적.
>
> **v2.1 변경** (cross-review):
> (1) Issue.type에서 `RISK` 제거 — 도메인 순수성 (이슈≠리스크, 리스크는 에스컬레이션 결과),
> (2) `escalatedToType`/`escalatedToId` → `escalationRefs[]` 경량 참조 배열로 전환,
> (3) `avgResolutionTime` 정의 명시 (resolvedAt − createdAt, reopen 시 첫 resolve만 포함),
> (4) Decision Pending 파생 배지 + `decisionPending` FilterSpec 키 추가,
> (5) AI 추천 액션에 `reasonCode` 필드 추가 (신뢰도/근거 표시),
> (6) Issue ↔ Task 자동 동기화 정책 훅 자리 마련 (onTaskCompleted — suggest only),
> (7) Reliability 3-tuple 표준화 (`asOf` + `completeness:{overall,breakdown?}` + `warnings:{code,message,severity}[]`),
> (8) Scope echo (`scope: { projectId }`) 전 API 응답에 적용,
> (9) Scope 2-tier (`securityKeys` + `explorationKeys`) 분리,
> (10) 이벤트 기반 refetch 규칙 추가 (상태 변경/에스컬레이션 직후 강제 refetch),
> (11) 깨지기 쉬운 곳 5→8 확장,
> (12) DoD v2.1 항목 추가.

---

## 1. 화면 개요

### 1.1 목적

이슈관리는 **"지금 무엇이 문제이고, 어떻게 해결할 것인가?"**에 대한 답을 제공한다.
기존 엑셀 기반 이슈 목록을 **"맥락 + 영향 + 결정"**이 결합된 살아있는 객체로 전환하여,
이슈의 발생부터 해결까지의 전 생명주기를 실시간으로 추적한다.

> **핵심 분리 원칙**: 이슈(Issue)는 **"지금 발생한 문제"**(현재 상태 기반),
> 리스크(Risk)는 **"앞으로 발생할 수 있는 위험"**(미래 예측 기반),
> 결정(Decision)은 **"합의된 방향"**(의사결정 기록).
> 이슈는 해결되지 않으면 리스크로 에스컬레이션되고, 리스크는 결정으로 승격된다.

> **00_총괄 원칙**: "이슈 = 맥락 + 영향 + 결정"
> 이슈 하나를 열면 (1) 왜 발생했는가(맥락), (2) 무엇에 영향을 주는가(영향),
> (3) 어떻게 처리할 것인가(결정)가 한 화면에서 파악되어야 한다.

### 1.2 설계 원칙

| 원칙 | 설명 |
|------|------|
| **이슈 = 맥락 + 영향 + 결정** | 이슈 상세 화면에서 발생 맥락, 영향 범위, 처리 결정이 동시에 표시 |
| **6단계 생명주기** | OPEN → TRIAGED → ASSIGNED → IN_PROGRESS → RESOLVED → CLOSED (+ REOPENED) |
| **에스컬레이션 경로** | Issue → Risk → Decision 3단계 승격 경로 지원 |
| **FilterSpec = 온톨로지** | 필터 키가 AI/URL/백엔드 공통 스키마로 통합 |
| **Comment Thread** | 이슈별 코멘트 스레드로 논의 이력 보존 |
| **관련 엔티티 링크** | Task/Story/Epic과 양방향 링크로 영향 범위 추적 |
| **Resolution 4종** | Fix, Won't Fix, Duplicate, Deferred — 해결 유형 명시 |
| **서버 트랜잭션** | 상태 전이 + 에스컬레이션은 서버에서만 처리 (프론트 임의 변경 불가) |
| **Capability 단일화** | `view_issues` (조회) + `manage_issues` (등록/처리/에스컬레이션) 2단 구조 |
| **Issue.type ≠ Risk** | Issue.type은 `BUG\|BLOCKER\|CHANGE_REQUEST\|QUESTION\|IMPROVEMENT` 5종. 리스크는 에스컬레이션 결과로만 생성 — 도메인 경계 순수성 유지 |
| **에스컬레이션 참조 경량화** | Issue에 `escalationRefs[]` 배열만 유지 — 상세 정보는 Risk/Decision API에서 조회. 캐시 불일치 방지 |
| **avgResolutionTime = 정의 필수** | `resolvedAt − createdAt` 기준, reopen 시 첫 resolve만 포함. 임원/PMO가 가장 신뢰하는 지표이므로 정의 누락 불가 |
| **Decision Pending = 파생 배지** | `escalated=true && escalatedToType=decision && decisionStatus≠DECIDED` 시 ⏸ Decision Pending 배지 표시. status는 변경하지 않음 |
| **AI 추천 = 근거 표시** | Context Panel 추천 액션에 `reasonCode` 표시 (e.g., "CRITICAL + D-2", "BLOCKED Task 2건"). P4 Observability 연결 |

### 1.3 행동 무게중심 — 관련 화면 역할 분담

> issues/decisions/kanban 3개 화면 간 "do" 행동의 홈 화면을 확정한다.
> AI가 조치 질문을 받았을 때 어디로 보낼지의 정책이 Preset별로 달라진다.

| 화면 | 역할 | 핵심 행동 |
|------|------|---------|
| **이슈 관리** (본 화면) | **문제 파악/처리 홈** | 이슈 등록, Triage, Assign, Resolve, Escalate |
| **결정/리스크 보드** | **의사결정 홈** | 리스크 평가, 결정 기록, 승인 |
| **칸반 보드** | **실행 홈** | 이슈 관련 Task 상태 변경 |

**Preset별 AI 라우팅 정책 (do_create_issue 예시)**:

| Preset | "이슈 등록해줘" → AI 라우팅 | 이유 |
|--------|-------------------------------|------|
| PM_WORK | → `targetNodeId: "issues"` | PM은 이슈 등록/Triage 주체 |
| PMO_CONTROL | → `targetNodeId: "issues"` | PMO는 Cross-project 이슈 분석 |
| DEV_EXECUTION | → `targetNodeId: "issues"` | DEV는 버그/블로커 보고 |
| CUSTOMER_APPROVAL | → `targetNodeId: "issues"` | Customer는 이슈 요청 등록 |

```typescript
/** Preset별 do intent 라우팅 정책 */
export const ISSUE_DO_INTENT_ROUTING: Record<ViewModePreset, Record<string, string>> = {
  PM_WORK: {
    do_create_issue:     "issues",
    do_triage_issue:     "issues",
    do_assign_issue:     "issues",
    do_resolve_issue:    "issues",
    do_escalate_risk:    "decisions",    // 에스컬레이션은 결정 보드로
    do_close_issue:      "issues",
  },
  PMO_CONTROL: {
    do_create_issue:     "issues",
    do_triage_issue:     "issues",
    do_assign_issue:     "issues",
    do_resolve_issue:    "issues",
    do_escalate_risk:    "decisions",
    do_close_issue:      "issues",
  },
  DEV_EXECUTION: {
    do_create_issue:     "issues",       // DEV는 이슈 보고
    do_resolve_issue:    "issues",       // 자기 할당 이슈 해결
    do_escalate_risk:    "issues",       // DEV는 에스컬레이션 요청만
  },
  EXEC_SUMMARY: {
    do_create_issue:     "issues",
    do_escalate_risk:    "decisions",
  },
  CUSTOMER_APPROVAL: {
    do_create_issue:     "issues",       // Customer는 이슈 요청
    do_resolve_issue:    "issues",       // 해결 확인 요청
  },
};
```

> **핵심**: 에스컬레이션(do_escalate_risk)은 PM/PMO에서는 결정 보드(decisions)로,
> DEV에서는 이슈 화면(issues)에서 에스컬레이션 요청만 가능.

### 1.4 핵심 질문 → 위젯 매핑

| 사용자 질문 | 화면 영역 | 설명 |
|-----------|---------|------|
| "이슈가 총 몇 건이야?" | 상단 KPI | 전체/미해결/심각도별 건수 |
| "CRITICAL 이슈 있어?" | 필터 + 배지 | severity=CRITICAL 필터 |
| "내게 할당된 이슈는?" | 필터 | assigneeId=me 필터 |
| "기한 초과된 이슈는?" | KPI + 필터 | overdue 필터 |
| "이 이슈는 어떤 태스크에 영향을 줘?" | Right Panel | 관련 엔티티 링크 패널 |
| "이슈 해결 평균 시간은?" | 상단 KPI | avg_resolution_time 메트릭 |
| "에스컬레이션 된 이슈 있어?" | KPI + 필터 | escalated 필터 |
| "이슈 ISS-042 상세 보여줘" | 상세 페이지 | `/issues/:issueId` 직접 이동 |
| "이번 스프린트 이슈 현황은?" | 필터 | sprintId 필터 |
| "블로커 이슈 목록 보여줘" | 필터 | type=BLOCKER 필터 |

---

## 2. MenuOntology 노드

### 2.1 타입 확정 (v2.0 변경사항)

01_대시보드 / 02_요구사항 / 03_백로그 v2.0 패턴을 동일하게 적용한다.

| 항목 | v1.0 | v2.0 (확정) | 변경 사유 |
|------|------|------------|----------|
| `entities` | 소문자 `"issue"` | PascalCase `"Issue"` | EntityType enum 정규화 |
| `intents` | 5개 | 16개로 확장 | 필터/액션 질문 커버리지 확보 |
| Capability | 2개 | 2개 유지 (내부 행위 구분) | `view_issues` + `manage_issues` (triage/assign/resolve/escalate/close 행위 포함) |
| `filterSpec` | 6키 | **14키** (v2.1: +1) | dateRange/q/overdue/escalated/decisionPending/resolutionType 추가 |
| `virtualNodes` | 없음 | **6개** (v2.1: +1) | critical/my-assigned/overdue/escalated/blockers/decision-pending |
| `metrics` | 4개 | **7개** | avg_resolution_time/overdue_count/escalated_count 추가 |
| AI 라우팅 | 단일 | Preset별 분기 | ISSUE_DO_INTENT_ROUTING 적용 |
| `nodeRole` | 없음 | `"detail"` 신규 | AI 스코어링 기준 (행동 질문에 +10 가산) |

### 2.2 FilterSpec 정의

FilterSpec은 **이 화면이 지원하는 필터 스키마**를 온톨로지 내부에 선언한다.
AI가 딥링크를 생성할 때, URL 파라미터 문자열이 아니라 **FilterSpec 키 기반 필터 객체**를 반환한다.

```typescript
/** 이슈 필터 스키마 — 온톨로지 내장 (v2.1: RISK 제거, decisionPending 추가) */
export interface IssueFilterSpec {
  /** 이슈 유형 (v2.1: RISK 제거 — 리스크는 에스컬레이션 결과로만 생성) */
  type?: "BUG" | "BLOCKER" | "CHANGE_REQUEST" | "QUESTION" | "IMPROVEMENT";
  /** 심각도 */
  severity?: "CRITICAL" | "HIGH" | "MEDIUM" | "LOW";
  /** 상태 */
  status?: "OPEN" | "TRIAGED" | "ASSIGNED" | "IN_PROGRESS" | "RESOLVED" | "CLOSED" | "REOPENED";
  /** 담당자 ID */
  assigneeId?: string;
  /** 보고자 ID */
  reporterId?: string;
  /** 파트 ID */
  partId?: string;
  /** 스프린트 ID */
  sprintId?: string;
  /** Epic ID */
  epicId?: string;
  /** Phase ID */
  phaseId?: string;
  /** 기간 필터 (ISO 8601) */
  dateFrom?: string;
  dateTo?: string;
  /** 기한 초과 여부 */
  overdue?: boolean;
  /** 에스컬레이션 여부 */
  escalated?: boolean;
  /** v2.1: 결정 대기 여부 (escalated + decisionStatus≠DECIDED) — 서버 파생 */
  decisionPending?: boolean;
  /** 해결 유형 */
  resolutionType?: "FIX" | "WONT_FIX" | "DUPLICATE" | "DEFERRED";
  /** 자유 검색어 */
  q?: string;
}
```

### 2.3 Issues 노드 (확정본 v2.0)

```typescript
const issuesNode: MenuOntologyNodeV2 = {
  nodeId: "issues",
  label: "이슈 관리",
  route: "/issues",
  icon: "BugReportRounded",
  domain: "control",
  nodeRole: "detail",              // detail node — action/filter queries get +10 boost

  requiredCaps: ["view_issues"],

  // --- intents: 16 (v2.0 expanded) ---
  intents: [
    // Ask
    "ask_issue_status",            // "이슈 현황 알려줘"
    "ask_issue_detail",            // "ISS-042 상세 보여줘"
    "ask_critical_issues",         // "CRITICAL 이슈 있어?"
    "ask_my_issues",               // "내 이슈 보여줘"
    "ask_overdue_issues",          // "기한 초과 이슈 있어?"
    "ask_escalated_issues",        // "에스컬레이션 된 이슈는?"
    "ask_blocked_tasks_by_issue",  // "이슈 때문에 막힌 태스크는?"
    "ask_issue_resolution_time",   // "이슈 해결 평균 시간은?"
    "ask_sprint_issues",           // "이번 스프린트 이슈는?"
    "ask_blocker_list",            // "블로커 이슈 목록"
    // Do
    "do_create_issue",             // "이슈 등록해줘"
    "do_triage_issue",             // "이슈 분류해줘"
    "do_assign_issue",             // "이슈 할당해줘"
    "do_resolve_issue",            // "이슈 해결 처리해줘"
    "do_escalate_risk",            // "리스크로 에스컬레이션해줘"
    "do_close_issue",              // "이슈 종료해줘"
  ],
  canonicalQuestions: [
    "이슈 현황 보여줘",
    "CRITICAL 이슈 몇 건이야?",
    "내게 할당된 이슈 보여줘",
    "기한 초과 이슈 있어?",
    "이슈 해결 평균 시간은?",
    "에스컬레이션 된 이슈 현황은?",
    "블로커 이슈 목록 알려줘",
    "새 이슈 등록해줘",
    "ISS-042 상세 보여줘",
    "이번 스프린트 이슈 몇 건이야?",
    "이슈 때문에 막힌 태스크 있어?",
    "이슈를 리스크로 에스컬레이션해줘",
  ],
  entities: ["Issue", "Task", "Story", "Epic", "Risk", "Decision", "Sprint"],
  keywords: [
    "이슈", "버그", "블로커", "장애", "결함", "문제", "에스컬레이션",
    "issue", "bug", "blocker", "defect", "incident", "escalation",
    "critical", "severity", "resolve", "triage", "assign",
    "기한초과", "overdue", "해결", "처리",
  ],
  metrics: [
    "total_issues",
    "open_count",
    "critical_count",
    "avg_resolution_time",
    "overdue_count",
    "escalated_count",
    "blocked_task_count",
  ],

  // --- FilterSpec ontology embedded (v2.0) ---
  filterSpec: {
    keys: [
      "type", "severity", "status", "assigneeId", "reporterId",
      "partId", "sprintId", "epicId", "phaseId",
      "dateFrom", "dateTo", "overdue", "escalated",
      "decisionPending",                 // v2.1: Decision Pending filter
      "resolutionType", "q",
    ],
    defaults: {},
  },

  defaultPreset: "PM_WORK",
  presetPolicies: [
    {
      preset: "EXEC_SUMMARY",
      ui: {
        density: "compact",
        defaultRightPanel: "closed",
        highlight: {
          metricKeys: ["total_issues", "critical_count", "escalated_count"],
          widgetKeys: ["KPI_TOTAL", "KPI_CRITICAL", "SEVERITY_CHART"],
        },
        hiddenColumns: ["reporterId", "partId", "sprintId", "epicId"],
      },
    },
    {
      preset: "PMO_CONTROL",
      ui: {
        density: "standard",
        defaultRightPanel: "open",
        highlight: {
          metricKeys: [
            "total_issues", "open_count", "critical_count",
            "avg_resolution_time", "escalated_count",
          ],
          widgetKeys: [
            "KPI_TOTAL", "KPI_OPEN", "KPI_CRITICAL",
            "KPI_AVG_RESOLUTION", "SEVERITY_DISTRIBUTION",
          ],
        },
      },
      suggestedActions: [
        {
          key: "export_issues",
          label: "이슈 리포트 Export",
          requiredCaps: ["export_reports"],
          targetNodeId: "reports",
        },
        {
          key: "view_decisions",
          label: "결정/리스크 보드",
          requiredCaps: ["view_decisions"],
          targetNodeId: "decisions",
        },
      ],
    },
    {
      preset: "PM_WORK",
      ui: {
        density: "detailed",
        defaultRightPanel: "open",
        highlight: {
          metricKeys: [
            "total_issues", "open_count", "critical_count",
            "overdue_count", "blocked_task_count",
            "avg_resolution_time", "escalated_count",
          ],
          widgetKeys: [
            "KPI_TOTAL", "KPI_OPEN", "KPI_CRITICAL",
            "KPI_OVERDUE", "KPI_BLOCKED_TASKS",
            "ISSUE_TABLE", "CONTEXT_PANEL",
          ],
        },
      },
      suggestedActions: [
        {
          key: "create_issue",
          label: "이슈 등록",
          requiredCaps: ["manage_issues"],
          targetNodeId: "issues",
        },
        {
          key: "view_kanban",
          label: "칸반 보드",
          requiredCaps: ["view_kanban"],
          targetNodeId: "kanban",
        },
        {
          key: "escalate_to_risk",
          label: "리스크 에스컬레이션",
          requiredCaps: ["manage_issues"],
          targetNodeId: "decisions",
        },
      ],
    },
    {
      preset: "DEV_EXECUTION",
      ui: {
        density: "standard",
        defaultRightPanel: "closed",
        defaultFilters: { assigneeId: "{{currentUserId}}" },
        highlight: {
          metricKeys: ["total_issues", "open_count"],
          widgetKeys: ["KPI_TOTAL", "ISSUE_TABLE"],
        },
      },
      suggestedActions: [
        {
          key: "report_issue",
          label: "이슈 보고",
          requiredCaps: ["manage_issues"],
          targetNodeId: "issues",
        },
        {
          key: "go_kanban",
          label: "칸반 보드",
          requiredCaps: ["view_kanban"],
          targetNodeId: "kanban",
        },
      ],
    },
    {
      preset: "CUSTOMER_APPROVAL",
      ui: {
        density: "compact",
        defaultRightPanel: "open",
        highlight: {
          metricKeys: ["total_issues", "critical_count", "open_count"],
          widgetKeys: ["KPI_TOTAL", "KPI_CRITICAL", "ISSUE_APPROVAL_BOX"],
        },
      },
      suggestedActions: [
        {
          key: "report_issue",
          label: "이슈 요청",
          requiredCaps: ["manage_issues"],
          targetNodeId: "issues",
        },
      ],
    },
    // AUDIT_EVIDENCE removed -- auditor has no access to issues
  ],

  // --- DeepLink: routeTemplate standard (:param format) ---
  deepLinks: [
    {
      pattern: "/issues?severity=:severity",
      description: "심각도별 이슈 필터",
      requiredParams: ["severity"],
    },
    {
      pattern: "/issues?status=:status",
      description: "상태별 이슈 필터",
      requiredParams: ["status"],
    },
    {
      pattern: "/issues?type=:type",
      description: "유형별 이슈 필터",
      requiredParams: ["type"],
    },
    {
      pattern: "/issues?assigneeId=:assigneeId",
      description: "담당자별 이슈 필터",
      requiredParams: ["assigneeId"],
    },
    {
      pattern: "/issues?sprintId=:sprintId",
      description: "스프린트별 이슈 필터",
      requiredParams: ["sprintId"],
    },
    {
      pattern: "/issues?overdue=true",
      description: "기한 초과 이슈 (v2.0)",
      requiredParams: [],
    },
    {
      pattern: "/issues?escalated=true",
      description: "에스컬레이션 된 이슈 (v2.0)",
      requiredParams: [],
    },
    {
      pattern: "/issues/:issueId",
      description: "이슈 상세 + Context Panel",
      requiredParams: ["issueId"],
    },
  ],

  // --- virtualNodes: deep links elevated to ontology 2nd layer (v2.0: 5) ---
  virtualNodes: [
    {
      virtualId: "issues.critical",
      label: "CRITICAL 이슈",
      routeTemplate: "/issues?severity=CRITICAL",
      requiredParams: [],
      intents: ["ask_critical_issues"],
      description: "심각도 CRITICAL 이슈 목록 직접 진입",
    },
    {
      virtualId: "issues.my-assigned",
      label: "내 할당 이슈",
      routeTemplate: "/issues?assigneeId={{currentUserId}}",
      requiredParams: [],
      intents: ["ask_my_issues"],
      description: "현재 사용자에게 할당된 이슈 목록 직접 진입",
    },
    {
      virtualId: "issues.overdue",
      label: "기한 초과 이슈",
      routeTemplate: "/issues?overdue=true",
      requiredParams: [],
      intents: ["ask_overdue_issues"],
      description: "기한 초과 이슈 목록 직접 진입 — 즉시 조치 필요",
    },
    {
      virtualId: "issues.escalated",
      label: "에스컬레이션 이슈",
      routeTemplate: "/issues?escalated=true",
      requiredParams: [],
      intents: ["ask_escalated_issues", "do_escalate_risk"],
      description: "에스컬레이션 된 이슈 목록 — 리스크/결정 보드 연결",
    },
    {
      virtualId: "issues.blockers",
      label: "블로커 이슈",
      routeTemplate: "/issues?type=BLOCKER",
      requiredParams: [],
      intents: ["ask_blocker_list", "ask_blocked_tasks_by_issue"],
      description: "BLOCKER 유형 이슈 목록 — 실행 차단 요인",
    },
    // v2.1: Decision Pending virtual node
    {
      virtualId: "issues.decision-pending",
      label: "결정 대기 이슈",
      routeTemplate: "/issues?decisionPending=true",
      requiredParams: [],
      intents: ["ask_escalated_issues"],
      description: "결정 보드에 에스컬레이션되었으나 아직 결정 미완료 이슈 — ⏸ Decision Pending 배지 표시",
    },
  ],

  // --- scope 2-tier separation (v2.1 standardized) ---
  scope: {
    securityKeys: ["projectId"],                    // tenant boundary — server enforced via JWT
    explorationKeys: [                              // user drill-down — frontend only
      "partId", "sprintId", "epicId", "phaseId",
      "type", "severity", "status", "assigneeId", "reporterId",
      "dateFrom", "dateTo", "overdue", "escalated", "decisionPending",
      "resolutionType", "q",
    ],
  },

  priority: 4,
};
```

### 2.4 FilterSpec URL 직렬화 규칙

**직렬화 방향**: `IssueFilterSpec → URL query string → IssueFilterSpec` (왕복 보장)

```typescript
/** FilterSpec → URL serialization */
function serializeIssueFilter(filter: IssueFilterSpec): URLSearchParams {
  const params = new URLSearchParams();
  for (const [key, value] of Object.entries(filter)) {
    if (value === undefined || value === null) continue;
    // boolean: only transmit "true" (false/undefined omit the key)
    if (typeof value === "boolean") {
      if (value) params.set(key, "true");
      continue;
    }
    params.set(key, String(value));
  }
  return params;
}

/** URL → FilterSpec deserialization */
function deserializeIssueFilter(params: URLSearchParams): IssueFilterSpec {
  const filter: IssueFilterSpec = {};
  const booleanKeys = ["overdue", "escalated", "decisionPending"] as const;  // v2.1: +decisionPending
  const enumKeys = {
    type: ["BUG", "BLOCKER", "CHANGE_REQUEST", "QUESTION", "IMPROVEMENT"],  // v2.1: RISK removed
    severity: ["CRITICAL", "HIGH", "MEDIUM", "LOW"],
    status: ["OPEN", "TRIAGED", "ASSIGNED", "IN_PROGRESS", "RESOLVED", "CLOSED", "REOPENED"],
    resolutionType: ["FIX", "WONT_FIX", "DUPLICATE", "DEFERRED"],
  } as const;

  for (const [key, value] of params.entries()) {
    // boolean
    if (booleanKeys.includes(key as any)) {
      (filter as any)[key] = value === "true";
      continue;
    }
    // enum validation — invalid value → skip (silent drop)
    if (key in enumKeys) {
      if ((enumKeys as any)[key].includes(value)) {
        (filter as any)[key] = value;
      }
      continue;
    }
    // string keys (assigneeId, reporterId, partId, sprintId, epicId, phaseId, dateFrom, dateTo, q)
    if ([
      "assigneeId", "reporterId", "partId", "sprintId", "epicId", "phaseId",
      "dateFrom", "dateTo", "q",
    ].includes(key)) {
      (filter as any)[key] = value;
    }
    // unknown keys → silent drop (forward compatibility)
  }
  return filter;
}
```

**직렬화 규칙 요약**:

| 규칙 | 예시 | 설명 |
|------|------|------|
| boolean은 `true`만 전송 | `?overdue=true` | `false`/`undefined`는 키 자체 생략 |
| enum은 화이트리스트 검증 | `?severity=CRITICAL` | 유효하지 않은 값은 silent drop |
| 복수 필터는 `&`로 연결 | `?severity=CRITICAL&status=OPEN` | 표준 query string |
| 미지 키는 무시 | `?futureKey=x` → 무시 | 하위 호환성 보장 |

---

## 3. 화면 구조

### 3.1 Right Panel panelMode 정의

Right Panel은 항상 동일한 영역이지만, **panelMode**에 따라 표시 내용이 달라진다.

```typescript
/** Right Panel mode — explicit enum */
export type IssuePanelMode =
  | "none"           // Right Panel hidden (EXEC_SUMMARY default)
  | "preview"        // Issue summary preview (list row selection)
  | "detail"         // Full issue detail (detail page)
  | "resolution"     // Resolve action panel (PM/PMO)
  | "escalation";    // Escalate to Risk/Decision panel (PM/PMO)
```

**panelMode 결정 규칙**:

| 컨텍스트 | Preset | 기본 panelMode |
|---------|--------|---------------|
| 목록 + 행 미선택 | ALL | `"none"` |
| 목록 + 행 선택 | EXEC_SUMMARY | `"none"` (Right Panel 숨김) |
| 목록 + 행 선택 | PM_WORK / PMO_CONTROL | `"preview"` |
| 목록 + 행 선택 | DEV_EXECUTION | `"preview"` (읽기 전용, 코멘트 가능) |
| 목록 + 행 선택 | CUSTOMER_APPROVAL | `"preview"` (이슈 승인/요청 패널 포함) |
| 상세 페이지 | ALL | `"detail"` |
| 상세 페이지 + 해결 탭 | PM_WORK / PMO_CONTROL | `"resolution"` → `"detail"` 토글 가능 |
| 상세 페이지 + 에스컬레이션 탭 | PM_WORK / PMO_CONTROL | `"escalation"` → `"detail"` 토글 가능 |

### 3.2 이슈 목록 (`/issues`)

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  이슈 관리  [ AI 보험금지급심사 구축 ]                          │
│  [ 필터 ▼ ]  [ 검색 🔍 ]                   [ + 이슈 등록 ]   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 전체    │  │ 미해결  │  │ CRITICAL│  │ 평균해결│        │
│  │ 47건    │  │  18건   │  │   3건   │  │ 3.2일   │        │
│  │         │  │ OPEN+   │  │  🔴     │  │         │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                     │
│  │ 기한초과│  │에스컬   │  │ 블로커  │                     │
│  │  5건    │  │레이션   │  │ 차단    │                     │
│  │  ⚠      │  │  4건    │  │ Task 7건│                     │
│  └─────────┘  └─────────┘  └─────────┘                     │
│                                                              │
│  ┌─ 필터 바 ──────────────────────────────────────────┐      │
│  │ 유형: [전체▼] 심각도: [전체▼] 상태: [전체▼]        │      │
│  │ 담당자: [전체▼] 보고자: [전체▼] Part: [전체▼]      │      │
│  │ Sprint: [전체▼] Phase: [전체▼]                     │      │
│  │ [ ] 기한초과만  [ ] 에스컬레이션만                  │ v2.0│
│  │                          ← FilterSpec 키와 1:1 매핑 │      │
│  └────────────────────────────────────────────────────┘      │
│                                                              │
│  ┌─ 이슈 테이블 ───────────────────────┬──────────────────┐  │
│  │                                      │ panelMode:       │  │
│  │ ID      | 제목      | 유형 | 심각도 │ "preview"        │  │
│  │         |           |      | 상태   │                  │  │
│  │ ────────────────────────────────── │                  │  │
│  │ ISS-042 | API 응답   | BUG  | CRIT │ ── 이슈 요약 ── │  │
│  │         | 지연       |      | OPEN │                  │  │
│  │         |           |      |  🔴   │ 유형: BUG        │  │
│  │                                      │ 심각도: CRITICAL │  │
│  │ ISS-038 | 배치 실패  | BUG  | HIGH │ 상태: OPEN       │  │
│  │         |           |      | ASSGN│ 보고자: 이○○     │  │
│  │         |           |      |  🟠  │ 담당: 미할당     │  │
│  │                                      │ 기한: 2026-02-10 │  │
│  │ ISS-035 | 권한 오류  | BUG  | MED  │ ⚠ 기한 D-2      │  │
│  │         |           |      | RSLVD│                  │  │
│  │         |           |      |  🟢  │ ── 관련 엔티티 ─ │  │
│  │                                      │ Task: TSK-155    │  │
│  │ ISS-031 | UI 깨짐   | BUG  | LOW  │ Story: S-12      │  │
│  │         |           |      | CLSD │ Epic: E-03       │  │
│  │         |           |      |  ⚪  │                  │  │
│  │                                      │ ── 추천 액션 ── │  │
│  │ ISS-028 | 성능 저하  |BLOCK | CRIT │ [Triage 시작]    │  │
│  │         |           |      | TRIAG│ [담당자 배정]    │  │
│  │         |           |      |  🟡  │ [에스컬레이션]   │  │
│  │                                      │                  │  │
│  │ ...                                  │                  │  │
│  │                                      │                  │  │
│  │ ◀ 1 2 3 4 5 ▶  (20건/페이지)        │                  │  │
│  └──────────────────────────────────────┴──────────────────┘  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 3.3 이슈 상세 (`/issues/:issueId`)

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  ← 이슈 관리  ISS-042  API 응답 지연                          │
│                        [ 편집 ] [ 에스컬레이션 ] [ 변경이력 ] │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─ 이슈 기본 정보 ─────────────────┬─ panelMode: "detail" ─┐│
│  │                                   │                       ││
│  │  이슈 ID: ISS-042                │  ── 상태 전이 ─────── ││
│  │  제목: API 응답 지연              │                       ││
│  │  설명: 보험금 산출 API 응답       │  OPEN ──────────────  ││
│  │        시간이 5초 이상 소요되어   │    │                   ││
│  │        사용자 경험 저하           │  TRIAGED  2026-02-05  ││
│  │                                   │    │                   ││
│  │  유형: BUG                       │  ASSIGNED 2026-02-06  ││
│  │  심각도: CRITICAL    🔴          │    │ → 김○○           ││
│  │  상태: ASSIGNED                  │  IN_PROGRESS (현재)   ││
│  │  우선순위: P1                    │    │                   ││
│  │                                   │  RESOLVED (대기)      ││
│  │  보고자: 이○○  (2026-02-04)     │    │                   ││
│  │  담당자: 김○○                   │  CLOSED (대기)        ││
│  │  기한: 2026-02-10               │                       ││
│  │  Phase: Step3 (통합테스트)       │  ── 영향 분석 ─────── ││
│  │  Part: AI모델구축                │                       ││
│  │  Sprint: Sprint-7                │  차단된 Task:          ││
│  │                                   │  TSK-155 반입 UI 구현 ││
│  │  에스컬레이션: 미적용            │    상태: BLOCKED       ││
│  │  관련 리스크: -                  │  TSK-170 반출 프로세스 ││
│  │                                   │    상태: BLOCKED       ││
│  ├─ 관련 엔티티 ────────────────────│                       ││
│  │                                   │  관련 Story:           ││
│  │  연결된 Task:                    │  S-88 반입신청        ││
│  │  ├─ TSK-155 반입 UI 구현         │    진행률: 40%        ││
│  │  │   상태: BLOCKED   담당: 김○○  │  S-91 반출신청        ││
│  │  └─ TSK-170 반출 프로세스 구현   │    진행률: 20%        ││
│  │      상태: BLOCKED   담당: 박○○  │                       ││
│  │                                   │  관련 Epic:            ││
│  │  연결된 Story:                   │  E-12 반출입관리      ││
│  │  ├─ S-88 반입신청                │    진행률: 45%        ││
│  │  └─ S-91 반출신청                │                       ││
│  │                                   │  ── 에스컬레이션 ─── ││
│  │  연결된 Epic:                    │  [리스크로 에스컬레   ││
│  │  └─ E-12 반출입관리              │   이션 ▶]             ││
│  │                                   │  [결정 요청 ▶]        ││
│  ├─ 코멘트 스레드 ──────────────────│                       ││
│  │                                   │  ── Resolution ───── ││
│  │  2026-02-06 14:30  김○○          │  [ Fix ]              ││
│  │  "API 쿼리 최적화 진행 중.       │  [ Won't Fix ]        ││
│  │   인덱스 추가 후 3초 이내로      │  [ Duplicate ]        ││
│  │   줄어들 예정."                  │  [ Deferred ]         ││
│  │                                   │                       ││
│  │  2026-02-05 10:00  이○○          │                       ││
│  │  "재현 확인. 대량 데이터(1만건   │                       ││
│  │   이상) 조회 시 발생."           │                       ││
│  │                                   │                       ││
│  │  ┌──────────────────────────┐    │                       ││
│  │  │ 코멘트 입력...            │    │                       ││
│  │  │                          │    │                       ││
│  │  │              [ 등록 ]    │    │                       ││
│  │  └──────────────────────────┘    │                       ││
│  │                                   │                       ││
│  └───────────────────────────────────┴───────────────────────┘│
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 4. Preset별 차이

### 4.1 EXEC_SUMMARY (Sponsor)

| 영역 | 표시 내용 |
|------|---------|
| KPI Row | 전체 건수, CRITICAL 건수, 에스컬레이션 건수만 표시 |
| 테이블 | 심각도/상태/제목만 표시 (축약) |
| Right Panel | panelMode: `"none"` (숨김) |
| CTA | 없음 (읽기 전용) |

### 4.2 PMO_CONTROL (PMO)

| 영역 | 표시 내용 |
|------|---------|
| KPI Row | 전체 건수, 미해결, CRITICAL, 평균 해결 시간, 에스컬레이션 건수 |
| 테이블 | 전체 컬럼 (Cross-project 분석 가능) |
| Right Panel | panelMode: `"preview"` (행 선택 시 이슈 요약 + 영향 분석) |
| CTA | `이슈 리포트 Export` → targetNodeId: `reports`, `결정/리스크 보드` → targetNodeId: `decisions` |

### 4.3 PM_WORK (PM)

| 영역 | 표시 내용 |
|------|---------|
| KPI Row | 전체 건수, 미해결, CRITICAL, 기한초과, 블로커 차단 Task, 평균 해결 시간, 에스컬레이션 |
| 테이블 | **전체 컬럼** + 관련 Task 수 |
| Right Panel | panelMode: `"preview"` + 추천 액션 |
| CTA | `이슈 등록` (cap: `manage_issues`), `칸반 보드` → targetNodeId: `kanban`, `리스크 에스컬레이션` (cap: `manage_issues`) → targetNodeId: `decisions` |

**PM_WORK 테이블 인라인 CTA**:

| 컨텍스트 | CTA | Capability |
|---------|-----|-----------|
| OPEN 이슈 | `[Triage]` | `manage_issues` |
| TRIAGED 이슈 | `[Assign]` | `manage_issues` |
| IN_PROGRESS 이슈 | `[Resolve]` | `manage_issues` |
| 모든 미해결 이슈 | `[에스컬레이션]` | `manage_issues` |

### 4.4 DEV_EXECUTION (DEV / DevReader)

| 영역 | 표시 내용 |
|------|---------|
| KPI Row | 전체 건수, 내 이슈 수 |
| 테이블 | 기본 컬럼 (기본 필터: assigneeId = me) |
| Right Panel | panelMode: `"preview"` (읽기 전용 + 코멘트 가능) |
| CTA | `이슈 보고` (cap: `manage_issues`), `칸반 보드` → targetNodeId: `kanban` |

**DEV의 행동 범위**:

| 행위 | 가능 여부 | 설명 |
|------|---------|------|
| 이슈 등록 | O | 버그/블로커 보고 |
| 코멘트 | O | 진행 상황 공유 |
| 상태 변경 | 제한적 | 자기 할당 이슈만 IN_PROGRESS → RESOLVED |
| Triage/Assign | X | PM 전용 |
| 에스컬레이션 | X | PM 전용 (요청만 코멘트로 가능) |

### 4.5 CUSTOMER_APPROVAL (Customer PM)

| 영역 | 표시 내용 |
|------|---------|
| KPI Row | 전체 건수, CRITICAL 건수, 미해결 건수 |
| 테이블 | 제목/유형/심각도/상태 (축약) |
| Right Panel | panelMode: `"preview"` + IssueApprovalBox |
| CTA | `이슈 요청` (cap: `manage_issues`) |

**CUSTOMER_APPROVAL panelMode: "preview" + IssueApprovalBox 상세**:

```
┌─ Right Panel (CUSTOMER_APPROVAL) ───────────┐
│                                               │
│  ── 이슈 요약 ────────────────────           │
│  ISS-042  API 응답 지연                      │
│  유형: BUG  │  심각도: CRITICAL              │
│  상태: ASSIGNED                              │
│  담당: 김○○                                 │
│                                               │
│  ── 처리 현황 ───────────────────            │
│  보고일: 2026-02-04                          │
│  기한: 2026-02-10                            │
│  경과: 4일 / 남은: 2일                       │
│                                               │
│  ── 최근 코멘트 ─────────────────            │
│  김○○ (2026-02-06):                         │
│  "API 쿼리 최적화 진행 중"                   │
│                                               │
│  ── 고객 액션 ───────────────────            │
│  [ 해결 확인 요청 ]                          │
│  [ 추가 정보 제공 ]                          │
│                                               │
└───────────────────────────────────────────────┘
```

---

## 5. 이슈 생명주기 상세

### 5.1 상태 전이 다이어그램

```
                    ┌────────────────────────────────┐
                    │          REOPENED               │
                    │  (해결이 불충분하여 재개)        │
                    └───────┬────────────────────────┘
                            │
                            ▼
┌──────┐    ┌──────────┐    ┌──────────┐    ┌─────────────┐    ┌──────────┐    ┌──────────┐
│ OPEN │───▶│ TRIAGED  │───▶│ ASSIGNED │───▶│ IN_PROGRESS │───▶│ RESOLVED │───▶│ CLOSED   │
│      │    │          │    │          │    │             │    │          │    │          │
│신규  │    │분류완료  │    │담당배정  │    │처리중       │    │해결완료  │    │최종종료  │
│등록  │    │          │    │          │    │             │    │          │    │          │
└──────┘    └──────────┘    └──────────┘    └─────────────┘    └──────────┘    └──────────┘
                                                                     │              │
                                                                     │              │
                                                                     └──────────────┘
                                                                       REOPENED
                                                                     (재개 가능)
```

**에스컬레이션 경로** (어느 상태에서든 가능):

```
Issue (any status except CLOSED)
  │
  ├──▶ Risk (리스크 등록)
  │      │
  │      └──▶ Decision (결정 요청)
  │
  └──▶ Decision (직접 결정 요청 — 리스크 단계 생략 가능)
```

### 5.2 상태 전이 규칙 — 서버 트랜잭션 필수

> **핵심 원칙**: 모든 상태 전이는 **서버 트랜잭션**으로만 처리한다.
> 프론트는 UI 상태를 임의로 변경하지 않으며, API 응답으로만 상태를 갱신한다.

#### 5.2.1 OPEN → TRIAGED (Triage)

```
서버 트랜잭션:
  [Triage 요청] PATCH /api/issues/:issueId/triage
    ├─ 필수: type 확정, severity 확정
    ├─ Issue.status: OPEN → TRIAGED
    ├─ triageBy, triagedAt 기록
    └─ cap: manage_issues

  게이트 조건:
    - type이 확정되어야 함 (BUG/BLOCKER/CHANGE_REQUEST/QUESTION/IMPROVEMENT) — v2.1: RISK 제거
    - severity가 확정되어야 함 (CRITICAL/HIGH/MEDIUM/LOW)
```

#### 5.2.2 TRIAGED → ASSIGNED (Assign)

```
서버 트랜잭션:
  [Assign 요청] PATCH /api/issues/:issueId/assign
    ├─ 필수: assigneeId
    ├─ Issue.status: TRIAGED → ASSIGNED
    ├─ assignedBy, assignedAt 기록
    ├─ 담당자에게 알림 발송
    └─ cap: manage_issues

  게이트 조건:
    - assigneeId가 프로젝트 멤버여야 함
    - 이미 ASSIGNED인 경우 담당자 변경으로 처리 (재할당)
```

#### 5.2.3 ASSIGNED → IN_PROGRESS (Start)

```
서버 트랜잭션:
  [Start 요청] PATCH /api/issues/:issueId/start
    ├─ Issue.status: ASSIGNED → IN_PROGRESS
    ├─ startedAt 기록
    └─ cap: manage_issues (담당자 본인도 가능)

  게이트 조건:
    - 현재 사용자가 assignee이거나 manage_issues Capability 보유
```

#### 5.2.4 IN_PROGRESS → RESOLVED (Resolve)

```
서버 트랜잭션:
  [Resolve 요청] PATCH /api/issues/:issueId/resolve
    ├─ 필수: resolutionType (FIX | WONT_FIX | DUPLICATE | DEFERRED)
    ├─ 필수: resolutionComment (해결 내용 설명)
    ├─ 선택: duplicateOfId (DUPLICATE인 경우 원본 이슈 ID)
    ├─ 선택: deferredUntil (DEFERRED인 경우 연기 시점)
    ├─ Issue.status: IN_PROGRESS → RESOLVED
    ├─ resolvedBy, resolvedAt 기록
    ├─ 관련 BLOCKED Task 자동 해제 여부 확인 (사용자 선택)
    └─ cap: manage_issues

  게이트 조건:
    - resolutionType 필수
    - resolutionComment 최소 10자
    - DUPLICATE인 경우 duplicateOfId가 유효한 이슈 ID여야 함

  부수 효과:
    - BLOCKED Task 자동 해제 옵션 → TaskStatus: BLOCKED → TODO (서버 트랜잭션)
```

#### 5.2.5 RESOLVED → CLOSED (Close)

```
서버 트랜잭션:
  [Close 요청] PATCH /api/issues/:issueId/close
    ├─ Issue.status: RESOLVED → CLOSED
    ├─ closedBy, closedAt 기록
    └─ cap: manage_issues

  게이트 조건:
    - 현재 상태가 RESOLVED여야 함
```

#### 5.2.6 RESOLVED/CLOSED → REOPENED (Reopen)

```
서버 트랜잭션:
  [Reopen 요청] PATCH /api/issues/:issueId/reopen
    ├─ 필수: reopenReason (재개 사유)
    ├─ Issue.status: RESOLVED/CLOSED → REOPENED
    ├─ REOPENED → OPEN과 동일하게 처리 (재 Triage 가능)
    ├─ reopenedBy, reopenedAt 기록
    └─ cap: manage_issues

  게이트 조건:
    - 현재 상태가 RESOLVED 또는 CLOSED여야 함
    - reopenReason 최소 10자
```

### 5.3 에스컬레이션 상세

```typescript
/** Escalation request */
interface IssueEscalationRequest {
  issueId: string;
  escalationType: "risk" | "decision";
  reason: string;                     // Escalation reason (min 10 chars)
  urgency: "immediate" | "next_review" | "informational";
  /** risk escalation specific */
  riskAssessment?: {
    probability: "high" | "medium" | "low";
    impact: "high" | "medium" | "low";
    mitigationPlan?: string;
  };
  /** decision escalation specific */
  decisionRequest?: {
    options: string[];                // Decision options
    recommendation?: string;         // Recommended option
    deadline?: string;               // Decision deadline (ISO 8601)
  };
}

/** Escalation response (v2.1: escalationRefs[] pattern) */
interface IssueEscalationResponse {
  issue: Issue;                       // Updated issue (escalated=true, escalationRefs updated)
  createdEntity: {
    type: "risk" | "decision";
    id: string;                       // RSK-001 or DEC-001
    route: string;                    // /decisions/RSK-001
  };
  // --- scope echo (v2.1) ---
  scope: { projectId: string };
}
```

**에스컬레이션 후 이슈 상태**:

| 에스컬레이션 | 이슈 상태 변경 | 설명 |
|------------|-------------|------|
| → Risk | 변경 없음 (escalated=true 플래그) | 이슈는 계속 처리, 리스크로 병행 관리 |
| → Decision | 변경 없음 (escalated=true 플래그) | 이슈는 결정 대기 상태로 병행 관리 |
| Decision 결정 후 | Issue 자동 RESOLVED (선택) | 결정에 따라 이슈 자동 해결 가능 |

> **v2.1: Decision Pending 파생 배지**
>
> - `escalated=true && escalationRefs` 중 `type=decision && decisionStatus≠DECIDED` 인 항목 존재 시
>   ⏸ **Decision Pending** 배지 표시 (status 자체는 변경하지 않음)
> - `decisionPending` FilterSpec 키로 필터 가능 (서버가 파생 계산)
> - PM이 "판단을 기다리는 중인 이슈" 를 한눈에 식별할 수 있게 함
> - `escalationRefs[]` 에는 `{ type, id, createdAt }` 경량 참조만 유지 — 상세 정보는 Risk/Decision API 조회

### 5.4 Resolution Type 상세

| Resolution | 의미 | 후속 조치 |
|-----------|------|---------|
| **FIX** | 문제를 수정하여 해결함 | BLOCKED Task 자동 해제 옵션 |
| **WON'T FIX** | 수정하지 않기로 결정함 | 사유 필수, BLOCKED Task 수동 해제 |
| **DUPLICATE** | 다른 이슈와 중복 | 원본 이슈 ID 연결, 원본에 코멘트 자동 추가 |
| **DEFERRED** | 현재 스프린트/단계에서 연기 | 연기 시점 필수, 해당 시점에 자동 REOPEN 가능 |

---

## 6. 데이터 구조 / API

### 6.1 Issue 엔티티

```typescript
interface Issue {
  id: string;                          // ISS-001
  projectId: string;
  title: string;
  description: string;

  // Classification (v2.1: RISK removed — risk is escalation result only)
  type: "BUG" | "BLOCKER" | "CHANGE_REQUEST" | "QUESTION" | "IMPROVEMENT";
  severity: "CRITICAL" | "HIGH" | "MEDIUM" | "LOW";
  status: "OPEN" | "TRIAGED" | "ASSIGNED" | "IN_PROGRESS" | "RESOLVED" | "CLOSED" | "REOPENED";

  // Assignment
  reporterId: string;                  // Issue reporter user ID
  reporterName: string;
  assigneeId?: string;                 // Assigned handler user ID
  assigneeName?: string;

  // Scope
  partId?: string;
  partName?: string;
  sprintId?: string;
  sprintName?: string;
  epicId?: string;
  phaseId?: string;

  // Timeline
  dueDate?: string;                    // ISO 8601
  isOverdue: boolean;                  // Server-computed

  // Resolution
  resolutionType?: "FIX" | "WONT_FIX" | "DUPLICATE" | "DEFERRED";
  resolutionComment?: string;
  duplicateOfId?: string;              // If DUPLICATE
  deferredUntil?: string;              // If DEFERRED

  // Escalation (v2.1: escalationRefs[] — lightweight refs, detail via Risk/Decision API)
  escalated: boolean;
  /** v2.1: replaces escalatedToType/escalatedToId — supports multiple escalations */
  escalationRefs: {
    type: "risk" | "decision";
    id: string;                        // RSK-001 or DEC-001
    createdAt: string;                 // ISO 8601
  }[];
  /** v2.1: server-computed derived state (Decision Pending badge) */
  decisionPending: boolean;

  // Related entities
  linkedTaskIds: string[];
  linkedStoryIds: string[];
  linkedEpicIds: string[];

  // Triage
  triagedBy?: string;
  triagedAt?: string;

  // Assignment tracking
  assignedBy?: string;
  assignedAt?: string;

  // Resolution tracking
  resolvedBy?: string;
  resolvedAt?: string;

  // Close tracking
  closedBy?: string;
  closedAt?: string;

  // Reopen tracking
  reopenedBy?: string;
  reopenedAt?: string;
  reopenReason?: string;

  // Comment count
  commentCount: number;

  // Metadata
  createdAt: string;
  updatedAt: string;
  createdBy: string;
}
```

### 6.2 IssueComment 엔티티

```typescript
interface IssueComment {
  id: string;
  issueId: string;
  authorId: string;
  authorName: string;
  authorRole: string;                  // "PM", "DEV", "Customer PM" etc.
  content: string;
  /** System-generated comment (status change, escalation, etc.) */
  isSystem: boolean;
  /** Mentions */
  mentions: string[];                  // User IDs mentioned in comment
  createdAt: string;
  updatedAt?: string;
}
```

### 6.3 Issues List API

```
GET /api/issues?projectId={projectId}
    &type={BUG|BLOCKER|CHANGE_REQUEST|QUESTION|IMPROVEMENT}
    &severity={CRITICAL|HIGH|MEDIUM|LOW}
    &status={OPEN|TRIAGED|ASSIGNED|IN_PROGRESS|RESOLVED|CLOSED|REOPENED}
    &assigneeId={assigneeId}
    &reporterId={reporterId}
    &partId={partId}
    &sprintId={sprintId}
    &epicId={epicId}
    &phaseId={phaseId}
    &dateFrom={ISO8601}
    &dateTo={ISO8601}
    &overdue={true}
    &escalated={true}
    &decisionPending={true}
    &resolutionType={FIX|WONT_FIX|DUPLICATE|DEFERRED}
    &q={searchText}
    &page={page}&size={size}
    &sortBy={createdAt|severity|dueDate|updatedAt}
    &sortDir={asc|desc}
```

> **참고**: 쿼리 파라미터는 §2.2 FilterSpec 키와 **1:1 매핑**된다.

```typescript
interface IssuesListResponse {
  items: Issue[];
  pagination: { page: number; size: number; total: number };
  stats: {
    totalIssues: number;
    openCount: number;
    triagedCount: number;
    assignedCount: number;
    inProgressCount: number;
    resolvedCount: number;
    closedCount: number;
    reopenedCount: number;
    criticalCount: number;
    highCount: number;
    mediumCount: number;
    lowCount: number;
    overdueCount: number;
    escalatedCount: number;
    blockedTaskCount: number;
    avgResolutionTimeHours: number;    // Average hours to resolve
  };

  // --- reliability 3-tuple (v2.1 standardized) ---
  asOf: string;
  completeness: { overall: number; breakdown?: Record<string, number> };
  warnings: { code: string; message: string; severity: "info" | "warn" | "critical" }[];
  // --- scope echo (v2.1) ---
  scope: { projectId: string };
}
```

### 6.4 Issue Detail API

```
GET /api/issues/:issueId
```

```typescript
interface IssueDetailResponse {
  issue: Issue;

  // Related entities detail
  linkedTasks: {
    id: string;
    title: string;
    status: string;
    assigneeName?: string;
  }[];
  linkedStories: {
    id: string;
    title: string;
    status: string;
    progress: number;
  }[];
  linkedEpics: {
    id: string;
    title: string;
    status: string;
    progress: number;
  }[];

  // Escalation detail
  escalation?: {
    type: "risk" | "decision";
    entityId: string;
    entityTitle: string;
    entityStatus: string;
    escalatedAt: string;
    escalatedBy: string;
  };

  // Status history
  statusHistory: {
    fromStatus: string;
    toStatus: string;
    changedBy: string;
    changedByName: string;
    changedAt: string;
    comment?: string;
  }[];

  // --- reliability 3-tuple (v2.1 standardized) ---
  asOf: string;
  completeness: { overall: number; breakdown?: Record<string, number> };
  warnings: { code: string; message: string; severity: "info" | "warn" | "critical" }[];
  // --- scope echo (v2.1) ---
  scope: { projectId: string };
}
```

### 6.5 Issue Comments API

```
GET    /api/issues/:issueId/comments                // List comments
POST   /api/issues/:issueId/comments                // Add comment (cap: manage_issues or view_issues for DEV)
PATCH  /api/issues/:issueId/comments/:commentId     // Edit own comment
DELETE /api/issues/:issueId/comments/:commentId     // Delete own comment
```

```typescript
interface IssueCommentsResponse {
  items: IssueComment[];
  pagination: { page: number; size: number; total: number };
}

interface CreateCommentRequest {
  content: string;
  mentions?: string[];               // User IDs to mention
}
```

### 6.6 Issue CRUD API

```
POST   /api/issues                                  // Create issue (cap: manage_issues)
GET    /api/issues/:issueId                         // Detail (cap: view_issues)
PATCH  /api/issues/:issueId                         // Edit issue (cap: manage_issues)
```

### 6.7 Issue Lifecycle API — Server Transactions

```
PATCH  /api/issues/:issueId/triage                  // Triage (cap: manage_issues)
PATCH  /api/issues/:issueId/assign                  // Assign (cap: manage_issues)
PATCH  /api/issues/:issueId/start                   // Start (cap: manage_issues, self-assign OK)
PATCH  /api/issues/:issueId/resolve                 // Resolve (cap: manage_issues)
PATCH  /api/issues/:issueId/close                   // Close (cap: manage_issues)
PATCH  /api/issues/:issueId/reopen                  // Reopen (cap: manage_issues)
POST   /api/issues/:issueId/escalate                // Escalate (cap: manage_issues)
```

```typescript
/** Triage request */
interface TriageRequest {
  type: Issue["type"];
  severity: Issue["severity"];
  comment?: string;
}

/** Assign request */
interface AssignRequest {
  assigneeId: string;
  comment?: string;
}

/** Resolve request */
interface ResolveRequest {
  resolutionType: "FIX" | "WONT_FIX" | "DUPLICATE" | "DEFERRED";
  resolutionComment: string;           // Min 10 chars
  duplicateOfId?: string;              // Required if DUPLICATE
  deferredUntil?: string;              // Required if DEFERRED
  /** Auto-unblock related tasks */
  unblockRelatedTasks?: boolean;
}

/** Reopen request */
interface ReopenRequest {
  reopenReason: string;                // Min 10 chars
}
```

**Lifecycle API Error Contract**:

```typescript
type IssueLifecycleErrorCode =
  | "INVALID_STATUS_TRANSITION"      // Current status does not allow this transition
  | "MISSING_REQUIRED_FIELD"         // Required field not provided
  | "INVALID_ASSIGNEE"              // Assignee is not a project member
  | "INVALID_DUPLICATE_REF"         // duplicateOfId does not exist
  | "COMMENT_TOO_SHORT"             // resolutionComment or reopenReason < 10 chars
  | "ALREADY_ESCALATED"             // Issue already escalated
  | "CONCURRENT_MODIFICATION";      // Optimistic lock conflict

interface IssueLifecycleErrorResponse {
  error: {
    code: IssueLifecycleErrorCode;
    message: string;
    currentStatus?: string;
    expectedStatus?: string[];
  };
}
```

| 에러 코드 | HTTP | 프론트 대응 |
|---------|------|-----------|
| `INVALID_STATUS_TRANSITION` | 409 | "현재 상태에서 이 전이가 불가능합니다" + 현재 상태 표시 |
| `MISSING_REQUIRED_FIELD` | 400 | 누락 필드 하이라이트 |
| `INVALID_ASSIGNEE` | 400 | "프로젝트 멤버를 선택하세요" |
| `INVALID_DUPLICATE_REF` | 400 | "유효한 이슈 ID를 입력하세요" |
| `COMMENT_TOO_SHORT` | 400 | "최소 10자 이상 입력하세요" |
| `ALREADY_ESCALATED` | 409 | "이미 에스컬레이션 된 이슈입니다" + 링크 |
| `CONCURRENT_MODIFICATION` | 409 | 데이터 새로고침 후 재시도 안내 |

### 6.8 Issue Stats API

```
GET /api/issues/stats?projectId={projectId}&partId={partId}
```

```typescript
interface IssueStatsResponse {
  stats: {
    totalIssues: number;
    openCount: number;
    criticalCount: number;
    overdueCount: number;
    escalatedCount: number;
    /** v2.1 추가 */
    decisionPendingCount: number;
    blockedTaskCount: number;
    /**
     * v2.1 정의 명시:
     * avgResolutionTimeHours = avg(resolvedAt − createdAt) in hours
     * - reopen된 이슈는 **첫 resolve 시점**만 포함
     * - RESOLVED/CLOSED 상태 이슈만 집계 대상
     * - 임원/PMO가 가장 신뢰하는 지표이므로 정의 누락 불가
     */
    avgResolutionTimeHours: number;
  };

  // Distribution
  byType: { type: string; count: number }[];
  bySeverity: { severity: string; count: number }[];
  byStatus: { status: string; count: number }[];
  byPart: { partId: string; partName: string; count: number }[];

  // Trend (last 4 weeks)
  weeklyTrend: {
    week: string;              // "2026-W06"
    opened: number;
    resolved: number;
    netChange: number;         // opened - resolved
  }[];

  // --- reliability 3-tuple (v2.1 standardized) ---
  asOf: string;
  completeness: { overall: number; breakdown?: Record<string, number> };
  warnings: { code: string; message: string; severity: "info" | "warn" | "critical" }[];
  // --- scope echo (v2.1) ---
  scope: { projectId: string };
}
```

### 6.9 Issue Entity Link API

```
POST   /api/issues/:issueId/links                   // Link task/story/epic (cap: manage_issues)
DELETE /api/issues/:issueId/links/:linkId            // Unlink (cap: manage_issues)
```

```typescript
interface IssueLinkRequest {
  targetType: "task" | "story" | "epic";
  targetId: string;
  relationship: "blocks" | "caused_by" | "related_to";
}

interface IssueLinkResponse {
  linkId: string;
  issueId: string;
  targetType: string;
  targetId: string;
  relationship: string;
  linkedBy: string;
  linkedAt: string;
}
```

---

## 7. 컴포넌트 분해

### 7.1 폴더 구조

```
features/
  issues/
    pages/
      IssuesPage.tsx                  ← List (ViewMode branching, Issue Table)
      IssueDetailPage.tsx             ← Issue detail (Context + Comments + Actions)
    components/
      // List
      IssueTable.tsx                  ← Main issue table
      IssueFilters.tsx                ← Filter bar (FilterSpec-based)
      IssueKpiRow.tsx                 ← Top KPI card row
      IssueSeverityChart.tsx          ← Severity distribution mini chart

      // Detail
      IssueBasicInfo.tsx              ← Issue basic info card
      IssueStatusTimeline.tsx         ← Status transition timeline
      IssueRelatedEntities.tsx        ← Linked Tasks/Stories/Epics
      IssueEscalationInfo.tsx         ← Escalation status and link

      // Comments
      IssueCommentThread.tsx          ← Comment thread container
      IssueCommentItem.tsx            ← Single comment display
      IssueCommentForm.tsx            ← Comment input form

      // Right Panel
      IssuePreviewPanel.tsx           ← panelMode: "preview" (issue summary)
      IssueContextPanel.tsx           ← panelMode: "detail" (full context)
      IssueResolveActions.tsx         ← panelMode: "resolution" (PM/PMO/DevReader)
      IssueEscalationPanel.tsx        ← panelMode: "escalation" (escalate to risk/decision)
      IssueApprovalBox.tsx            ← Customer PM approval/request actions
      IssueRightPanel.tsx             ← panelMode branching renderer

      // Actions
      IssueCreateModal.tsx            ← Issue creation modal
      IssueEditModal.tsx              ← Issue edit modal
      IssueTriageDialog.tsx           ← Triage confirmation dialog
      IssueAssignDialog.tsx           ← Assignee selection dialog
      IssueResolveDialog.tsx          ← Resolution type + comment dialog
      IssueEscalateDialog.tsx         ← Escalation form dialog
      IssueReopenDialog.tsx           ← Reopen reason dialog
      IssueLinkDialog.tsx             ← Link task/story/epic dialog

      // Status display
      IssueSeverityBadge.tsx          ← Severity badge (CRITICAL/HIGH/MEDIUM/LOW)
      IssueStatusBadge.tsx            ← Status badge (7 values)
      IssueTypeBadge.tsx              ← Type badge (6 values)
      IssueOverdueBadge.tsx           ← Overdue warning badge
      IssueEscalatedBadge.tsx         ← Escalation indicator badge

    api/
      issuesApi.ts                    ← Issues API calls
      issueCommentsApi.ts             ← Comments API calls
    hooks/
      useIssuesList.ts                ← List TanStack Query
      useIssueDetail.ts               ← Detail TanStack Query
      useIssueComments.ts             ← Comments TanStack Query (real-time subscription)
      useIssueStats.ts                ← Stats Query (reliability included)
      useIssueMutations.ts            ← Lifecycle mutations (triage/assign/resolve/etc.)
      useIssuePanelMode.ts            ← Right Panel mode state management
      useIssueLinks.ts                ← Entity link management
```

### 7.2 IssuesPage.tsx ViewMode 분기

```typescript
function IssuesPage() {
  const { viewMode } = useAccessContext();
  const { panelMode, setPanelMode } = useIssuePanelMode(viewMode);
  const [selectedIssue, setSelectedIssue] = useState<Issue | null>(null);

  // panelMode-based Right Panel component
  const rightPanelContent = useMemo(() => {
    switch (panelMode) {
      case "none":         return undefined;
      case "preview":      return (
        <IssuePreviewPanel
          issue={selectedIssue}
          viewMode={viewMode}
        />
      );
      case "resolution":   return <IssueResolveActions issue={selectedIssue} />;
      case "escalation":   return <IssueEscalationPanel issue={selectedIssue} />;
      default:             return <IssuePreviewPanel issue={selectedIssue} viewMode={viewMode} />;
    }
  }, [panelMode, selectedIssue, viewMode]);

  return (
    <PageLayout
      header={<IssuePageHeader viewMode={viewMode} />}
      rightPanel={rightPanelContent}
      rightPanelDefault={PRESET_POLICIES[viewMode].ui.defaultRightPanel}
    >
      <IssueKpiRow viewMode={viewMode} />
      <IssueFilters
        viewMode={viewMode}
        filterSpec={issuesNode.filterSpec}
      />
      <IssueTable
        viewMode={viewMode}
        columns={getIssueColumnsForPreset(viewMode)}
        onRowSelect={(issue) => {
          setSelectedIssue(issue);
          if (viewMode === "EXEC_SUMMARY") {
            setPanelMode("none");
          } else {
            setPanelMode("preview");
          }
        }}
      />
    </PageLayout>
  );
}
```

### 7.3 IssueRightPanel.tsx — panelMode 분기

```typescript
interface IssueRightPanelProps {
  panelMode: IssuePanelMode;
  selectedIssue?: Issue;
  viewMode: ViewModePreset;
}

function IssueRightPanel({ panelMode, selectedIssue, viewMode }: IssueRightPanelProps) {
  switch (panelMode) {
    case "none":
      return null;
    case "preview":
      return (
        <>
          <IssuePreviewPanel issue={selectedIssue} viewMode={viewMode} />
          {viewMode === "CUSTOMER_APPROVAL" && (
            <IssueApprovalBox issue={selectedIssue} />
          )}
        </>
      );
    case "detail":
      return <IssueContextPanel issue={selectedIssue} />;
    case "resolution":
      return <IssueResolveActions issue={selectedIssue} />;
    case "escalation":
      return <IssueEscalationPanel issue={selectedIssue} />;
  }
}
```

---

## 8. AI Navigation 연동

### 8.1 virtualNode 우선 반환 정책

> AI는 가능한 경우 raw filter 대신 **virtualId를 우선 반환**한다.
> virtualId 기반 딥링크는 파라미터 오류 가능성이 낮고, 사용자에게 "의미있는 업무 개념"으로 안내된다.

```typescript
/** AI virtualNode matching priority */
const ISSUE_VIRTUAL_NODE_PRIORITY: {
  virtualId: string;
  matchFilter: Partial<IssueFilterSpec>;
}[] = [
  { virtualId: "issues.critical",    matchFilter: { severity: "CRITICAL" } },
  { virtualId: "issues.my-assigned", matchFilter: { assigneeId: "{{currentUserId}}" } },
  { virtualId: "issues.overdue",     matchFilter: { overdue: true } },
  { virtualId: "issues.escalated",   matchFilter: { escalated: true } },
  { virtualId: "issues.blockers",    matchFilter: { type: "BLOCKER" } },
];

function resolveIssueNavigation(filter: IssueFilterSpec): AiIssueNavigation {
  // 1. virtualNode match attempt (priority)
  for (const vn of ISSUE_VIRTUAL_NODE_PRIORITY) {
    if (isSubsetMatch(vn.matchFilter, filter)) {
      return { targetNodeId: "issues", virtualId: vn.virtualId, filter, reason: "..." };
    }
  }
  // 2. No match → raw filter
  return { targetNodeId: "issues", filter, reason: "..." };
}
```

### 8.2 AI 질문 → 화면 이동 (FilterSpec + virtualId)

```typescript
/** AI navigation recommendation (v2.0) */
interface AiIssueNavigation {
  targetNodeId: "issues";
  /** v2.0: virtualNode priority return */
  virtualId?: string;
  /** FilterSpec-based filter object */
  filter: IssueFilterSpec;
  /** Direct entity navigation */
  entityId?: string;                   // "ISS-042"
  reason: string;
}
```

| 질문 | AI 반환 | virtualId? | 설명 |
|------|--------|-----------|------|
| "CRITICAL 이슈 있어?" | `{ severity: "CRITICAL" }` | `issues.critical` | virtualId 우선 |
| "내 이슈 보여줘" | `{ assigneeId: "me" }` | `issues.my-assigned` | virtualId 우선 |
| "기한 초과 이슈는?" | `{ overdue: true }` | `issues.overdue` | virtualId 우선 |
| "에스컬레이션 된 이슈" | `{ escalated: true }` | `issues.escalated` | virtualId 우선 |
| "블로커 목록" | `{ type: "BLOCKER" }` | `issues.blockers` | virtualId 우선 |
| "ISS-042 상세" | `entityId: "ISS-042"` | — | 직접 이동 |
| "이번 스프린트 이슈" | `{ sprintId: "sprint-7" }` | — | raw filter |
| "미해결 이슈 목록" | `{ status: "OPEN" }` | — | raw filter |

### 8.3 Preset별 do intent AI 라우팅

AI는 조치(do) 질문 시 §1.3의 `ISSUE_DO_INTENT_ROUTING`에 따라 targetNodeId를 결정한다.

```
PM이 "이슈 등록해줘" → targetNodeId: "issues"
PMO가 "에스컬레이션해줘" → targetNodeId: "decisions"
DEV가 "이슈 보고해줘" → targetNodeId: "issues"
Customer가 "이슈 요청해줘" → targetNodeId: "issues"
```

### 8.4 Context Panel에서의 AI 추천

```
── 추천 액션 (v2.1: reasonCode 표시) ─────────────────
🔴 ISS-042 CRITICAL / 기한 D-2
  → [Triage 시작]               ← targetNodeId: "issues", action: "triage"
    reasonCode: "CRITICAL + D-2"   ← 근거 (tooltip)

⚠ TSK-155 반입 UI 구현 BLOCKED
  → [칸반에서 확인]             ← targetNodeId: "kanban", filter: { status: "BLOCKED" }
    reasonCode: "BLOCKED Task 2건" ← 근거 (tooltip)

⚠ 에스컬레이션 미적용 — 4일 경과
  → [리스크 에스컬레이션]       ← targetNodeId: "decisions"
    reasonCode: "미적용 4일 경과"  ← 근거 (tooltip)

⏸ Decision Pending — DEC-015 결정 대기
  → [결정 보드에서 확인]        ← targetNodeId: "decisions", entityId: "DEC-015"
    reasonCode: "Decision Pending 3일"

ℹ 관련 Epic E-12 진행률 45%
  → [백로그에서 확인]           ← targetNodeId: "backlog", entityId: "E-12"
    reasonCode: "관련 Epic 45%"
```

> **v2.1: AI 추천 액션 데이터 구조**
>
> ```typescript
> interface AiRecommendedAction {
>   label: string;                     // CTA 텍스트
>   targetNodeId: string;              // 이동 대상 노드
>   filter?: Partial<IssueFilterSpec>; // 필터 (optional)
>   entityId?: string;                 // 직접 이동 (optional)
>   action?: string;                   // 트리거 액션 (optional)
>   /** v2.1: 추천 근거 — tooltip으로 표시, P4 Observability 연결 */
>   reasonCode: string;
>   /** v2.1: 추천 우선순위 (높을수록 상단) */
>   priority: number;
> }
> ```

---

## 9. 인터랙션 상세

### 9.1 테이블 행 클릭

```
행 클릭 → panelMode에 따라 Right Panel 내용 변경
  EXEC_SUMMARY: panelMode="none" → 변화 없음
  PM_WORK/PMO_CONTROL: panelMode="preview" → 이슈 요약 + 영향 분석 + 추천 액션
  DEV_EXECUTION: panelMode="preview" → 이슈 요약 (읽기 전용 + 코멘트 가능)
  CUSTOMER_APPROVAL: panelMode="preview" → 이슈 요약 + IssueApprovalBox

행 더블클릭 → /issues/:issueId 상세 페이지로 이동
```

### 9.2 인라인 CTA

```
PM_WORK Preset:
  OPEN 이슈:        [Triage]      → IssueTriageDialog (cap: manage_issues)
  TRIAGED 이슈:     [Assign]      → IssueAssignDialog (cap: manage_issues)
  ASSIGNED 이슈:    [Start]       → PATCH /start (cap: manage_issues)
  IN_PROGRESS 이슈: [Resolve]     → IssueResolveDialog (cap: manage_issues)
  RESOLVED 이슈:    [Close]       → PATCH /close (cap: manage_issues)
  미해결 이슈:      [에스컬레이션] → IssueEscalateDialog (cap: manage_issues)

DEV_EXECUTION Preset:
  ASSIGNED (내것):   [Start]       → PATCH /start (self-assign only)
  IN_PROGRESS (내것): [Resolve]    → IssueResolveDialog (self-assign only)
  [이슈 보고]                      → IssueCreateModal (cap: manage_issues)

CUSTOMER_APPROVAL Preset:
  [이슈 요청]                      → IssueCreateModal (cap: manage_issues)
  [해결 확인 요청]                  → Comment with request tag
```

### 9.3 코멘트 스레드

```
코멘트 입력 → POST /api/issues/:issueId/comments
  모든 Preset에서 코멘트 작성 가능 (view_issues 이상)
  @mention 지원 → 언급된 사용자에게 알림
  시스템 코멘트 자동 생성:
    - 상태 전이 시 (isSystem=true)
    - 에스컬레이션 시 (isSystem=true)
    - 담당자 변경 시 (isSystem=true)
```

### 9.4 관련 엔티티 링크

```
PM_WORK Preset (cap: manage_issues):
  [+ 연결] 클릭 → IssueLinkDialog 열림
    → Task/Story/Epic 검색
    → 관계 유형 선택 (blocks/caused_by/related_to)
    → 연결 확정 → POST /api/issues/:issueId/links

  관련 엔티티 클릭:
    Task → targetNodeId: "kanban", entityId: taskId
    Story → targetNodeId: "backlog", entityId: storyId
    Epic → targetNodeId: "backlog", entityId: epicId
```

### 9.5 에스컬레이션

```
PM_WORK / PMO_CONTROL Preset (cap: manage_issues):
  [에스컬레이션] 클릭 → IssueEscalateDialog 열림
    → 에스컬레이션 유형 선택 (Risk / Decision)
    → 사유 입력 (최소 10자)
    → 긴급도 선택 (immediate/next_review/informational)
    → Risk: 확률/영향/대응계획 입력
    → Decision: 선택지/권고안/기한 입력
    → POST /api/issues/:issueId/escalate
    → 성공 시 결정/리스크 보드 이동 CTA 표시
```

### 9.6 KPI 카드 클릭 → 필터 활성화

```typescript
/** KPI card click → filter activation mapping */
const ISSUE_KPI_CARD_FILTER_MAP: Record<string, {
  filter: Partial<IssueFilterSpec>;
  virtualId?: string;
}> = {
  KPI_TOTAL:          { filter: {} },
  KPI_OPEN:           { filter: { status: "OPEN" } },
  KPI_CRITICAL:       { filter: { severity: "CRITICAL" }, virtualId: "issues.critical" },
  KPI_AVG_RESOLUTION: { filter: {} },  // Info only, no filter
  KPI_OVERDUE:        { filter: { overdue: true }, virtualId: "issues.overdue" },
  KPI_ESCALATED:      { filter: { escalated: true }, virtualId: "issues.escalated" },
  KPI_BLOCKED_TASKS:  { filter: { type: "BLOCKER" }, virtualId: "issues.blockers" },
};
```

**인터랙션 흐름**:

```
1. KPI 카드 클릭
2. ISSUE_KPI_CARD_FILTER_MAP에서 filter 객체 조회
3. 현재 필터 바에 merge (기존 필터 초기화 → 새 필터 적용)
4. URL 업데이트 (§2.4 serializeIssueFilter)
5. 동일 카드 재클릭 시 필터 해제 (toggle 동작)
```

### 9.7 데이터 새로고침

| 항목 | 주기 |
|------|------|
| 이슈 목록 | 페이지 진입 시 + 필터 변경 시 |
| 이슈 상세 | 페이지 진입 시 |
| 코멘트 | 실시간 구독 (WebSocket/SSE) + 폴링 30초 fallback |
| 통계 | 페이지 진입 시 (캐시 5분) |
| 관련 엔티티 | 상세 페이지 진입 시 |

**v2.1: 이벤트 기반 refetch 규칙**

| 이벤트 | refetch 대상 | optimistic update |
|--------|-------------|-------------------|
| 상태 전이 (triage/assign/start/resolve/close/reopen) | `issues-list`, `issue-detail`, `issue-stats` | O (즉시 상태 반영) |
| 에스컬레이션 | `issues-list`, `issue-detail`, `issue-stats`, `decisions` | O (escalated=true, escalationRefs 추가) |
| 코멘트 등록 | `issue-comments` | O (낙관적 추가) |
| 엔티티 링크 변경 | `issue-detail`, `issue-links` | X (서버 응답 대기) |
| Decision 결과 반영 (외부) | `issues-list`, `issue-detail`, `issue-stats` | X (decisionPending 재계산) |
| Resolution + unblock | `issues-list`, `issue-detail`, `issue-stats`, `kanban-tasks` | O (BLOCKED→TODO) |

> **원칙**: 상태 변경/에스컬레이션 직후 **1회 강제 refetch** + optimistic update로 체감 즉시 반영.
> 폴링은 보조 수단으로만 사용.

### 9.8 Issue ↔ Task 동기화 정책 (v2.1)

> **현재 정책**: Task 완료 시 Issue 자동 resolve **하지 않음** (명시적 정책).
> Issue resolve는 항상 PM의 판단에 의해 수행된다.

```typescript
/**
 * v2.1: future hook — Task 완료 시 관련 Issue resolve 제안 (no auto)
 * Issue에 연결된 모든 Task가 DONE이면 PM에게 resolve 제안 알림
 */
// onTaskCompleted(taskId: string): void {
//   const relatedIssues = findIssuesByLinkedTask(taskId);
//   for (const issue of relatedIssues) {
//     const allTasksDone = issue.linkedTaskIds
//       .every(tid => getTaskStatus(tid) === "DONE");
//     if (allTasksDone && issue.status === "IN_PROGRESS") {
//       notifyPM(issue, "suggest_resolve",
//         `모든 연결 Task 완료 — Issue ${issue.id} resolve 검토 필요`);
//     }
//   }
// }
```

---

## 10. 반응형 대응

| 너비 | 테이블 | Right Panel | 필터 |
|------|--------|------------|------|
| >=1440px | 전체 컬럼 | Right Panel (30%) | 인라인 |
| 1280px | 축약 컬럼 (-3개) | Drawer (toggle) | 인라인 |
| <=1024px | 카드 뷰 전환 | Drawer (overlay) | 접이식 |
| <=768px | 카드 뷰 (1열) | Full Modal | 접이식 |

**카드 뷰 (<= 1024px) 구조**:

```
┌─ Issue Card ────────────────────────┐
│ 🔴 CRITICAL  │  BUG  │  OPEN       │
│                                      │
│ ISS-042  API 응답 지연               │
│                                      │
│ 보고: 이○○  │  담당: 김○○          │
│ 기한: 2026-02-10  │  ⚠ D-2         │
│                                      │
│ Task: 2  │  Comment: 3              │
│ [ Triage ] [ 상세 ▶ ]              │
└──────────────────────────────────────┘
```

---

## 11. 깨지기 쉬운 곳 체크리스트 — 8곳 (v2.1: 5→8)

| # | 위험 지점 | 해결책 | 검증 방법 |
|---|---------|--------|---------|
| 1 | **상태 전이 무결성** | 모든 전이는 서버 트랜잭션으로만 처리. 프론트 UI는 API 응답으로만 상태 갱신. 잘못된 전이 시 409 반환 | OPEN 이슈에 직접 /resolve 호출 → 409 INVALID_STATUS_TRANSITION 확인 |
| 2 | **에스컬레이션 중복 방지** | `escalated=true` 플래그로 중복 에스컬레이션 차단. 409 ALREADY_ESCALATED 반환 | 이미 에스컬레이션 된 이슈에 재 에스컬레이션 시도 → 409 수신 확인 |
| 3 | **Resolution 시 BLOCKED Task 처리** | Resolve 요청에 `unblockRelatedTasks` 옵션 포함. 사용자가 명시적으로 선택해야 함. 서버 트랜잭션으로 원자성 보장 | FIX 해결 시 BLOCKED Task가 TODO로 변경되는지 확인. unblockRelatedTasks=false 시 BLOCKED 유지 확인 |
| 4 | **코멘트 실시간 동기화** | WebSocket/SSE 우선, 30초 폴링 fallback. 연결 끊김 시 자동 재연결 + 누락 코멘트 동기화 | 두 브라우저에서 같은 이슈 상세 열기 → 한쪽에서 코멘트 → 다른 쪽 실시간 표시 확인 |
| 5 | **동시 상태 변경 충돌** | 낙관적 잠금(If-Match + version). 409 Conflict 시 현재 데이터 반환 + "다른 사용자가 상태를 변경했습니다" 안내 | 두 PM이 같은 이슈를 동시에 Triage → 후자가 409 수신 확인 |
| 6 | **escalationRefs[] ↔ Decision/Risk 정합성** (v2.1) | Issue에는 `escalationRefs[]` 경량 참조만 유지. Decision/Risk 상태 변경 시 `decisionPending` 서버에서 재계산. Issue가 오래된 참조를 캐싱하지 않음 | Decision DECIDED 후 Issue의 `decisionPending=false` 확인. Decision 삭제/취소 시 `escalationRefs`에서 제거 확인 |
| 7 | **avgResolutionTime 정의 불일치** (v2.1) | 정의: `resolvedAt − createdAt`, reopen 시 첫 resolve만 포함. RESOLVED/CLOSED만 집계. IssuesListResponse.stats와 IssueStatsResponse.stats에서 동일 로직 사용 | reopen 후 재 resolve된 이슈의 avgResolutionTime이 첫 resolve 기준인지 확인 |
| 8 | **Decision Pending 배지 ↔ FilterSpec 동기화** (v2.1) | `decisionPending` 서버 파생 필드. FE는 Decision 상태를 직접 계산하지 않음. Decision 결과 반영 시 이벤트 기반 refetch로 동기화 | `decisionPending=true` 필터 적용 → Decision DECIDED 후 해당 이슈 필터에서 제외 확인 |

---

## 12. Capability 매핑

| Capability | 행위 | 대표 역할 | v2.1 비고 |
|-----------|------|---------|----------|
| `view_issues` | 이슈 조회, 목록 탐색, 코멘트 조회 | ALL (역할별 범위 제한) | DevReader는 `view_issues`만 — read-only |
| `manage_issues` | 이슈 등록, Triage, Assign, Resolve, Close, Reopen, Escalate, 코멘트 작성, 엔티티 링크 | PM, PMO | v2.1: DevReader 제외 (read-only) |
| `manage_issues` (DEV 제한) | 이슈 보고(등록), 코멘트 작성, 자기 할당 이슈 Start/Resolve | DEV | 서버 행위별 역할 검증 |
| `manage_issues` (Customer 제한) | 이슈 요청(등록), 코멘트 작성 | Customer PM | 서버 행위별 역할 검증 |
| `export_reports` | 이슈 리포트 Export | PMO | — |

> **v2.1: DevReader 구분**
>
> - **DevReader** = 다른 파트 소속 DEV로, 본 프로젝트에는 읽기 전용 참여
> - DevReader는 `view_issues`만 보유 — 이슈 조회/필터/코멘트 조회만 가능
> - 코멘트 작성, 이슈 등록, 상태 변경 모두 불가
> - Preset은 DEV_EXECUTION과 동일하되 모든 CTA 숨김
> - 서버에서 `manage_issues` Capability 미부여로 강제

> **DEV 행동 제한**: DEV는 `manage_issues` Capability를 보유하지만,
> 서버에서 **행위별 역할 검증**을 추가로 수행한다:
> - DEV는 이슈 등록(보고), 코멘트, 자기 할당 이슈의 Start/Resolve만 가능
> - Triage, Assign, Close, Escalate는 PM/PMO 역할에서만 허용
> - 프론트에서는 Preset에 따라 해당 CTA를 숨김 처리

---

## 13. 테이블 컬럼 정의

### 13.1 전체 컬럼 (PM_WORK 기준)

| # | 컬럼명 | 필드 | 너비 | 정렬 | 필터 |
|---|--------|------|------|------|------|
| 1 | 이슈 ID | id | 100px | 기본 | - |
| 2 | 제목 | title | 200px | - | ✅ (q) |
| 3 | 유형 | type | 100px | - | ✅ |
| 4 | 심각도 | severity | 90px | ✅ | ✅ |
| 5 | 상태 | status | 110px | - | ✅ |
| 6 | 담당자 | assigneeName | 100px | - | ✅ |
| 7 | 보고자 | reporterName | 100px | - | ✅ |
| 8 | Part | partName | 100px | - | ✅ |
| 9 | Sprint | sprintName | 100px | - | ✅ |
| 10 | 기한 | dueDate | 100px | ✅ | - |
| 11 | 코멘트 | commentCount | 60px | - | - |
| 12 | 관련 Task | linkedTaskIds.length | 60px | - | - |
| 13 | 에스컬레이션 | escalated | 60px | - | ✅ |

### 13.2 Preset별 숨김 컬럼

| Preset | 숨김 컬럼 |
|--------|---------|
| EXEC_SUMMARY | #7, #8, #9, #11, #12 |
| PMO_CONTROL | #9 |
| PM_WORK | (전체 표시) |
| DEV_EXECUTION | #7, #8, #13 |
| CUSTOMER_APPROVAL | #7, #8, #9, #11, #12, #13 |

---

## 14. DoD (본 화면 완료 기준)

- [ ] `/issues` 목록에서 Preset별 컬럼 분기 동작
- [ ] FilterSpec 기반 다차원 필터 (type/severity/status/assigneeId/reporterId/partId/sprintId/epicId/phaseId/dateFrom/dateTo/overdue/escalated/resolutionType/q) 동작
- [ ] KPI Row: 전체 건수, 미해결, CRITICAL, 평균 해결 시간, 기한초과, 에스컬레이션, 블로커 차단 Task 표시
- [ ] panelMode 기반 Right Panel 분기 (none/preview/detail/resolution/escalation)
- [ ] 행 클릭 → panelMode에 따라 Right Panel 내용 변경
- [ ] `/issues/:issueId` 상세 페이지 표시
- [ ] 이슈 기본 정보 + 상태 전이 타임라인 표시
- [ ] **서버 트랜잭션**: 6단계 상태 전이 (OPEN→TRIAGED→ASSIGNED→IN_PROGRESS→RESOLVED→CLOSED + REOPENED)
- [ ] Triage: type + severity 확정 (cap: `manage_issues`)
- [ ] Assign: 담당자 배정 (cap: `manage_issues`)
- [ ] Start: 처리 시작 (cap: `manage_issues`, self-assign OK)
- [ ] Resolve: 4종 resolution (FIX/WONT_FIX/DUPLICATE/DEFERRED) + comment (cap: `manage_issues`)
- [ ] Close: 최종 종료 (cap: `manage_issues`)
- [ ] Reopen: 재개 + 사유 (cap: `manage_issues`)
- [ ] **에스컬레이션**: Issue → Risk / Decision 에스컬레이션 (cap: `manage_issues`)
- [ ] 에스컬레이션 중복 방지 (escalated=true 플래그)
- [ ] **Resolution 부수 효과**: FIX 시 BLOCKED Task 자동 해제 옵션 (서버 트랜잭션)
- [ ] **코멘트 스레드**: 등록/조회/편집/삭제 + @mention + 시스템 코멘트
- [ ] 코멘트 실시간 구독 (WebSocket/SSE + 30초 폴링 fallback)
- [ ] **관련 엔티티 링크**: Task/Story/Epic 양방향 링크 (blocks/caused_by/related_to)
- [ ] 관련 엔티티 클릭 → targetNodeId 기반 딥링크 이동
- [ ] DEV: 이슈 보고 + 코멘트 + 자기 할당 이슈 Start/Resolve
- [ ] CUSTOMER_APPROVAL: IssueApprovalBox (이슈 요청, 해결 확인 요청)
- [ ] **virtualNode 우선 반환**: CRITICAL/MY-ASSIGNED/OVERDUE/ESCALATED/BLOCKERS 5개 virtualId AI 우선
- [ ] **Preset별 do intent 라우팅** (PM→issues, PMO→issues, DEV→issues, Customer→issues, escalate→decisions)
- [ ] AI 추천 액션: FilterSpec + virtualId 딥링크 + CRITICAL/기한초과/에스컬레이션 알림
- [ ] KPI 카드 클릭 → 필터 활성화 (toggle 동작)
- [ ] **Lifecycle API 에러 계약**: 7종 에러 코드별 HTTP 상태 + 프론트 대응
- [ ] **동시 상태 변경 충돌**: 낙관적 잠금(If-Match + version) + 409 병합 안내
- [ ] 반응형 레이아웃 (>=1440 / 1280 / <=1024 / <=768)
- [ ] **FilterSpec URL 직렬화**: serialize/deserialize 왕복 보장, boolean "true"만 전송, enum 화이트리스트 검증, 미지 키 silent drop

**v2.1 추가 DoD**:

- [ ] Issue.type에서 `RISK` 제거 — 5종(BUG/BLOCKER/CHANGE_REQUEST/QUESTION/IMPROVEMENT)만 허용
- [ ] `escalationRefs[]` 경량 참조 배열로 전환 — `escalatedToType`/`escalatedToId` 제거
- [ ] `decisionPending` 서버 파생 필드 — FilterSpec + virtualNode(`issues.decision-pending`) + ⏸ 배지 동작
- [ ] `avgResolutionTimeHours` 정의 검증: `resolvedAt − createdAt`, reopen 시 첫 resolve만 포함
- [ ] Reliability 3-tuple 표준화: `completeness: {overall, breakdown?}` + `warnings: {code, message, severity}[]`
- [ ] Scope echo: 모든 API 응답에 `scope: { projectId }` 포함
- [ ] Scope 2-tier: `securityKeys` (projectId — JWT 강제) + `explorationKeys` (나머지 필터) 분리
- [ ] AI 추천 액션 `reasonCode` 표시 (tooltip) + `priority` 정렬
- [ ] 이벤트 기반 refetch: 상태 변경/에스컬레이션 직후 1회 강제 refetch + optimistic update
- [ ] Decision 결과 반영 시 `decisionPending` refetch (`issues-list`, `issue-detail`, `issue-stats`)
- [ ] Issue ↔ Task 동기화 정책 훅 자리 확보 (주석 코드, 비활성)
- [ ] 깨지기 쉬운 곳 8항목 전체 검증 통과

---

## 부록 A. 변경이력

| 버전 | 날짜 | 변경 내용 |
|------|------|---------|
| v1.0 | 2026-02-08 | 초안 작성 |
| v2.0 | 2026-02-08 | 13개 영역 피드백 반영: (1) entities PascalCase + intents 16개 확장, (2) FilterSpec 온톨로지 내장 13키, (3) 이슈 생명주기 6단계 + REOPENED 상세 (서버 트랜잭션), (4) 에스컬레이션 경로 Issue→Risk→Decision 확정, (5) Resolution 4종 + BLOCKED Task 자동 해제, (6) Capability 내 행위별 역할 검증 (DEV 제한), (7) Right Panel panelMode 5종 정의, (8) 코멘트 스레드 실시간 구독, (9) 관련 엔티티 양방향 링크 (blocks/caused_by/related_to), (10) virtualNode 5개 (critical/my-assigned/overdue/escalated/blockers), (11) Preset별 do intent 라우팅 (ISSUE_DO_INTENT_ROUTING), (12) Lifecycle API 에러 계약 7종, (13) 깨지기 쉬운 5곳 체크리스트 |
| v2.1 | 2026-02-08 | 12개 영역 cross-review 반영: (1) Issue.type에서 RISK 제거 (도메인 순수성), (2) escalationRefs[] 경량 참조 배열 전환, (3) avgResolutionTime 정의 명시, (4) Decision Pending 파생 배지 + decisionPending FilterSpec/virtualNode, (5) AI 추천 액션 reasonCode 필드 추가, (6) Issue↔Task 동기화 정책 훅 자리 마련, (7) Reliability 3-tuple 표준화, (8) Scope echo 전 API 적용, (9) Scope 2-tier 분리, (10) 이벤트 기반 refetch 규칙 추가, (11) 깨지기 쉬운 곳 5→8 확장, (12) DevReader read-only 구분 + DoD v2.1 항목 추가 |
