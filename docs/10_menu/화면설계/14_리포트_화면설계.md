# 14. 리포트 화면설계

> 작성일: 2026-02-08
> 버전: v2.1
> 라우트: `/reports` (리포트 카탈로그 + 생성기), `/stats` (크로스 프로젝트 통계)
> 필요 Capability: `view_reports`, `generate_reports`, `export_reports`, `view_stats`, `admin_reports`
> 기본 Preset: 역할에 따라 자동 결정
> 노드 역할: **Hub** (허브 노드 — 다수 데이터 소스를 집계하여 보고)

---

## 1. 화면 개요

### 1.1 목적

리포트 화면은 **"공유/보고를 어떻게 할 것인가?"**에 대한 통합 진입점이다.
프로젝트의 다양한 데이터 소스(Phase, Sprint, Issue, Risk, Deliverable, Resource 등)를
집계하여 **표준 리포트 카탈로그 + 온디맨드 생성기 + 크로스 프로젝트 통계**를 제공한다.

> **v2.0 설계 원칙**: 리포트는 단순 PDF Export가 아니라,
> 인터랙티브 대시보드 형식의 **실시간 리포트 뷰어** + 스냅샷(PDF/Excel/PPT) 생성 이원 구조이다.

### 1.2 설계 원칙

| 원칙 | 설명 |
|------|------|
| **카탈로그 기반** | 9종 표준 리포트 템플릿을 카탈로그로 제공, 선택 → 생성 흐름 |
| **Preset 분기** | 역할에 따라 접근 가능 리포트 유형, 기본 필터, CTA가 달라짐 |
| **Hub 집계** | 다수 도메인(Phase/Sprint/Issue/Risk/Resource)의 데이터를 집계 |
| **이중 뷰** | 인터랙티브 뷰어(화면) + 스냅샷 Export(PDF/Excel/PPT) 병행 |
| **스케줄링** | 일간/주간/월간 자동 생성 + 이메일 배포 지원 |
| **AI CTA 통합** | AI가 리포트 추천 + 이상 징후 하이라이트 + 근거 링크 제공 |
| **Hub 페널티** | AI 스코어링에서 행동/목록 질문에는 허브 노드 페널티 적용. 단, 리포트/통계 키워드 포함 시 예외 (v2.1) |
| **ReportType ACL** | UI 숨김 + 서버 측 report_type_policy 정책 테이블로 2중 검증 (v2.1) |
| **서명 URL** | downloadUrl은 짧은 TTL 서명 URL + 다운로드 시점 권한 재검증 (v2.1) |
| **Stage 기반 생성** | 비동기 생성 상태를 queued→collecting→aggregating→rendering→completed/failed 단계로 세분화 (v2.1) |
| **Provenance 디버깅** | 리포트/통계의 provenance를 traceId/rowCount/cacheHit/latency/policyDecision 포함 운영 수준으로 확장 (v2.1) |
| **Scope 교집합** | /stats의 projectIds는 사용자 접근 가능 프로젝트와 교집합 처리, 빈 결과는 403 대신 빈 데이터 + 사유 (v2.1) |

### 1.3 핵심 질문 → 위젯/리포트 매핑

| 사용자 질문 | 대상 리포트/위젯 | Preset |
|-----------|----------------|--------|
| "프로젝트 현황 보고서 만들어줘" | Project Status Report | PM, PMO |
| "이번 스프린트 번다운 보여줘" | Sprint Burndown | PM, DEV |
| "EVM 지표 확인하고 싶어" | EVM/SPI/CPI Report | PMO, PM |
| "결함 밀도 추이는?" | Quality Dashboard | PM, PMO |
| "리소스 활용률 보여줘" | Resource Utilization | PMO, PM |
| "리스크 히트맵 보여줘" | Risk Heatmap | PM, PMO, EXEC |
| "Phase Gate 상태 확인" | Phase Gate Status | PMO, EXEC |
| "요구사항 추적 매트릭스 보여줘" | Requirement Traceability Matrix | PM, PMO |
| "전체 프로젝트 비교 통계" | Cross-Project Stats (/stats) | PMO, Sponsor |
| "속도 추이 차트 보여줘" | Velocity Trend | PM, DEV |

---

## 2. MenuOntology 노드

### 2.1 타입 확정 (v2.0 변경사항)

v1.0 대비 변경된 필드 규격:

| 항목 | v1.0 | v2.0 (확정) | 변경 사유 |
|------|------|------------|----------|
| `entities` | 소문자 `"project"` | PascalCase `"Project"` | EntityType enum 정규화 |
| `ui.highlightMetrics` | 단일 배열 | `ui.highlight.metricKeys` + `ui.highlight.widgetKeys` | 데이터 키 vs UI 위젯 키 분리 |
| `suggestedActions.capability` | 단일 `Capability` | `requiredCaps?: Capability[]` + `route?: string` | 복수 권한 + 목적지 명시 |
| `deepLinks.pattern` | `{reportType}` 형태 | `:reportType` 형태 | 라우트 템플릿 표준 통일 |
| `nodeRole` | 없음 | `"hub"` 신규 | AI 스코어링 페널티 기준 |
| `filterSpec` | 없음 | 리포트 전용 FilterSpec 추가 | 다차원 필터 지원 |

### 2.2 FilterSpec 정의

```typescript
/** 리포트 화면 전용 FilterSpec */
interface ReportFilterSpec {
  /** 리포트 유형 */
  reportType?: ReportType;
  /** 기간 범위 */
  dateRange?: {
    from: string;  // ISO date
    to: string;    // ISO date
  };
  /** 단계 필터 */
  phaseId?: string;
  /** 파트 필터 */
  partId?: string;
  /** 스프린트 필터 */
  sprintId?: string;
  /** Export 형식 */
  format?: "pdf" | "excel" | "pptx";
  /** 스케줄 필터 */
  scheduled?: boolean;
}

type ReportType =
  | "project_status"
  | "sprint_burndown"
  | "velocity_trend"
  | "evm_spi_cpi"
  | "quality_dashboard"
  | "resource_utilization"
  | "risk_heatmap"
  | "phase_gate_status"
  | "requirement_traceability";

/** 통계 화면 전용 FilterSpec */
interface StatsFilterSpec {
  /** 기간 범위 */
  dateRange?: {
    from: string;
    to: string;
  };
  /** 프로젝트 ID 목록 (다중 선택) */
  projectIds?: string[];
  /** 비교 지표 */
  metricKey?: string;
  /** 집계 단위 */
  aggregation?: "daily" | "weekly" | "monthly" | "quarterly";
}
```

### 2.3 Reports 노드 (확정본)

```typescript
const reportsNode: MenuOntologyNodeV2 = {
  nodeId: "reports",
  label: "프로젝트 리포트",
  route: "/reports",
  icon: "AssessmentRounded",
  domain: "reports",
  nodeRole: "hub",               // ★ 허브 노드 — 다수 소스 집계

  requiredCaps: ["view_reports"],

  intents: [
    "ask_report_export",
    "ask_overall_status",
    "ask_progress",
    "ask_sprint_velocity",
    "ask_risk",
    "ask_test_coverage",
    "ask_phase_status",
    "do_export_report",
  ],
  canonicalQuestions: [
    "프로젝트 현황 보고서 보여줘",
    "이번 스프린트 번다운 차트 보여줘",
    "EVM 지표 확인하고 싶어",
    "결함 밀도 추이 알려줘",
    "리소스 활용률 리포트 만들어줘",
    "리스크 히트맵 보여줘",
    "Phase Gate 상태 보고서",
    "요구사항 추적 매트릭스 보여줘",
    "리포트 PDF로 다운로드 해줘",
    "주간 보고서 자동 생성 설정해줘",
  ],
  entities: ["Project", "Phase", "Sprint", "Risk", "Issue", "Requirement", "Task"],
  keywords: [
    "리포트", "보고서", "통계", "차트", "현황", "번다운", "EVM", "SPI", "CPI",
    "결함", "품질", "리소스", "히트맵", "추적", "매트릭스", "Export",
    "report", "statistics", "chart", "burndown", "velocity", "quality",
    "resource", "heatmap", "traceability", "export", "pdf", "excel",
  ],
  metrics: [
    "report_count",
    "scheduled_report_count",
    "spi_index",
    "cpi_index",
    "defect_density",
    "test_coverage",
    "resource_utilization_rate",
    "risk_exposure",
    "requirement_coverage",
    "velocity_trend",
    "burndown_deviation",
  ],

  filterSpec: {
    keys: [
      "reportType", "dateRange", "phaseId", "partId",
      "sprintId", "format", "scheduled",
    ],
  },

  defaultPreset: "PM_WORK",
  presetPolicies: [
    {
      preset: "EXEC_SUMMARY",
      ui: {
        density: "compact",
        defaultRightPanel: "closed",
        highlight: {
          metricKeys: ["spi_index", "cpi_index", "risk_exposure"],
          widgetKeys: ["REPORT_CATALOG_SUMMARY", "PHASE_GATE_MINI"],
        },
      },
      suggestedActions: [
        {
          key: "view_phase_gate",
          label: "Phase Gate 보고서",
          requiredCaps: ["view_reports"],
          targetNodeId: "reports",
        },
      ],
    },
    {
      preset: "PMO_CONTROL",
      ui: {
        density: "standard",
        defaultRightPanel: "open",
        highlight: {
          metricKeys: ["spi_index", "cpi_index", "defect_density", "resource_utilization_rate"],
          widgetKeys: ["REPORT_CATALOG_FULL", "REPORT_SCHEDULE_PANEL"],
        },
      },
      suggestedActions: [
        {
          key: "generate_evm",
          label: "EVM 리포트 생성",
          requiredCaps: ["generate_reports"],
          targetNodeId: "reports",
        },
        {
          key: "export_all",
          label: "전체 리포트 Export",
          requiredCaps: ["export_reports"],
          targetNodeId: "reports",
        },
        {
          key: "view_stats",
          label: "크로스 프로젝트 통계",
          requiredCaps: ["view_stats"],
          targetNodeId: "stats",
        },
      ],
    },
    {
      preset: "PM_WORK",
      ui: {
        density: "detailed",
        defaultRightPanel: "open",
        highlight: {
          metricKeys: ["velocity_trend", "burndown_deviation", "defect_density"],
          widgetKeys: ["REPORT_CATALOG_FULL", "REPORT_PREVIEW_PANEL"],
        },
      },
      suggestedActions: [
        {
          key: "generate_status",
          label: "프로젝트 현황 보고서",
          requiredCaps: ["generate_reports"],
          targetNodeId: "reports",
        },
        {
          key: "generate_burndown",
          label: "스프린트 번다운",
          requiredCaps: ["generate_reports"],
          targetNodeId: "reports",
        },
        {
          key: "export_report",
          label: "리포트 Export",
          requiredCaps: ["export_reports"],
          targetNodeId: "reports",
        },
      ],
    },
    {
      preset: "DEV_EXECUTION",
      ui: {
        density: "standard",
        defaultRightPanel: "closed",
        defaultFilters: { reportType: "sprint_burndown" },
        highlight: {
          metricKeys: ["velocity_trend", "burndown_deviation"],
          widgetKeys: ["REPORT_CATALOG_LIMITED"],
        },
      },
      suggestedActions: [
        {
          key: "view_burndown",
          label: "스프린트 번다운",
          requiredCaps: ["view_reports"],
          targetNodeId: "reports",
        },
      ],
    },
  ],

  // ─── DeepLink: routeTemplate 표준 (:param 형태) ───
  deepLinks: [
    {
      pattern: "/reports?type=:reportType",
      description: "특정 리포트 유형으로 직접 진입",
      requiredParams: ["reportType"],
    },
    {
      pattern: "/reports?type=:reportType&phaseId=:phaseId",
      description: "단계별 리포트 필터",
      requiredParams: ["reportType", "phaseId"],
    },
    {
      pattern: "/reports?type=:reportType&sprintId=:sprintId",
      description: "스프린트별 리포트 필터",
      requiredParams: ["reportType", "sprintId"],
    },
    {
      pattern: "/reports?scheduled=true",
      description: "예약된 리포트 목록",
      requiredParams: [],
    },
    {
      pattern: "/stats",
      description: "크로스 프로젝트 통계 화면",
      requiredParams: [],
    },
  ],

  // ─── 가상 노드: 딥링크를 온톨로지 2계층으로 승격 ───
  virtualNodes: [
    {
      virtualId: "reports.sprint-burndown",
      label: "스프린트 번다운",
      routeTemplate: "/reports?type=sprint_burndown&sprintId=:sprintId",
      requiredParams: ["sprintId"],
      intents: ["ask_sprint_velocity", "ask_progress"],
      description: "현재 스프린트의 번다운 차트를 리포트에서 바로 확인",
    },
    {
      virtualId: "reports.evm",
      label: "EVM 리포트",
      routeTemplate: "/reports?type=evm_spi_cpi",
      requiredParams: [],
      intents: ["ask_overall_status", "ask_progress"],
      description: "EVM(SPI/CPI) 지표를 리포트에서 확인",
    },
    {
      virtualId: "reports.quality",
      label: "품질 대시보드",
      routeTemplate: "/reports?type=quality_dashboard",
      requiredParams: [],
      intents: ["ask_test_coverage"],
      description: "결함 밀도, 테스트 커버리지 등 품질 지표 리포트",
    },
    {
      virtualId: "reports.traceability",
      label: "요구사항 추적 매트릭스",
      routeTemplate: "/reports?type=requirement_traceability",
      requiredParams: [],
      intents: ["ask_requirement_status", "ask_trace_impact"],
      description: "요구사항 → 설계 → 코드 → 테스트 추적 매트릭스",
    },
    // --- v2.1: 사용 빈도 높은 리포트 3종 virtualNode 승격 ---
    {
      virtualId: "reports.risk-heatmap",
      label: "리스크 히트맵",
      routeTemplate: "/reports?type=risk_heatmap",
      requiredParams: [],
      matchFilters: { reportType: "risk_heatmap" },
      intents: ["ask_risk"],
      description: "리스크 발생확률 × 영향도 히트맵 리포트",
    },
    {
      virtualId: "reports.resource-utilization",
      label: "리소스 활용률",
      routeTemplate: "/reports?type=resource_utilization",
      requiredParams: [],
      matchFilters: { reportType: "resource_utilization" },
      intents: ["ask_resource_status"],
      description: "인력별/파트별 리소스 배분 및 활용률 리포트",
    },
    {
      virtualId: "reports.phase-gate",
      label: "Phase Gate 보고서",
      routeTemplate: "/reports?type=phase_gate_status",
      requiredParams: [],
      matchFilters: { reportType: "phase_gate_status" },
      intents: ["ask_phase_status"],
      description: "Phase Gate 통과 조건 충족 여부 및 승인 상태",
    },
  ],

  // ─── 스코프 2계층 분리 ───
  scope: {
    keys: ["project"],
    params: ["projectId"],
  },

  priority: 2,
};
```

### 2.4 Stats 노드 (확정본)

```typescript
const statsNode: MenuOntologyNodeV2 = {
  nodeId: "stats",
  label: "통계 대시보드",
  route: "/stats",
  icon: "BarChartRounded",
  domain: "reports",
  nodeRole: "hub",

  requiredCaps: ["view_stats"],

  intents: [
    "ask_overall_status",
    "ask_progress",
    "ask_health_score",
  ],
  canonicalQuestions: [
    "전체 프로젝트 비교 통계 보여줘",
    "포트폴리오 KPI 요약 알려줘",
    "프로젝트 건강도 매트릭스 보여줘",
    "리소스 배분 현황 알려줘",
    "프로젝트별 진행률 비교해줘",
  ],
  entities: ["Project", "Phase", "Sprint", "Risk"],
  keywords: [
    "통계", "비교", "포트폴리오", "크로스프로젝트", "건강도", "매트릭스",
    "stats", "statistics", "portfolio", "cross-project", "comparison",
  ],
  metrics: [
    "portfolio_health_avg",
    "project_count",
    "resource_allocation_rate",
    "overall_spi",
    "overall_cpi",
  ],

  defaultPreset: "PMO_CONTROL",
  presetPolicies: [
    {
      preset: "EXEC_SUMMARY",
      ui: {
        density: "compact",
        defaultRightPanel: "closed",
        highlight: {
          metricKeys: ["portfolio_health_avg", "project_count"],
          widgetKeys: ["PORTFOLIO_KPI_ROW", "PROJECT_HEALTH_MATRIX"],
        },
      },
    },
    {
      preset: "PMO_CONTROL",
      ui: {
        density: "standard",
        defaultRightPanel: "open",
        highlight: {
          metricKeys: ["portfolio_health_avg", "resource_allocation_rate", "overall_spi"],
          widgetKeys: ["PORTFOLIO_KPI_ROW", "PROJECT_HEALTH_MATRIX", "RESOURCE_ALLOCATION_CHART"],
        },
      },
      suggestedActions: [
        {
          key: "export_portfolio",
          label: "포트폴리오 리포트 Export",
          requiredCaps: ["export_reports"],
          targetNodeId: "reports",
        },
      ],
    },
  ],

  deepLinks: [
    {
      pattern: "/stats?metric=:metricKey",
      description: "특정 지표 중심 통계 뷰",
      requiredParams: ["metricKey"],
    },
    {
      pattern: "/stats?projectIds=:projectIds",
      description: "특정 프로젝트 비교 뷰",
      requiredParams: ["projectIds"],
    },
  ],

  virtualNodes: [],

  scope: {
    keys: ["project"],
    params: ["projectId"],
  },

  priority: 3,
};
```

---

## 3. 화면 구조

### 3.1 panelMode 정의

```typescript
type ReportPanelMode =
  | "none"          // Right Panel 닫힘
  | "preview"       // 리포트 미리보기
  | "schedule"      // 스케줄 설정
  | "export"        // Export 옵션
  | "filter";       // 고급 필터
```

### 3.2 /reports 와이어프레임

#### 3.2.1 PM_WORK (PM)

> **목표**: "필요한 리포트를 빠르게 찾아 생성하고, 바로 확인/공유한다"

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  프로젝트 리포트  [ AI 보험금지급심사 구축 ▼ ]                 │
│  [ Preset: PM ▼ ] [🔄 새로고침]                               │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 전체    │  │ 예약된  │  │ 최근    │  │ Export  │        │
│  │ 리포트  │  │ 리포트  │  │ 생성    │  │ 대기    │        │
│  │   9종   │  │   3건   │  │  5건    │  │   1건   │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│                                                              │
│  ┌──────── 리포트 카탈로그 ─────────────┬──────────────────┐ │
│  │                                      │  리포트 미리보기  │ │
│  │  ┌──────────────────────────────┐    │                  │ │
│  │  │ 📊 프로젝트 현황 보고서       │    │  [선택한 리포트  │ │
│  │  │    전체 프로젝트 상태 요약     │    │   미리보기 영역] │ │
│  │  │    최근 생성: 2026-02-07      │    │                  │ │
│  │  │    [생성] [Export] [스케줄]   │    │  기간: 전체      │ │
│  │  └──────────────────────────────┘    │  단계: S2        │ │
│  │                                      │  형식: PDF       │ │
│  │  ┌──────────────────────────────┐    │                  │ │
│  │  │ 📈 스프린트 번다운             │    │  ┌──────────┐   │ │
│  │  │    번다운 차트 + 속도 추이     │    │  │ 차트     │   │ │
│  │  │    최근 생성: 2026-02-08      │    │  │ 미리보기 │   │ │
│  │  │    [생성] [Export]            │    │  │          │   │ │
│  │  └──────────────────────────────┘    │  └──────────┘   │ │
│  │                                      │                  │ │
│  │  ┌──────────────────────────────┐    │  [PDF Export]    │ │
│  │  │ 📉 Velocity Trend            │    │  [Excel Export]  │ │
│  │  │    스프린트별 속도 추이        │    │  [PPT Export]   │ │
│  │  │    최근 생성: 2026-02-06      │    │                  │ │
│  │  │    [생성] [Export]            │    │  ────────────── │ │
│  │  └──────────────────────────────┘    │  스케줄 설정     │ │
│  │                                      │  ☐ 매주 월요일   │ │
│  │  ┌──────────────────────────────┐    │  ☐ 매일 09:00   │ │
│  │  │ 💰 EVM (SPI/CPI)             │    │  ☐ 매월 1일     │ │
│  │  │    Earned Value 지표 분석     │    │                  │ │
│  │  │    SPI: 0.92  CPI: 0.87      │    │                  │ │
│  │  │    [생성] [Export] [스케줄]   │    │                  │ │
│  │  └──────────────────────────────┘    │                  │ │
│  │                                      │                  │ │
│  │  ┌──────────────────────────────┐    │                  │ │
│  │  │ 🔍 품질 대시보드              │    │                  │ │
│  │  │    결함 밀도, 테스트 커버리지  │    │                  │ │
│  │  │    [생성] [Export]            │    │                  │ │
│  │  └──────────────────────────────┘    │                  │ │
│  │                                      │                  │ │
│  │  ... (Resource, Risk Heatmap,       │                  │ │
│  │       Phase Gate, Traceability)     │                  │ │
│  │                                      │                  │ │
│  └──────────────────────────────────────┴──────────────────┘ │
│                                                              │
│  ┌─────────────────────┐  ┌──────────────────────┐           │
│  │ [ 현황 보고서 생성 ]  │  │ [ 리포트 Export ]     │           │
│  │   Primary CTA        │  │   Secondary CTA      │           │
│  └─────────────────────┘  └──────────────────────┘           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**위젯 구성**:

| 위젯 | WidgetKey | 위치 | 데이터 |
|------|-----------|------|--------|
| Total Report Types Card | `KPI_REPORT_TYPES` | 상단 KPI Row | report_type_count (9종) |
| Scheduled Reports Card | `KPI_SCHEDULED` | 상단 KPI Row | scheduled_report_count |
| Recent Generated Card | `KPI_RECENT_GENERATED` | 상단 KPI Row | recently_generated_count |
| Export Pending Card | `KPI_EXPORT_PENDING` | 상단 KPI Row | export_pending_count |
| Report Catalog (Full) | `REPORT_CATALOG_FULL` | 중앙 Main | report_types[] + last_generated + status |
| Report Preview Panel | `REPORT_PREVIEW_PANEL` | 우측 Panel | 선택된 리포트 미리보기 + 필터 + Export |

**CTA**: `현황 보고서 생성` → reportType: `project_status` (Primary), `리포트 Export` → export flow (Secondary)
**Right Panel**: 기본 `open` — 리포트 미리보기 고정

---

#### 3.2.2 PMO_CONTROL (PMO)

> **목표**: "모든 프로젝트의 리포트를 통제하고, 스케줄 관리 및 일괄 Export를 수행한다"

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  프로젝트 리포트  [ 전체 프로젝트 ▼ ]  [ Export ⬇ ]           │
│  [ Preset: PMO ▼ ] [🔄 새로고침]                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ SPI     │  │  CPI    │  │ 예약    │  │ 미생성  │        │
│  │  0.92   │  │  0.87   │  │ 리포트  │  │ 리포트  │        │
│  │  🟡     │  │  🟡     │  │   5건   │  │   2건   │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│                                                              │
│  ┌──────── 리포트 카탈로그 ─────────────┬──────────────────┐ │
│  │                                      │  스케줄 관리      │ │
│  │  [전체] [생성됨] [예약됨] [미생성]    │                  │ │
│  │                                      │  주간 보고서      │ │
│  │  리포트명        | 상태 | 최근  | 예약 │  매주 월 09:00   │ │
│  │  ─────────────────────────────────── │  수신: PM 전체   │ │
│  │  프로젝트 현황   | ✅  | 2/7  | 주간 │  [편집] [중지]   │ │
│  │  스프린트 번다운 | ✅  | 2/8  | -    │                  │ │
│  │  Velocity Trend  | ✅  | 2/6  | -    │  EVM 리포트      │ │
│  │  EVM (SPI/CPI)  | ✅  | 2/5  | 월간 │  매월 1일 09:00  │ │
│  │  품질 대시보드   | ✅  | 2/4  | -    │  수신: PMO       │ │
│  │  리소스 활용률   | ⚠  | 2/1  | -    │  [편집] [중지]   │ │
│  │  리스크 히트맵   | ✅  | 2/7  | 주간 │                  │ │
│  │  Phase Gate     | ✅  | 2/3  | -    │  리스크 히트맵    │ │
│  │  추적 매트릭스   | ❌  | -    | -    │  매주 금 17:00   │ │
│  │                                      │  수신: PM, PMO   │ │
│  │  ▸ 행 클릭 → 미리보기/Export         │  [편집] [중지]   │ │
│  └──────────────────────────────────────┴──────────────────┘ │
│                                                              │
│  ┌─────────────────────┐  ┌──────────────────────┐           │
│  │ [ EVM 리포트 생성 ]  │  │ [ 전체 Export ]       │           │
│  │   Primary CTA        │  │   Secondary CTA      │           │
│  └─────────────────────┘  └──────────────────────┘           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**위젯 구성**:

| 위젯 | WidgetKey | 위치 | 데이터 |
|------|-----------|------|--------|
| SPI Card | `KPI_SPI` | 상단 KPI Row | spi_index |
| CPI Card | `KPI_CPI` | 상단 KPI Row | cpi_index |
| Scheduled Count Card | `KPI_SCHEDULED` | 상단 KPI Row | scheduled_report_count |
| Not Generated Card | `KPI_NOT_GENERATED` | 상단 KPI Row | not_generated_count |
| Report Catalog (Full) | `REPORT_CATALOG_FULL` | 중앙 Main | 테이블 형태 카탈로그 |
| Schedule Panel | `REPORT_SCHEDULE_PANEL` | 우측 Panel | 예약 리포트 목록 + 편집/중지 |

---

#### 3.2.3 EXEC_SUMMARY (Sponsor)

> **목표**: "핵심 보고서만 빠르게 확인하고, 필요하면 다운로드한다"

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  프로젝트 리포트  [ AI 보험금지급심사 구축 ▼ ]                 │
│  [ Preset: EXEC ▼ ] [🔄 새로고침]                             │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                      │
│  │ SPI     │  │  CPI    │  │ 리스크  │                      │
│  │  0.92   │  │  0.87   │  │ 노출도  │                      │
│  │  🟡     │  │  🟡     │  │  High   │                      │
│  └─────────┘  └─────────┘  └─────────┘                      │
│                                                              │
│  ┌────────────────────────────────────────────────────┐      │
│  │  핵심 리포트 (3종)                                  │      │
│  │                                                    │      │
│  │  ┌─────────────────────────────────────────────┐   │      │
│  │  │ 📊 프로젝트 현황 보고서     [보기] [PDF ⬇]  │   │      │
│  │  │    최근: 2026-02-07 | 상태: 정상             │   │      │
│  │  └─────────────────────────────────────────────┘   │      │
│  │                                                    │      │
│  │  ┌─────────────────────────────────────────────┐   │      │
│  │  │ 💰 EVM (SPI/CPI) 리포트     [보기] [PDF ⬇]  │   │      │
│  │  │    최근: 2026-02-05 | SPI: 0.92, CPI: 0.87  │   │      │
│  │  └─────────────────────────────────────────────┘   │      │
│  │                                                    │      │
│  │  ┌─────────────────────────────────────────────┐   │      │
│  │  │ 🔥 Phase Gate 상태 보고서   [보기] [PDF ⬇]  │   │      │
│  │  │    최근: 2026-02-03 | Gate2: 승인대기        │   │      │
│  │  └─────────────────────────────────────────────┘   │      │
│  │                                                    │      │
│  └────────────────────────────────────────────────────┘      │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

### 3.3 /stats 와이어프레임 (PMO_CONTROL)

> **목표**: "여러 프로젝트를 비교하고 포트폴리오 수준의 의사결정을 지원한다"

```
┌──────────────────────────────────────────────────────────────┐
│  Page Header                                                  │
│  통계 대시보드  [ 전체 프로젝트 ]  [ 기간: 최근 3개월 ▼ ]      │
│  [ Preset: PMO ▼ ] [🔄 새로고침]  [ Export ⬇ ]               │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 프로젝트│  │ 평균   │  │ 평균    │  │ 리소스  │        │
│  │  Count  │  │ Health │  │  SPI    │  │ 배분율  │        │
│  │   3개   │  │  B+ 🟡 │  │  0.94   │  │  78%    │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│                                                              │
│  ┌──────── 프로젝트 비교 ──────────────┬──────────────────┐  │
│  │                                     │  상세 분석        │  │
│  │  프로젝트 건강도 매트릭스             │                  │  │
│  │                                     │  선택 프로젝트:   │  │
│  │  프로젝트명  | Health | SPI | CPI   │  AI 보험심사 구축 │  │
│  │  ──────────────────────────────────│                  │  │
│  │  AI보험심사  | 🟡 B+  | 0.92| 0.87 │  Health Trend    │  │
│  │  차세대Core  | 🟢 A   | 1.05| 1.02 │  ▁▂▃▄▅▃▄▅       │  │
│  │  디지털채널  | 🔴 D   | 0.65| 0.58 │                  │  │
│  │                                     │  Risk Exposure   │  │
│  │  ─────────────────────────────     │  High: 2         │  │
│  │                                     │  Medium: 5       │  │
│  │  진행률 비교 차트                    │                  │  │
│  │  AI보험심사  ████████░░  72%        │  Resource Load   │  │
│  │  차세대Core  █████████░  85%        │  ████████░░ 82%  │  │
│  │  디지털채널  ████░░░░░░  45%        │                  │  │
│  │                                     │  [상세 보기 →]   │  │
│  ├─────────────────────────────────────│                  │  │
│  │  리소스 배분 현황                    │                  │  │
│  │                                     │                  │  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐        │                  │  │
│  │  │ BE   │ │ FE   │ │ QA   │        │                  │  │
│  │  │ 85%  │ │ 72%  │ │ 65%  │        │                  │  │
│  │  └──────┘ └──────┘ └──────┘        │                  │  │
│  │                                     │                  │  │
│  └─────────────────────────────────────┴──────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────┐                    │
│  │  [ 포트폴리오 리포트 Export ] (CTA)  │                    │
│  └──────────────────────────────────────┘                    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**위젯 구성**:

| 위젯 | WidgetKey | 위치 | 데이터 |
|------|-----------|------|--------|
| Project Count Card | `KPI_PROJECT_COUNT` | 상단 KPI Row | portfolio_project_count |
| Health Average Card | `KPI_HEALTH_AVG` | 상단 KPI Row | portfolio_health_avg |
| SPI Average Card | `KPI_SPI_AVG` | 상단 KPI Row | overall_spi |
| Resource Allocation Card | `KPI_RESOURCE_ALLOC` | 상단 KPI Row | resource_allocation_rate |
| Project Health Matrix | `PROJECT_HEALTH_MATRIX` | 중앙 상단 | projects[] + health/spi/cpi |
| Progress Comparison Chart | `PROGRESS_COMPARISON_CHART` | 중앙 중단 | projects[] + progress bar |
| Resource Allocation Chart | `RESOURCE_ALLOCATION_CHART` | 중앙 하단 | resource_groups[] + utilization |
| Detail Analysis Panel | `STATS_DETAIL_PANEL` | 우측 Panel | 선택 프로젝트 상세 분석 |

---

## 4. Preset별 차이

| 속성 | EXEC_SUMMARY | PMO_CONTROL | PM_WORK | DEV_EXECUTION |
|------|-------------|-------------|---------|---------------|
| **밀도(density)** | compact | standard | detailed | standard |
| **Right Panel** | closed | open (스케줄 관리) | open (미리보기) | closed |
| **표시 리포트** | 3종 (현황/EVM/PhaseGate) | 9종 전체 | 9종 전체 | 2종 (번다운/Velocity) |
| **KPI 핵심** | SPI, CPI, 리스크 노출도 | SPI, CPI, 예약건수, 미생성건수 | 전체리포트, 예약, 최근생성, Export대기 | Velocity, 번다운편차 |
| **CTA (Primary)** | Phase Gate 보고서 | EVM 리포트 생성 | 현황 보고서 생성 | 스프린트 번다운 |
| **CTA (Secondary)** | - | 전체 Export | 리포트 Export | - |
| **필터** | 기간만 | 전체 (reportType/기간/단계/파트/스프린트) | 전체 | reportType 고정 (sprint_burndown) |
| **스케줄링** | 조회만 | 생성/편집/중지 가능 | 생성/편집 가능 | 불가 |
| **Export 형식** | PDF만 | PDF/Excel/PPT | PDF/Excel/PPT | PDF만 |
| **/stats 접근** | 가능 (요약) | 가능 (전체) | 제한적 | 불가 |

---

## 5. 리포트 카탈로그 상세

### 5.1 리포트 유형 정의

```typescript
interface ReportTypeDefinition {
  type: ReportType;
  label: string;
  description: string;
  icon: string;
  category: "status" | "execution" | "quality" | "resource" | "governance";
  requiredCaps: Capability[];
  availableFormats: ("pdf" | "excel" | "pptx")[];
  schedulable: boolean;
  dataSources: string[];
  refreshInterval: string;
}
```

### 5.2 Project Status Report (프로젝트 현황 보고서)

```typescript
const projectStatusReport: ReportTypeDefinition = {
  type: "project_status",
  label: "프로젝트 현황 보고서",
  description: "프로젝트 전반의 진행 상황, 리스크, 이슈, 일정 편차를 종합한 표준 보고서",
  icon: "AssessmentRounded",
  category: "status",
  requiredCaps: ["view_reports"],
  availableFormats: ["pdf", "excel", "pptx"],
  schedulable: true,
  dataSources: ["phases", "risks", "issues", "deliverables", "budget", "sprints"],
  refreshInterval: "daily",
};
```

**구성 섹션**:

| 섹션 | 내용 | 데이터 소스 |
|------|------|-----------|
| 요약 | 프로젝트 기본 정보, 전체 진행률, Health Score | project, healthScore |
| 단계별 현황 | Phase별 계획/실적/편차/원인/조치 | phases[] |
| 리스크 현황 | 등급별 리스크 건수, Top-5 리스크 상세 | risks[] |
| 이슈 현황 | Open/Resolved/Blocker 건수, 해결율 | issues[] |
| 산출물 현황 | 제출/승인/반려 건수, 승인율 | deliverables[] |
| 예산 현황 | 계획 예산 vs 집행 예산, 소진율 | budget |
| 일정 요약 | 전체 일정 대비 진행률, 마일스톤 현황 | milestones[] |
| AI 인사이트 | AI 기반 진단 + 리스크 예측 | ai_insight |

---

### 5.3 Sprint Burndown (스프린트 번다운)

```typescript
const sprintBurndownReport: ReportTypeDefinition = {
  type: "sprint_burndown",
  label: "스프린트 번다운",
  description: "스프린트 기간 중 Ideal vs Actual 번다운 차트 + 완료/잔여 SP 분석",
  icon: "TrendingDownRounded",
  category: "execution",
  requiredCaps: ["view_reports"],
  availableFormats: ["pdf", "excel"],
  schedulable: false,
  dataSources: ["sprints", "tasks", "stories"],
  refreshInterval: "realtime",
};
```

**차트 구성**:

```
Burndown Chart:
  Y축: Story Points (잔여)
  X축: Sprint 일차 (Day 1 ~ Day N)
  선: Ideal (직선), Actual (꺾은선)
  영역: 편차 하이라이트 (빨강=지연, 초록=선행)

Summary Table:
  | 항목 | 값 |
  | 전체 SP | 48 |
  | 완료 SP | 32 |
  | 잔여 SP | 16 |
  | 이상적 잔여 | 12 |
  | 편차 | +4 SP (지연) |
  | 일 평균 소화율 | 3.2 SP/일 |
  | 예상 완료일 | Sprint 종료 +2일 |
```

---

### 5.4 Velocity Trend (속도 추이)

```typescript
const velocityTrendReport: ReportTypeDefinition = {
  type: "velocity_trend",
  label: "속도 추이",
  description: "스프린트별 완료 Story Point 추이 + 이동 평균선",
  icon: "SpeedRounded",
  category: "execution",
  requiredCaps: ["view_reports"],
  availableFormats: ["pdf", "excel"],
  schedulable: false,
  dataSources: ["sprints", "stories"],
  refreshInterval: "per_sprint",
};
```

**차트 구성**:

```
Velocity Chart:
  Y축: Story Points (완료)
  X축: Sprint (Sprint-1 ~ Sprint-N)
  바: 스프린트별 완료 SP
  선: 3-Sprint 이동 평균선
  기준선: 목표 Velocity (점선)
```

---

### 5.5 EVM / SPI / CPI (Earned Value Management)

```typescript
const evmReport: ReportTypeDefinition = {
  type: "evm_spi_cpi",
  label: "EVM (SPI/CPI) 리포트",
  description: "Earned Value Management 기반 SPI(일정성과지수), CPI(비용성과지수), EAC(완료시점 예상비용) 분석",
  icon: "AccountBalanceRounded",
  category: "governance",
  requiredCaps: ["view_reports"],
  availableFormats: ["pdf", "excel", "pptx"],
  schedulable: true,
  dataSources: ["phases", "budget", "tasks", "milestones"],
  refreshInterval: "weekly",
};
```

**핵심 지표**:

| 지표 | 계산식 | 의미 | 정상 범위 |
|------|--------|------|----------|
| PV (Planned Value) | 계획 시점까지의 예정 가치 | 예정 작업량 | - |
| EV (Earned Value) | 실제 완료된 작업의 계획 가치 | 실제 성과 | - |
| AC (Actual Cost) | 실제 투입 비용 | 실제 비용 | - |
| SPI (Schedule Performance Index) | EV / PV | 일정 효율성 | 0.9 ~ 1.1 |
| CPI (Cost Performance Index) | EV / AC | 비용 효율성 | 0.9 ~ 1.1 |
| SV (Schedule Variance) | EV - PV | 일정 편차 | >= 0 |
| CV (Cost Variance) | EV - AC | 비용 편차 | >= 0 |
| EAC (Estimate at Completion) | BAC / CPI | 완료시점 예상비용 | <= BAC |
| ETC (Estimate to Complete) | EAC - AC | 잔여 예상비용 | - |

**차트 구성**:

```
EVM S-Curve:
  Y축: Value (원)
  X축: 기간 (월별)
  선: PV (파란), EV (초록), AC (빨강)
  영역: SV 편차(EV-PV), CV 편차(EV-AC)

SPI/CPI Gauge:
  ┌───────────┐  ┌───────────┐
  │   SPI     │  │   CPI     │
  │   0.92    │  │   0.87    │
  │   🟡 주의  │  │   🟡 주의  │
  └───────────┘  └───────────┘
```

---

### 5.6 Quality Dashboard (품질 대시보드)

```typescript
const qualityDashboardReport: ReportTypeDefinition = {
  type: "quality_dashboard",
  label: "품질 대시보드",
  description: "결함 밀도, 테스트 커버리지, 결함 추이, 테스트 실행률 등 품질 지표 종합",
  icon: "VerifiedRounded",
  category: "quality",
  requiredCaps: ["view_reports"],
  availableFormats: ["pdf", "excel"],
  schedulable: true,
  dataSources: ["issues", "test_cases", "test_runs", "deliverables"],
  refreshInterval: "daily",
};
```

**핵심 지표**:

| 지표 | 계산식 | 설명 |
|------|--------|------|
| Defect Density | 결함 수 / KLOC | 코드 1,000줄당 결함 수 |
| Test Coverage | 테스트된 요구사항 / 전체 요구사항 | 요구사항 기반 테스트 커버리지 |
| Defect Resolution Rate | 해결 결함 / 전체 결함 | 결함 해결율 |
| Test Execution Rate | 실행된 TC / 전체 TC | 테스트 케이스 실행률 |
| Test Pass Rate | 통과 TC / 실행된 TC | 테스트 통과율 |
| Defect Leakage Rate | 운영 결함 / 전체 결함 | 테스트 누락 결함 비율 |

---

### 5.7 Resource Utilization (리소스 활용률)

```typescript
const resourceUtilizationReport: ReportTypeDefinition = {
  type: "resource_utilization",
  label: "리소스 활용률",
  description: "인력별/파트별 리소스 배분, 활용률, 가용 리소스 현황 분석",
  icon: "PeopleRounded",
  category: "resource",
  requiredCaps: ["view_reports"],
  availableFormats: ["pdf", "excel"],
  schedulable: true,
  dataSources: ["tasks", "users", "parts", "sprints"],
  refreshInterval: "daily",
};
```

**차트 구성**:

```
Resource Heatmap (파트별 x 주차별):
         Week1  Week2  Week3  Week4
  BE      🟢    🟡     🔴     🟡
  FE      🟡    🟢     🟢     🟡
  QA      🟢    🟢     🟡     🔴
  Infra   🔴    🔴     🟡     🟡

Utilization Bar (인력별):
  홍길동   ████████░░  82%
  김영희   ██████████  100% ⚠ (과부하)
  박철수   ████░░░░░░  45%  ℹ (유휴)
```

---

### 5.8 Risk Heatmap (리스크 히트맵)

```typescript
const riskHeatmapReport: ReportTypeDefinition = {
  type: "risk_heatmap",
  label: "리스크 히트맵",
  description: "리스크를 발생 확률 x 영향도 매트릭스로 시각화, 상위 리스크 관리 현황",
  icon: "WarningAmberRounded",
  category: "governance",
  requiredCaps: ["view_reports"],
  availableFormats: ["pdf", "excel", "pptx"],
  schedulable: true,
  dataSources: ["risks", "issues", "phases"],
  refreshInterval: "weekly",
};
```

**차트 구성**:

```
Risk Heatmap Matrix (발생확률 x 영향도):

  영향도 ↑
  High   │  🟡 2건  │  🟠 1건  │  🔴 1건  │
  Medium │  🟢 3건  │  🟡 2건  │  🟠 1건  │
  Low    │  🟢 5건  │  🟢 2건  │  🟡 1건  │
         └──────────┴──────────┴──────────┘
           Low        Medium      High     → 발생확률
```

---

### 5.9 Phase Gate Status (Phase Gate 상태)

```typescript
const phaseGateStatusReport: ReportTypeDefinition = {
  type: "phase_gate_status",
  label: "Phase Gate 상태 보고서",
  description: "각 Phase Gate의 통과 조건 충족 여부, 승인 상태, Gate Review 결과 종합",
  icon: "FactCheckRounded",
  category: "governance",
  requiredCaps: ["view_reports"],
  availableFormats: ["pdf", "excel", "pptx"],
  schedulable: false,
  dataSources: ["phases", "deliverables", "decisions", "milestones"],
  refreshInterval: "on_demand",
};
```

**구성 요소**:

```
Phase Gate Timeline:
  ── Gate1 ✅ ── Gate2 🔄 ── Gate3 ⏳ ── Gate4 ⏳ ──
     통과       심사중      대기        대기

Gate2 상세:
  | 통과 조건 | 충족 | 비고 |
  | 설계 산출물 승인 | ✅ | 2026-02-03 |
  | 테스트 커버리지 80% | ⚠ 75% | 5% 미달 |
  | 리스크 대응 계획 | ✅ | 승인 완료 |
  | PMO 승인 | ⏳ | 미결정 |
```

---

### 5.10 Requirement Traceability Matrix (요구사항 추적 매트릭스)

```typescript
const requirementTraceabilityReport: ReportTypeDefinition = {
  type: "requirement_traceability",
  label: "요구사항 추적 매트릭스",
  description: "요구사항 → 설계 → 코드(Epic/Story) → 테스트 케이스 간 추적성 매트릭스",
  icon: "AccountTreeRounded",
  category: "governance",
  requiredCaps: ["view_reports"],
  availableFormats: ["pdf", "excel"],
  schedulable: false,
  dataSources: ["requirements", "epics", "stories", "test_cases", "deliverables"],
  refreshInterval: "on_demand",
};
```

**매트릭스 구성**:

```
요구사항 추적 매트릭스:

  요구사항 ID | 요구사항명          | Epic    | Story   | TC     | 상태
  ──────────────────────────────────────────────────────────────────
  REQ-001    | 실시간 사기탐지     | E-01   | S-01~05 | TC-001~010 | ✅ 완전
  REQ-002    | 스코어링 API       | E-02   | S-06~08 | TC-011~018 | ✅ 완전
  REQ-003    | 대시보드 구현       | E-03   | S-09~12 | TC-019~025 | ⚠ TC 부족
  REQ-004    | 배치 처리 시스템    | E-04   | S-13~15 | -          | ❌ TC 미연결
  REQ-005    | 감사 로그           | -      | -       | -          | ❌ 미구현

  커버리지 요약:
  | 항목 | 수치 |
  | 전체 요구사항 | 25건 |
  | Epic 연결 | 22건 (88%) |
  | Story 연결 | 20건 (80%) |
  | TC 연결 | 18건 (72%) |
  | 완전 추적 | 15건 (60%) |
```

---

## 6. 통계 화면 상세 (/stats)

### 6.1 목적

통계 대시보드(`/stats`)는 PMO/Sponsor가 **여러 프로젝트를 횡단적으로 비교**하여
포트폴리오 수준의 의사결정을 지원하는 화면이다.
단일 프로젝트의 `/reports`와 달리, `/stats`는 크로스 프로젝트 데이터를 집계한다.

### 6.2 접근 권한

| Preset | /stats 접근 | 기능 범위 |
|--------|-----------|----------|
| EXEC_SUMMARY (Sponsor) | 가능 | 포트폴리오 KPI 요약, 건강도 매트릭스 (읽기 전용) |
| PMO_CONTROL (PMO) | 가능 | 전체 기능 (비교, 상세 분석, Export) |
| PM_WORK (PM) | 제한적 | 자기 프로젝트만 포함된 비교 뷰 |
| DEV_EXECUTION (DEV) | 불가 | 메뉴 미노출 |
| CUSTOMER_APPROVAL | 제한적 | 담당 프로젝트만 포함된 요약 뷰 |

### 6.3 포트폴리오 KPI 구성

```typescript
interface PortfolioKpis {
  projectCount: number;
  avgHealthScore: number;
  avgHealthGrade: string;
  avgSpi: number;
  avgCpi: number;
  resourceAllocationRate: number;
  totalBudgetBurnRate: number;
  riskExposure: {
    high: number;
    medium: number;
    low: number;
  };
  projectsAtRisk: number;        // Health D 이하 프로젝트 수
}
```

### 6.4 프로젝트 건강도 매트릭스

프로젝트를 행, 지표를 열로 배치한 매트릭스 형태:

```
┌───────────────────────────────────────────────────────────┐
│  프로젝트 건강도 매트릭스                                   │
│                                                           │
│  프로젝트명    | Health | SPI  | CPI  | 진행률 | 리스크 | 리소스 │
│  ─────────────────────────────────────────────────────── │
│  AI보험심사    | 🟡 B+  | 0.92 | 0.87 | 72%   | 3 H   | 82%   │
│  차세대Core    | 🟢 A   | 1.05 | 1.02 | 85%   | 1 M   | 75%   │
│  디지털채널    | 🔴 D   | 0.65 | 0.58 | 45%   | 5 H   | 95%   │
│                                                           │
│  ▸ 행 클릭 → 우측 Panel 상세 분석 표시                    │
│  ▸ 열 헤더 클릭 → 해당 지표 기준 정렬                     │
│  ▸ 상태 색상 셀 클릭 → 해당 프로젝트 리포트로 딥링크       │
└───────────────────────────────────────────────────────────┘
```

### 6.5 리소스 배분 분석

```
┌───────────────────────────────────────────────────────────┐
│  리소스 배분 현황                                          │
│                                                           │
│  파트별 배분:                                              │
│  ┌──────────────────────────────────────────────────┐     │
│  │  BE   │  전체 15명 / 배정 12명 / 가용 3명         │     │
│  │       │  ████████████░░░  80%                    │     │
│  │  FE   │  전체 10명 / 배정 7명 / 가용 3명          │     │
│  │       │  ██████████░░░░░  70%                    │     │
│  │  QA   │  전체 8명 / 배정 5명 / 가용 3명           │     │
│  │       │  █████████░░░░░░  63%                    │     │
│  │  PM   │  전체 5명 / 배정 5명 / 가용 0명           │     │
│  │       │  ██████████████  100% ⚠                  │     │
│  └──────────────────────────────────────────────────┘     │
│                                                           │
│  프로젝트별 배분:                                          │
│  AI보험심사:  BE 5 / FE 3 / QA 2 / PM 2  = 12명           │
│  차세대Core:  BE 4 / FE 2 / QA 2 / PM 1  = 9명            │
│  디지털채널:  BE 3 / FE 2 / QA 1 / PM 2  = 8명            │
└───────────────────────────────────────────────────────────┘
```

### 6.6 트렌드 차트

| 차트 | X축 | Y축 | 데이터 |
|------|-----|-----|--------|
| Health Score Trend | 주차 | 점수 (0~100) | 프로젝트별 Health 추이선 |
| SPI/CPI Trend | 월 | 지수 (0~2) | 프로젝트별 SPI/CPI 추이선 |
| Velocity Comparison | Sprint | SP | 프로젝트별 Velocity 바 그래프 |
| Risk Exposure Trend | 주차 | 건수 | 프로젝트별 리스크 노출 추이 |

---

## 7. 데이터 구조 / API

### 7.1 리포트 엔티티

```typescript
/** v2.1: 리포트 생성 단계 (stage 기반 비동기 상태) */
type ReportGenerationStage =
  | "queued"        // 대기열 진입
  | "collecting"    // 데이터 수집 중
  | "aggregating"   // 집계/계산 중
  | "rendering"     // 차트/문서 렌더링 중
  | "completed"     // 완료
  | "failed";       // 실패

/** 리포트 인스턴스 (생성된 리포트) */
interface ReportInstance {
  id: string;
  projectId: string;
  type: ReportType;
  title: string;
  status: "generating" | "completed" | "failed";
  /** v2.1: 세분화된 생성 단계 — UI에서 "데이터 수집 중..." 등 설명 가능 */
  stage: ReportGenerationStage;
  generatedAt: string;          // ISO datetime
  generatedBy: string;          // userId
  parameters: {
    dateRange?: { from: string; to: string };
    phaseId?: string;
    partId?: string;
    sprintId?: string;
  };
  /** v2.1: 유형별 타입 유니온 (Record<string, unknown> → zod 검증 가능) */
  data: ReportData;
  exportHistory: ExportRecord[];
  scheduledId?: string;           // 예약 생성인 경우
}

/**
 * v2.1: 리포트 유형별 데이터 타입 유니온 — 프론트/백 모두 JSON schema(또는 zod)로 검증
 * ReportChartRenderer의 방어코드를 줄이고 런타임 오류를 방지한다.
 */
type ReportData =
  | { _type: "project_status"; summary: ProjectStatusData }
  | { _type: "sprint_burndown"; burndown: SprintBurndownData }
  | { _type: "velocity_trend"; velocity: VelocityTrendData }
  | { _type: "evm_spi_cpi"; evm: EvmData }
  | { _type: "quality_dashboard"; quality: QualityData }
  | { _type: "resource_utilization"; resource: ResourceData }
  | { _type: "risk_heatmap"; risk: RiskHeatmapData }
  | { _type: "phase_gate_status"; phaseGate: PhaseGateData }
  | { _type: "requirement_traceability"; traceability: TraceabilityData };

/** Export 기록 */
interface ExportRecord {
  id: string;
  format: "pdf" | "excel" | "pptx";
  exportedAt: string;
  exportedBy: string;
  /** v2.1: 서명 URL — 짧은 TTL(10~30분) + 다운로드 시점 권한 재검증 */
  downloadUrl: string;            // signed URL (HMAC + expiry)
  downloadUrlExpiresAt: string;   // v2.1: 서명 URL 만료 시간 (ISO datetime)
  fileSize: number;               // bytes
  expiresAt: string;              // 파일 자체 만료 시간 (보관 정책)
}

/**
 * v2.1 서명 URL 보안 규칙:
 *   1. downloadUrl은 반드시 HMAC 서명 + 만료 시간 포함 (TTL: 10~30분)
 *   2. 다운로드 요청 시 서버가 "현재 사용자 권한 + 프로젝트 스코프 + reportType 정책" 재검증
 *   3. 외부 공유 시에도 서명 만료로 무단 접근 차단
 *   4. 만료 후에는 /api/reports/exports/:exportId/renew API로 새 서명 URL 발급
 *   5. renew 시에도 ReportType ACL 정책 재검증 (권한 변경 반영)
 */

/** 예약 리포트 설정 */
interface ScheduledReport {
  id: string;
  projectId: string;
  type: ReportType;
  schedule: {
    frequency: "daily" | "weekly" | "monthly";
    dayOfWeek?: number;           // 0=Sun, 1=Mon, ... (weekly)
    dayOfMonth?: number;          // 1~28 (monthly)
    time: string;                 // "09:00"
    timezone: string;             // "Asia/Seoul"
  };
  format: "pdf" | "excel" | "pptx";
  recipients: {
    userId: string;
    name: string;
    email: string;
  }[];
  parameters: {
    dateRange?: { from: string; to: string };
    phaseId?: string;
    partId?: string;
  };
  status: "active" | "paused" | "deleted";
  lastRunAt?: string;
  nextRunAt?: string;
  createdBy: string;
  createdAt: string;
}

/** 리포트 카탈로그 아이템 (화면 표시용) */
interface ReportCatalogItem {
  type: ReportType;
  label: string;
  description: string;
  icon: string;
  category: string;
  lastGenerated?: {
    id: string;
    generatedAt: string;
    status: string;
  };
  scheduledReport?: {
    id: string;
    frequency: string;
    nextRunAt: string;
    status: string;
  };
  quickMetrics?: Record<string, number | string>;  // 카탈로그 카드에 표시할 핵심 지표
  availableFormats: string[];
  canGenerate: boolean;           // 현재 사용자의 generate_reports 권한 여부
  canExport: boolean;             // 현재 사용자의 export_reports 권한 여부
  canSchedule: boolean;           // 현재 사용자의 스케줄 설정 권한 여부
}
```

### 7.2 REST API 계약

#### 7.2.1 리포트 카탈로그 조회

```
GET /api/reports/catalog?projectId={projectId}
```

```typescript
interface ReportCatalogResponse {
  items: ReportCatalogItem[];
  stats: {
    totalTypes: number;
    generatedToday: number;
    scheduledActive: number;
    exportPending: number;
  };
  asOf: string;
}
```

#### 7.2.2 리포트 생성

```
POST /api/reports/generate
```

```typescript
interface GenerateReportRequest {
  projectId: string;
  type: ReportType;
  parameters?: {
    dateRange?: { from: string; to: string };
    phaseId?: string;
    partId?: string;
    sprintId?: string;
  };
}

interface GenerateReportResponse {
  reportId: string;
  status: "generating";
  /** v2.1: 세분화된 생성 단계 */
  stage: ReportGenerationStage;
  estimatedCompletionMs: number;
  /** v2.1: 기존 생성 건 재사용 시 true (idempotent 히트) */
  cached: boolean;
}

/**
 * v2.1 Idempotency 정책:
 *   - 동일 (projectId + type + parameters) 조합에 대해 10분 캐시
 *   - 데이터 갱신 후 새 리포트가 필요하면:
 *     (a) asOf bucket(floor(now / 15min)) 기반 자동 만료, 또는
 *     (b) forceRegenerate: true 헤더 (admin_reports cap 필요)
 *   - forceRegenerate는 PMO만 사용 가능 (남용 방지)
 */
```

#### 7.2.3 리포트 조회

```
GET /api/reports/:reportId
```

```typescript
interface ReportDetailResponse {
  report: ReportInstance;
  completeness: {
    phases: CompletenessStatus;
    risks: CompletenessStatus;
    issues: CompletenessStatus;
    budget: CompletenessStatus;
    sprints: CompletenessStatus;
    tests: CompletenessStatus;
  };
  warnings: {
    code: string;
    message: string;
    severity: "info" | "warning" | "error";
  }[];
  /** v2.1: 운영/디버깅 수준 provenance — "왜 수치가 이상하지?" 즉시 추적 가능 */
  provenance: {
    [source: string]: DataProvenanceV2;
  };
}

/** v2.1: 확장 Provenance — 소스 표시를 넘어 디버깅/감사 추적 수준 */
interface DataProvenanceV2 {
  sourceName: string;             // "phases" | "issues" | "neo4j" | ...
  asOf: string;                   // ISO datetime — 해당 소스의 데이터 기준 시점
  traceId: string;                // 요청 추적 ID (distributed tracing)
  rowCount: number;               // 소스에서 가져온 행 수
  cacheHit: boolean;              // 캐시 적중 여부
  latencyMs: number;              // 소스 조회 소요 시간 (ms)
  policyDecision: "allowed" | "filtered" | "masked";  // 권한 정책 적용 결과
  warnings?: string[];            // 소스별 경고 (truncated, timeout 등)
}
```

#### 7.2.4 리포트 Export

```
POST /api/reports/:reportId/export
```

```typescript
interface ExportReportRequest {
  format: "pdf" | "excel" | "pptx";
  includeCharts: boolean;
  includeRawData: boolean;       // Excel에서만 유효
  pageSize?: "A4" | "Letter";    // PDF에서만 유효
}

interface ExportReportResponse {
  exportId: string;
  status: "processing" | "completed";
  downloadUrl?: string;          // completed 시 반환 (v2.1: signed URL)
  /** v2.1: 폴링 URL — 클라이언트가 이 URL로 상태 확인 */
  statusUrl: string;             // e.g. "/api/reports/exports/{exportId}"
  estimatedCompletionMs?: number;
}
```

#### 7.2.5 리포트 Export 상태 확인 (폴링)

```
GET /api/reports/exports/:exportId
```

```typescript
interface ExportStatusResponse {
  exportId: string;
  status: "processing" | "completed" | "failed";
  progress: number;              // 0~100
  /** v2.1: 서버 권장 폴링 간격 (ms) — 클라이언트 백오프에 사용 */
  retryAfterMs: number;          // progress < 80 → 2000, >= 80 → 5000
  downloadUrl?: string;          // v2.1: signed URL (completed 시)
  fileSize?: number;
  expiresAt?: string;
  error?: string;
}

/**
 * v2.1 Export 폴링 백오프 정책:
 *   1차: retryAfterMs를 서버가 응답에 포함 (progress < 80 → 2초, >= 80 → 5초)
 *   2차(추후): SSE 또는 WebSocket으로 export 상태 push 전환
 *   클라이언트 규칙:
 *     - retryAfterMs가 있으면 해당 값 사용
 *     - 없으면 기본 2초 + progress >= 80이면 5초로 백오프
 *     - 최대 폴링 시간 5분 → 초과 시 "시간 초과" 안내 + 수동 확인 링크
 */
```

#### 7.2.6 Export 서명 URL 재발급 (v2.1)

```
POST /api/reports/exports/:exportId/renew
```

```typescript
interface RenewDownloadUrlResponse {
  exportId: string;
  downloadUrl: string;            // 새 signed URL
  downloadUrlExpiresAt: string;   // 새 만료 시간
}
```

> **검증**: renew 요청 시에도 현재 사용자 권한 + ReportType ACL 정책을 재검증한다.
> 권한이 변경되었으면 403 반환.

#### 7.2.7 예약 리포트 관리

```
GET    /api/reports/schedules?projectId={projectId}
POST   /api/reports/schedules
PUT    /api/reports/schedules/:scheduleId
DELETE /api/reports/schedules/:scheduleId
```

```typescript
interface CreateScheduleRequest {
  projectId: string;
  type: ReportType;
  schedule: {
    frequency: "daily" | "weekly" | "monthly";
    dayOfWeek?: number;
    dayOfMonth?: number;
    time: string;
    timezone: string;
  };
  format: "pdf" | "excel" | "pptx";
  recipientUserIds: string[];
  parameters?: {
    dateRange?: { from: string; to: string };
    phaseId?: string;
    partId?: string;
  };
}

interface ScheduleListResponse {
  schedules: ScheduledReport[];
  stats: {
    active: number;
    paused: number;
    total: number;
  };
}
```

#### 7.2.8 리포트 이력 조회

```
GET /api/reports/history?projectId={projectId}&type={reportType}&limit={limit}&offset={offset}
```

```typescript
interface ReportHistoryResponse {
  items: {
    id: string;
    type: ReportType;
    title: string;
    generatedAt: string;
    generatedBy: string;
    status: string;
    exportCount: number;
  }[];
  total: number;
  limit: number;
  offset: number;
}
```

### 7.3 통계 API

> **v2.1 Scope 교집합 규칙**: 모든 `/api/stats/*` 요청에서 `projectIds`를 그대로 신뢰하지 않는다.
> 서버는 **사용자가 접근 가능한 프로젝트 집합과 교집합**을 만들어 처리한다.

```typescript
/**
 * v2.1 /stats Scope 교집합 처리:
 *   1. 요청의 projectIds와 사용자 접근 가능 프로젝트 교집합 계산
 *   2. 교집합 결과가 빈 경우 → 403이 아닌 빈 데이터 + warnings + nextStep
 *      (외부 감사/고객의 경우 403은 "왜 안 되지?" UX를 유발)
 *   3. 응답에 accessibleProjectIds를 포함하여 UI가 어떤 프로젝트가 포함되었는지 표시
 *   4. 제외된 프로젝트는 warnings에 "접근 불가 프로젝트 N건 제외" 포함
 */
```

#### 7.3.1 포트폴리오 KPI

```
GET /api/stats/portfolio
```

```typescript
interface PortfolioStatsResponse {
  kpis: PortfolioKpis;
  projects: {
    id: string;
    name: string;
    healthScore: number;
    healthGrade: string;
    spi: number;
    cpi: number;
    progress: number;
    riskCount: { high: number; medium: number; low: number };
    resourceUtilization: number;
    budgetBurnRate: number;
    /** v2.1: 프로젝트별 데이터 기준 시점 — asOf가 24시간 이상 오래된 경우 배지 표시 */
    projectAsOf: string;
  }[];
  /** v2.1: 응답에 포함된 프로젝트 ID 목록 (교집합 결과) */
  accessibleProjectIds: string[];
  asOf: string;
  completeness: Record<string, CompletenessStatus>;
  warnings: { code: string; message: string; severity: string }[];
}
```

#### 7.3.2 프로젝트 비교 데이터

```
GET /api/stats/comparison?projectIds={id1,id2,id3}&metric={metricKey}&period={period}
```

```typescript
interface ComparisonResponse {
  metric: string;
  period: string;
  dataPoints: {
    projectId: string;
    projectName: string;
    values: {
      date: string;
      value: number;
    }[];
  }[];
  asOf: string;
}
```

#### 7.3.3 리소스 배분 현황

```
GET /api/stats/resources?projectIds={id1,id2,id3}
```

```typescript
interface ResourceStatsResponse {
  byPart: {
    partName: string;
    total: number;
    assigned: number;
    available: number;
    utilizationRate: number;
  }[];
  byProject: {
    projectId: string;
    projectName: string;
    members: {
      partName: string;
      count: number;
    }[];
    totalMembers: number;
  }[];
  asOf: string;
}
```

#### 7.3.4 지표 카탈로그 (v2.1)

> **v2.1 추가**: metricKey를 문자열로 열어두되, UI에서 선택 가능한 목록은 서버가 내려준다.
> 통계 지표가 추가될 때 프론트 배포 없이 서버 설정만으로 반영 가능.

```
GET /api/stats/metrics/catalog
```

```typescript
interface MetricCatalogResponse {
  metrics: MetricDefinition[];
}

interface MetricDefinition {
  key: string;                    // e.g. "spi", "cpi", "health_score", "velocity"
  label: string;                  // "일정성과지수 (SPI)"
  description: string;
  unit: string;                   // "%", "index", "SP", "건"
  allowedAggregations: ("daily" | "weekly" | "monthly" | "quarterly")[];
  warningThreshold?: number;      // 이 값 이하면 경고 배지
  dangerThreshold?: number;       // 이 값 이하면 위험 배지
}
```

---

## 8. 컴포넌트 분해

### 8.1 폴더 구조

```
features/
  reports/
    pages/
      ReportsPage.tsx              ← 리포트 카탈로그 (ViewMode 분기)
      StatsPage.tsx                ← 크로스 프로젝트 통계 (PMO/Sponsor)
    layout/
      ReportShell.tsx              ← 공통 레이아웃 (KPI + Catalog + RightPanel)
      ReportKpiSlot.tsx            ← KPI Row 슬롯 렌더러
      ReportCatalogSlot.tsx        ← 카탈로그 영역 슬롯 렌더러
      ReportRightPanelSlot.tsx     ← Right Panel 슬롯 렌더러
    components/
      // KPI Cards
      ReportKpiRow.tsx             ← 상단 KPI 카드 Row
      SpiCpiCard.tsx               ← SPI/CPI 게이지 카드
      ScheduledReportCard.tsx      ← 예약 리포트 건수 카드

      // Catalog
      ReportCatalog.tsx            ← 리포트 카탈로그 그리드/리스트
      ReportCatalogCard.tsx        ← 개별 리포트 카드 (유형/상태/CTA)
      ReportCatalogTable.tsx       ← PMO용 테이블 형태 카탈로그
      ReportStatusBadge.tsx        ← 리포트 상태 배지 (생성됨/생성중/미생성/실패)
      ReportCategoryFilter.tsx     ← 카테고리 필터 탭 (전체/실행/품질/거버넌스)

      // Preview
      ReportPreviewPanel.tsx       ← 리포트 미리보기 (Right Panel)
      ReportChartRenderer.tsx      ← 차트 렌더러 (리포트 유형별 차트 분기)
      BurndownChart.tsx            ← 번다운 차트 (Sprint Burndown용)
      VelocityChart.tsx            ← Velocity Trend 차트
      EvmSCurve.tsx                ← EVM S-Curve 차트
      SpiCpiGauge.tsx              ← SPI/CPI 게이지 위젯
      QualityMetricsChart.tsx      ← 품질 지표 차트
      ResourceHeatmap.tsx          ← 리소스 히트맵
      RiskHeatmapMatrix.tsx        ← 리스크 히트맵 매트릭스
      PhaseGateTimeline.tsx        ← Phase Gate 타임라인
      TraceabilityMatrix.tsx       ← 요구사항 추적 매트릭스 테이블

      // Export
      ExportDialog.tsx             ← Export 형식 선택 + 옵션 다이얼로그
      ExportProgressBar.tsx        ← Export 진행률 표시
      ExportHistoryList.tsx        ← Export 이력 목록

      // Schedule
      SchedulePanel.tsx            ← 스케줄 관리 패널 (Right Panel)
      ScheduleForm.tsx             ← 스케줄 생성/편집 폼
      ScheduleListWidget.tsx       ← 예약 리포트 목록 위젯
      RecipientSelector.tsx        ← 수신자 선택 컴포넌트

      // Generation
      ReportGenerateDialog.tsx     ← 리포트 생성 다이얼로그 (필터/파라미터 설정)
      ReportGeneratingStatus.tsx   ← 리포트 생성 중 상태 표시
      ReportFilterBar.tsx          ← 필터 바 (기간/단계/파트/스프린트)

      // Stats Page
      PortfolioKpiRow.tsx          ← 포트폴리오 KPI 카드 Row
      ProjectHealthMatrix.tsx      ← 프로젝트 건강도 매트릭스 테이블
      ProgressComparisonChart.tsx  ← 진행률 비교 차트
      ResourceAllocationChart.tsx  ← 리소스 배분 차트
      TrendChart.tsx               ← 범용 트렌드 차트 (Health/SPI/CPI/Risk)
      StatsDetailPanel.tsx         ← 통계 상세 분석 패널 (Right Panel)
      ProjectSelector.tsx          ← 비교 프로젝트 선택 컴포넌트

      // Common
      DataIncompleteBadge.tsx      ← completeness !== "ok" 시 배지
      ReportEmptyState.tsx         ← 리포트 없음 상태

    config/
      reportPresetLayouts.ts       ← ReportPresetLayout 정의
      reportCatalogConfig.ts       ← 9종 리포트 유형 설정
      statsPresetLayouts.ts        ← StatsPresetLayout 정의
      reportTypePolicyConfig.ts    ← v2.1: ReportType ACL 정책 (서버 응답 캐시)
    api/
      reportsApi.ts                ← 리포트 API 호출
      statsApi.ts                  ← 통계 API 호출
      metricsCatalogApi.ts         ← v2.1: 지표 카탈로그 API 호출
    hooks/
      useReportCatalog.ts          ← 카탈로그 TanStack Query
      useReportGenerate.ts         ← 리포트 생성 mutation
      useReportExport.ts           ← Export mutation + 폴링 (v2.1: 백오프 적용)
      useReportSchedules.ts        ← 예약 리포트 CRUD Query
      useReportHistory.ts          ← 이력 조회 Query
      useReportPreview.ts          ← 미리보기 데이터 Query
      usePortfolioStats.ts         ← 포트폴리오 KPI Query
      useProjectComparison.ts      ← 프로젝트 비교 Query
      useResourceStats.ts          ← 리소스 통계 Query
      useReportPanelMode.ts        ← Right Panel 모드 상태
      useReportActionPolicy.ts     ← v2.1: 리포트 행위 정책 훅 (아래 참조)
      useMetricsCatalog.ts         ← v2.1: 지표 카탈로그 TanStack Query
```

### 8.1.1 useReportActionPolicy 훅 (v2.1)

> **v2.1 추가**: ReportCatalogCard, ReportCatalogTable, ExportDialog 등에서 버튼 표시/비활성화를
> 개별적으로 판단하면 권한 버그가 흩어진다. **단일 정책 훅**으로 일관성을 보장한다.

```typescript
/** 리포트 행위 정책 — 모든 UI 버튼은 이 결과만 보고 렌더/disable 처리 */
function useReportActionPolicy(reportType: ReportType): ReportActionPolicyResult {
  const { capabilities, role, preset } = useAccessContext();
  const { data: typePolicy } = useReportTypePolicy(reportType);  // 서버 ACL 캐시

  return useMemo(() => ({
    canView:     typePolicy?.canView     ?? false,
    canGenerate: typePolicy?.canGenerate ?? false,
    canExport:   typePolicy?.canExport   ?? false,
    canSchedule: typePolicy?.canSchedule ?? false,
    /** 비활성 사유 — UI에서 tooltip으로 표시 */
    disabledReasons: {
      generate: !typePolicy?.canGenerate ? "generate_reports 권한 필요" : undefined,
      export:   !typePolicy?.canExport   ? "export_reports 권한 필요"   : undefined,
      schedule: !typePolicy?.canSchedule ? "admin_reports 권한 필요"    : undefined,
    },
  }), [capabilities, role, typePolicy]);
}
```

### 8.1.2 RecipientSelector 제약 규칙 (v2.1)

> **v2.1 추가**: 수신자 선택 시 "아무나 고르기"가 아니라 정책 기반으로 제한한다.

| 규칙 | 설명 |
|------|------|
| **프로젝트 스코프** | 해당 프로젝트의 PartMember만 수신자 목록에 노출 |
| **reportType 접근 배지** | 수신자가 해당 reportType을 view 가능한지 즉시 표시 (배지: ✅/⚠) |
| **권한 불충족 기본 차단** | view_reports 불가 수신자는 기본적으로 선택 불가 (grey-out) |
| **관리자 override** | admin_reports 보유 시 강제 선택 가능 (경고 확인 후) |
| **실행 시점 재검증** | 스케줄 실행 시 수신자 권한을 다시 검증 (기존 v2.0 유지) |

### 8.2 ReportsPage.tsx ViewMode 분기

```typescript
function ReportsPage() {
  const { viewMode } = useAccessContext();
  const { panelMode, setPanelMode } = useReportPanelMode(viewMode);
  const { data: catalog } = useReportCatalog();

  // panelMode에 따른 Right Panel 컴포넌트 결정
  const rightPanelContent = useMemo(() => {
    switch (panelMode) {
      case "none":      return undefined;
      case "preview":   return <ReportPreviewPanel />;
      case "schedule":  return <SchedulePanel />;
      case "export":    return <ExportDialog />;
      case "filter":    return <ReportFilterBar />;
      default:          return undefined;
    }
  }, [panelMode]);

  // Preset에 따라 표시할 리포트 유형 필터
  const visibleReportTypes = useMemo(() => {
    switch (viewMode) {
      case "EXEC_SUMMARY":
        return ["project_status", "evm_spi_cpi", "phase_gate_status"];
      case "DEV_EXECUTION":
        return ["sprint_burndown", "velocity_trend"];
      default:
        return ALL_REPORT_TYPES;  // PM_WORK, PMO_CONTROL: 전체
    }
  }, [viewMode]);

  return (
    <PageLayout
      header={<ReportToolbar viewMode={viewMode} />}
      rightPanel={rightPanelContent}
      rightPanelDefault={
        viewMode === "PM_WORK" || viewMode === "PMO_CONTROL"
          ? "open"
          : "closed"
      }
    >
      <ReportKpiRow viewMode={viewMode} />
      <ReportFilterBar viewMode={viewMode} />

      {viewMode === "PMO_CONTROL" ? (
        <ReportCatalogTable
          items={catalog?.items.filter(i => visibleReportTypes.includes(i.type))}
          onRowClick={(item) => setPanelMode("preview")}
        />
      ) : viewMode === "EXEC_SUMMARY" ? (
        <ReportCatalog
          items={catalog?.items.filter(i => visibleReportTypes.includes(i.type))}
          variant="compact"
          onCardClick={(item) => setPanelMode("preview")}
        />
      ) : (
        <ReportCatalog
          items={catalog?.items.filter(i => visibleReportTypes.includes(i.type))}
          variant="detailed"
          onCardClick={(item) => setPanelMode("preview")}
        />
      )}
    </PageLayout>
  );
}
```

### 8.3 StatsPage.tsx 구조

```typescript
function StatsPage() {
  const { viewMode } = useAccessContext();
  const { data: portfolio } = usePortfolioStats();
  const [selectedProjectId, setSelectedProjectId] = useState<string | null>(null);

  // Sponsor는 compact, PMO는 standard
  const density = viewMode === "EXEC_SUMMARY" ? "compact" : "standard";

  return (
    <PageLayout
      header={<StatsToolbar viewMode={viewMode} />}
      rightPanel={
        selectedProjectId ? (
          <StatsDetailPanel projectId={selectedProjectId} />
        ) : undefined
      }
      rightPanelDefault={viewMode === "PMO_CONTROL" ? "open" : "closed"}
    >
      <PortfolioKpiRow data={portfolio?.kpis} density={density} />

      <ProjectHealthMatrix
        projects={portfolio?.projects}
        onRowClick={(projectId) => setSelectedProjectId(projectId)}
      />

      <ProgressComparisonChart projects={portfolio?.projects} />

      {viewMode === "PMO_CONTROL" && (
        <ResourceAllocationChart />
      )}
    </PageLayout>
  );
}
```

---

## 9. AI Navigation 연동

### 9.1 virtualNode 우선 반환 정책

> **v2.0 핵심**: AI는 가능한 경우 raw filter 대신 **virtualId를 우선 반환**한다.
> virtualId 기반 딥링크는 파라미터 오류 가능성이 낮고, 사용자에게 "의미있는 업무 개념"으로 안내된다.

```typescript
/** AI virtualNode 매칭 우선순위 */
const VIRTUAL_NODE_PRIORITY: { virtualId: string; matchFilter: Partial<ReportFilterSpec> }[] = [
  { virtualId: "reports.sprint-burndown",        matchFilter: { reportType: "sprint_burndown" } },
  { virtualId: "reports.evm",                    matchFilter: { reportType: "evm_spi_cpi" } },
  { virtualId: "reports.quality",                matchFilter: { reportType: "quality_dashboard" } },
  { virtualId: "reports.traceability",           matchFilter: { reportType: "requirement_traceability" } },
  // v2.1: 사용 빈도 높은 3종 virtualNode 승격
  { virtualId: "reports.risk-heatmap",           matchFilter: { reportType: "risk_heatmap" } },
  { virtualId: "reports.resource-utilization",   matchFilter: { reportType: "resource_utilization" } },
  { virtualId: "reports.phase-gate",             matchFilter: { reportType: "phase_gate_status" } },
];

function resolveReportsNavigation(filter: ReportFilterSpec): AiReportsNavigation {
  // 1. virtualNode 매칭 시도 (우선)
  for (const vn of VIRTUAL_NODE_PRIORITY) {
    if (isSubsetMatch(vn.matchFilter, filter)) {
      return { targetNodeId: "reports", virtualId: vn.virtualId, filter, reason: "..." };
    }
  }
  // 2. 매칭 실패 시 raw filter 반환
  return { targetNodeId: "reports", filter, reason: "..." };
}
```

### 9.2 AI 질문 → 화면 이동 (FilterSpec + virtualId)

```typescript
/** AI가 반환하는 네비게이션 추천 */
interface AiReportsNavigation {
  targetNodeId: "reports" | "stats";
  /** virtualNode 우선 반환 */
  virtualId?: string;
  /** FilterSpec 기반 필터 객체 */
  filter?: ReportFilterSpec | StatsFilterSpec;
  /** 사유 */
  reason: string;
}
```

| 질문 | AI 반환 targetNodeId | virtualId | 설명 |
|------|---------------------|-----------|------|
| "스프린트 번다운 보여줘" | `reports` | `reports.sprint-burndown` | virtualId 우선 |
| "EVM 지표 확인" | `reports` | `reports.evm` | virtualId 우선 |
| "품질 리포트 보여줘" | `reports` | `reports.quality` | virtualId 우선 |
| "추적 매트릭스 보여줘" | `reports` | `reports.traceability` | virtualId 우선 |
| "리스크 히트맵 보여줘" | `reports` | - | raw filter: `{ reportType: "risk_heatmap" }` |
| "프로젝트 현황 보고서 만들어줘" | `reports` | - | raw filter + do intent |
| "전체 프로젝트 비교해줘" | `stats` | - | /stats로 이동 |
| "포트폴리오 KPI 보여줘" | `stats` | - | /stats로 이동 |
| "리포트 PDF로 내보내줘" | `reports` | - | do_export_report intent |

### 9.3 Preset별 do intent AI 라우팅

AI는 조치(do) 질문 시 Preset에 따라 targetNodeId를 결정한다.

```
PM이 "리포트 만들어줘" → targetNodeId: "reports" (카탈로그에서 생성)
PMO가 "리포트 만들어줘" → targetNodeId: "reports" (카탈로그에서 생성 + 스케줄 관리)
Sponsor가 "리포트 보여줘" → targetNodeId: "reports" (3종 핵심 리포트 + 읽기 전용)
DEV가 "번다운 보여줘" → targetNodeId: "reports" (sprint_burndown 고정)
PMO가 "전체 통계 보여줘" → targetNodeId: "stats" (크로스 프로젝트 통계)
```

### 9.4 Hub 노드 페널티 적용

리포트 화면은 Hub 노드(nodeRole: "hub")이므로, 행동/목록 질문에는 스코어 페널티가 적용된다.

> **v2.1 변경**: 리포트/통계 도메인 키워드가 포함된 질문은 허브 페널티를 면제하거나 보너스로 전환한다.
> "리포트 만들어줘"처럼 **행동 동사 + 리포트 키워드**가 동시에 있을 때, 허브 페널티로 다른 노드로 새는 것을 방지.

```text
Intent→Node 스코어링:

// --- v2.1: 도메인 키워드 기반 허브 페널티 예외 ---
REPORT_DOMAIN_KEYWORDS = ["리포트", "보고서", "export", "pdf", "ppt", "excel", "통계",
                          "report", "statistics", "stats", "export"]

IF 질문에 REPORT_DOMAIN_KEYWORDS 포함:
    hubPenaltyOverride = true   // 허브 페널티 면제
ELSE:
    hubPenaltyOverride = false

// --- 기본 스코어링 ---
IF 질문에 행동/목록 동사 포함 ("생성해줘", "만들어줘", "목록", "열어줘"):
    IF node.nodeRole === "hub" (reports):
        IF hubPenaltyOverride:
            score += 10   // v2.1: 리포트 키워드 → 페널티 면제 + 보너스
        ELSE:
            score -= 20   // Hub 페널티 (기존)
    IF node.nodeRole === "action" || node.nodeRole === "detail":
        score += 10   // Action/Detail 보너스

IF 질문이 요약/상태/판단 성격 ("추이 보여줘", "현황 알려줘", "분석해줘"):
    IF node.nodeRole === "hub" (reports):
        score += 15   // Hub 보너스
```

**예시**:

| 질문 | Reports (hub) | Sprint (detail) | 최종 추천 |
|------|--------------|----------------|----------|
| "이번 스프린트 상세 보여줘" | 60 - 20 = **40** | 75 + 10 = **85** | `/sprints/:id` |
| "스프린트 번다운 차트 보여줘" | 80 + 15 = **95** | 50 = **50** | `/reports?type=sprint_burndown` |
| "프로젝트 현황 종합 분석" | 85 + 15 = **100** | 30 = **30** | `/reports?type=project_status` |
| "리포트 만들어줘" (v2.1) | 70 + 10 = **80** | 50 = **50** | `/reports` (hub 면제) |
| "보고서 PDF로 내보내줘" (v2.1) | 70 + 10 = **80** | 30 = **30** | `/reports` (hub 면제) |

### 9.5 Completeness/Provenance UI 강제 표시 규칙 (v2.1)

> **v2.1 추가**: completeness/warnings/provenance가 형식만 있고 실제로 사용되지 않는 것을 방지한다.
> UI가 이 정보를 **강제로 표시**하여 데이터 신뢰성을 사용자에게 전달한다.

**미리보기 패널 규칙**:

| 조건 | UI 동작 |
|------|---------|
| 항상 | 미리보기 패널 상단에 `asOf` + completeness 요약 + warnings 요약 표시 (접을 수는 있어도 숨기지 않기) |
| completeness !== "ok" | 차트/표 옆에 "부분 데이터" 배지 표시 (DataIncompleteBadge) |
| warnings.length > 0 | 경고 배너 표시 (severity별 색상: info=파랑, warning=노랑, error=빨강) |
| Export 시 completeness !== "ok" | **파일 표지/각주에 동일 경고를 자동 삽입** (책임성 확보) |

**provenance 표시 규칙**:

| 조건 | UI 동작 |
|------|---------|
| 개발/디버그 모드 | provenance 전체 표시 (traceId, rowCount, cacheHit, latencyMs, policyDecision) |
| 일반 사용자 모드 | provenance 접이식 — "데이터 출처 상세" 토글 (기본 접힘) |
| policyDecision === "filtered" 또는 "masked" | "일부 데이터가 권한 정책에 의해 필터링되었습니다" 안내 표시 |
| cacheHit === true | "캐시된 데이터" 배지 (asOf 시간과 함께) |

**/stats 화면 추가 규칙**:

| 조건 | UI 동작 |
|------|---------|
| 프로젝트별 projectAsOf가 24시간 이상 오래됨 | 해당 프로젝트 행에 "데이터 오래됨" 배지 (기존 v2.0) |
| 프로젝트별 projectAsOf 편차 > 12시간 | "비교 시점 불일치" 경고 배너 (v2.1 추가) |
| accessibleProjectIds가 요청 projectIds보다 적음 | "접근 불가 프로젝트 N건 제외" warnings 표시 |

---

## 10. 인터랙션 상세

### 10.1 리포트 생성 플로우

```text
1. 카탈로그에서 리포트 유형 선택
   → 카탈로그 카드의 [생성] 버튼 클릭
   → v2.1: useReportActionPolicy(reportType).canGenerate 확인
2. 리포트 생성 다이얼로그 표시
   → 파라미터 설정 (기간/단계/파트/스프린트)
   → [생성] 클릭
3. POST /api/reports/generate 호출
   → 응답: { reportId, status: "generating", stage: "queued", estimatedCompletionMs, cached }
   → v2.1: cached === true이면 "기존 리포트 재사용" 안내 + 바로 미리보기
4. 생성 중 상태 표시 (v2.1: stage 기반 설명)
   → ReportGeneratingStatus 컴포넌트 표시
   → stage별 UI 텍스트:
     queued     → "대기 중..."
     collecting → "데이터 수집 중..."
     aggregating→ "집계/계산 중..."
     rendering  → "차트 생성 중..."
   → 가짜 진행률: 0~90% 점진 + completed 시 100% (estimatedCompletionMs는 참고용)
5. 폴링: GET /api/reports/:reportId (3초 간격)
   → status === "completed" 시 미리보기 패널 자동 열림
   → status === "failed" 시 에러 메시지 + [재시도] 버튼
6. Right Panel에 리포트 미리보기 표시
   → 차트 + 요약 데이터 렌더링
   → v2.1: completeness/warnings 배너 상단 표시 (§9.5 규칙)
   → [Export] 버튼 활성화
```

### 10.2 필터 변경 인터랙션

```
기간 필터 변경:
  → 카탈로그의 quickMetrics 재계산 (서버 요청)
  → 미리보기 패널 데이터 갱신
  → URL 업데이트: /reports?from=2026-01-01&to=2026-02-08

단계/파트 필터 변경:
  → 해당 필터가 적용 가능한 리포트만 강조
  → 미리보기 패널 데이터 갱신

스프린트 필터 변경:
  → Sprint Burndown / Velocity 리포트에만 적용
  → 해당 리포트 카드의 미리보기 갱신
```

### 10.3 Export 플로우

```text
1. 미리보기 패널에서 [PDF Export] / [Excel Export] / [PPT Export] 클릭
   → v2.1: useReportActionPolicy(reportType).canExport 확인
2. ExportDialog 표시
   → 옵션 설정 (차트 포함 여부, Raw 데이터 포함 여부, 페이지 크기)
   → v2.1: completeness !== "ok"이면 "파일에 경고 포함됨" 안내 표시
   → [Export 시작] 클릭
3. POST /api/reports/:reportId/export 호출
   → 응답: { exportId, status: "processing", statusUrl }
4. ExportProgressBar 표시
   → v2.1: 백오프 폴링 — retryAfterMs 사용 (기본 2초, progress >= 80 시 5초)
   → progress 퍼센트 표시
   → 최대 5분 폴링 → 초과 시 "시간 초과" 안내 + 수동 확인 링크(statusUrl)
5. status === "completed" 시:
   → 자동 다운로드 시작 (downloadUrl — v2.1: signed URL)
   → Toast: "리포트 다운로드가 완료되었습니다"
   → v2.1: downloadUrl 만료 시 [다시 다운로드] → POST /exports/:id/renew
6. status === "failed" 시:
   → 에러 메시지 표시
   → [재시도] 버튼
```

### 10.4 드릴다운 인터랙션

```
카탈로그 카드 클릭:
  → panelMode="preview" → Right Panel에 해당 리포트 미리보기 표시

미리보기 내 차트 영역 클릭:
  → Burndown Chart의 특정 일자 클릭 → 해당 일자의 Sprint 상세 (nodeId: "sprints")
  → EVM S-Curve의 Phase 구간 클릭 → 해당 Phase 상세 (nodeId: "phases")
  → Risk Heatmap의 셀 클릭 → 해당 리스크 상세 (nodeId: "decisions")
  → Quality Dashboard의 결함 클릭 → 이슈 관리 (nodeId: "issues")
  → Traceability Matrix의 행 클릭 → 요구사항 상세 (nodeId: "requirements")

프로젝트 건강도 매트릭스 행 클릭 (/stats):
  → Right Panel에 해당 프로젝트 상세 분석
  → [상세 보기 →] 클릭 시 해당 프로젝트의 /reports로 이동
```

### 10.5 스케줄 관리 플로우

```
1. 카탈로그 카드의 [스케줄] 버튼 클릭
   → panelMode="schedule" → Right Panel에 SchedulePanel 표시
2. 새 스케줄 생성:
   → ScheduleForm 표시
   → 빈도 (일간/주간/월간), 시간, 형식, 수신자 설정
   → [저장] 클릭 → POST /api/reports/schedules
3. 기존 스케줄 편집:
   → 스케줄 항목의 [편집] 클릭 → ScheduleForm (pre-filled)
   → [저장] 클릭 → PUT /api/reports/schedules/:id
4. 스케줄 일시 중지:
   → [중지] 클릭 → PUT /api/reports/schedules/:id { status: "paused" }
5. 스케줄 삭제:
   → [삭제] 클릭 → 확인 다이얼로그 → DELETE /api/reports/schedules/:id
```

### 10.6 데이터 새로고침

| 항목 | 주기 |
|------|------|
| 카탈로그 | 페이지 진입 시 + 리포트 생성 완료 시 |
| 리포트 미리보기 | 리포트 카드 선택 시 |
| KPI Cards | 5분 자동 갱신 (TanStack Query `refetchInterval`) |
| 통계 데이터 (/stats) | 페이지 진입 시 (캐시 10분) |
| Export 상태 | 폴링 2초 간격 (진행 중일 때만) |
| 리포트 생성 상태 | 폴링 3초 간격 (생성 중일 때만) |
| 전체 | 수동 새로고침 버튼 (Page Header) |

### 10.7 KPI 카드 클릭 → 필터 활성화

```typescript
/** KPI 카드 클릭 시 활성화되는 필터 매핑 */
const REPORT_KPI_FILTER_MAP: Record<string, {
  filter: Partial<ReportFilterSpec>;
  panelMode: ReportPanelMode;
}> = {
  KPI_REPORT_TYPES:    { filter: {}, panelMode: "none" },
  KPI_SCHEDULED:       { filter: { scheduled: true }, panelMode: "schedule" },
  KPI_RECENT_GENERATED:{ filter: {}, panelMode: "preview" },
  KPI_EXPORT_PENDING:  { filter: {}, panelMode: "export" },
  KPI_SPI:             { filter: { reportType: "evm_spi_cpi" }, panelMode: "preview" },
  KPI_CPI:             { filter: { reportType: "evm_spi_cpi" }, panelMode: "preview" },
  KPI_NOT_GENERATED:   { filter: {}, panelMode: "none" },
};
```

**인터랙션 흐름**:

```text
1. KPI 카드 클릭
2. REPORT_KPI_FILTER_MAP에서 filter 객체 + panelMode 조회
3. 카탈로그 필터 적용 + panelMode 전환
4. 동일 카드 재클릭 시 필터 해제 (toggle 동작)
5. 활성 카드는 테두리 강조 + 필터 활성 아이콘으로 표시
```

---

## 11. 반응형 대응

### 11.1 브레이크포인트별 레이아웃

#### /reports 화면

| 너비 | 카탈로그 | Right Panel | 필터 | KPI Row |
|------|---------|-------------|------|---------|
| >=1440px | 카드 3열 그리드 또는 테이블 (PMO) | 30% 고정 (visible) | 인라인 | 4열 가로 배치 |
| 1280px | 카드 2열 그리드 또는 테이블 | Drawer (toggle) | 인라인 | 4열 가로 배치 |
| <=1024px | 카드 2열 그리드 (테이블 → 카드) | Drawer (overlay) | 접이식 | 2x2 그리드 |
| <=768px | 카드 1열 세로 스택 | Full Modal | 접이식 | 1열 세로 스택 |

#### /stats 화면

| 너비 | 매트릭스 | Right Panel | 차트 | KPI Row |
|------|---------|-------------|------|---------|
| >=1440px | 전체 열 표시 테이블 | 30% 고정 (visible) | 가로 2열 배치 | 4열 가로 배치 |
| 1280px | 핵심 열만 (Health/SPI/CPI/진행률) | Drawer (toggle) | 가로 2열 배치 | 4열 가로 배치 |
| <=1024px | 핵심 열만 (스크롤) | Drawer (overlay) | 세로 1열 스택 | 2x2 그리드 |
| <=768px | 카드 형태 전환 | Full Modal | 세로 1열 스택 | 1열 세로 스택 |

### 11.2 위젯 축소 규칙

- **>=1440px**: 전체 표시, 차트 상세 모드
- **1280px**: 차트 축약 모드 (범례 축소), Right Panel → Drawer 전환
- **<=1024px**: EVM S-Curve → SPI/CPI 게이지만 표시, 리소스 히트맵 → 바 차트로 전환
- **<=768px**: 차트 → 숫자 요약으로 전환, 미리보기 → 접이식 (펼치기 버튼)

### 11.3 Export 다이얼로그 반응형

| 너비 | 다이얼로그 형태 |
|------|---------------|
| >=1024px | 중앙 모달 (480px 너비) |
| <=768px | Full-screen 바텀 시트 |

---

## 12. 깨지기 쉬운 곳 체크리스트

> **v2.0**: 이 5곳을 막으면 안정화가 빨라진다.
> **v2.1**: 6~10번 항목 추가 — 권한 우회, 서명 URL, 산식 불일치, 허브 페널티, 폴링 부하.

| # | 위험 지점 | 해결책 | 검증 방법 |
| --- | --------- | ------ | --------- |
| 1 | **리포트 생성 타임아웃** — EVM/Traceability 같은 복잡 리포트는 생성 시간이 30초 이상 소요될 수 있어 HTTP 타임아웃 발생 | 비동기 생성 패턴 적용: POST → reportId 반환 → 폴링으로 상태 확인. 서버 측 백그라운드 워커 처리. 60초 타임아웃 + 재시도 1회. v2.1: stage 기반 진행 상태 표시 | EVM 리포트 생성 요청 후 30초 이상 경과해도 폴링이 정상 동작하는지 확인. 타임아웃 시 "생성 중" 상태 유지 + 재시도 버튼 표시 확인. v2.1: stage가 queued→collecting→aggregating→rendering 순서로 전이하는지 확인 |
| 2 | **Export 파일 크기 폭발** — 전체 기간 + Raw 데이터 포함 시 Excel 파일이 수백 MB가 될 수 있음 | Export 시 기간 제한 (최대 6개월), Raw 데이터 포함 시 경고 + 확인 다이얼로그, 서버 측 파일 크기 제한 (50MB). 초과 시 "기간을 좁혀주세요" 안내 | 전체 기간 + Raw 데이터 Excel Export 시 경고 다이얼로그 표시 확인. 50MB 초과 시 서버에서 413 반환 + 프론트 안내 확인 |
| 3 | **스케줄 리포트 수신자 권한 변경** — 스케줄 설정 후 수신자의 역할/권한이 변경되면 권한 없는 리포트가 배포될 수 있음 | 스케줄 실행 시점에 수신자 권한 재검증. 권한 없는 수신자는 건너뛰고 관리자에게 알림. 스케줄 목록에 "권한 경고" 배지 표시. v2.1: RecipientSelector에서 reportType view 가능 여부 배지 표시 | 수신자의 view_reports 권한 제거 후 스케줄 실행 → 해당 수신자 건너뛰기 + 관리자 알림 확인. v2.1: RecipientSelector에서 권한 불충족 수신자 grey-out 확인 |
| 4 | **크로스 프로젝트 통계 데이터 정합성** — /stats에서 여러 프로젝트 데이터를 집계할 때, 프로젝트별 갱신 시점이 다르면 비교 수치가 왜곡됨 | 각 프로젝트의 `asOf` 타임스탬프를 명시적으로 표시. 24시간 이상 갱신되지 않은 프로젝트는 "데이터 오래됨" 배지 표시. 집계 시 동일 시점 기준 정규화 | 프로젝트 A (1시간 전 갱신) + 프로젝트 B (3일 전 갱신) 비교 시, B에 "데이터 오래됨" 배지 표시 확인 |
| 5 | **동시 리포트 생성 요청** — 여러 사용자가 동일 리포트를 동시에 생성 요청하면 중복 생성 + 서버 부하 발생 | 동일 (projectId + reportType + parameters) 조합에 대해 진행 중인 생성 요청이 있으면 기존 reportId 반환 (idempotent). v2.1: asOf bucket(15분) 기반 자동 만료 + PMO 전용 forceRegenerate | 동일 리포트를 2개 브라우저에서 동시 생성 → 동일 reportId 반환 확인. v2.1: 15분 경과 후 재요청 시 새 리포트 생성 확인. forceRegenerate 시 admin_reports cap 검증 |
| 6 | **v2.1 ReportType ACL 우회** — UI에서 리포트 유형을 숨겨도 URL 직접 접근(`/reports?type=evm_spi_cpi`)이나 API 직접 호출로 제한된 리포트에 접근 가능 | 서버 측 ReportType ACL 정책 테이블(report_type_policy)로 모든 API에서 role × reportType × 행위 검증. UI 숨김은 보조 수단일 뿐 | DEV 계정으로 `/reports?type=evm_spi_cpi` 직접 접근 → 403 또는 빈 뷰 + 안내 확인. Sponsor 계정으로 `risk_heatmap` API 호출 → 403 확인 |
| 7 | **v2.1 downloadUrl 무단 공유** — Export 파일의 downloadUrl이 외부에 공유되면 권한 없는 사용자가 다운로드 가능 | downloadUrl을 HMAC 서명 URL(TTL 10~30분)로 발급. 다운로드 시점에 현재 사용자 권한 + 프로젝트 스코프 + reportType 정책 재검증. 만료 시 renew API로 재발급 | 서명 URL 만료 후 접근 → 401 + "만료됨" 안내 + renew 링크 확인. 다른 사용자 세션에서 서명 URL 접근 → 403 확인 |
| 8 | **v2.1 산식 정의 불일치** — /dashboard 진행률, /reports(project_status) 진행률, /stats 진행률이 서로 다른 산식으로 계산되면 신뢰가 무너짐 | 진행률/SPI/CPI 등 핵심 지표의 계산 로직을 **공통 서비스(MetricsCalculationService)**로 단일화. /dashboard, /reports, /stats 모두 동일 서비스 호출 | /dashboard 진행률 vs /reports(project_status) 진행률 vs /stats 진행률 스냅샷 비교 → 동일 값 확인. 데이터 변경 후 3곳 동시 갱신 확인 |
| 9 | **v2.1 Hub 페널티 키워드 충돌** — "리포트 만들어줘"에서 행동 동사 + 리포트 키워드가 동시에 있으면 Hub 페널티와 예외가 충돌하여 스코어링이 불안정 | REPORT_DOMAIN_KEYWORDS 매칭 시 hubPenaltyOverride=true로 페널티 완전 면제 + 보너스(+10). 키워드 없는 행동 질문만 기존 페널티(-20) 적용 | "리포트 만들어줘" → reports 노드 추천 확인. "스프린트 상세 보여줘" → sprints 노드 추천 확인 (기존 페널티 유지) |
| 10 | **v2.1 Export 폴링 서버 부하** — 다수 사용자가 동시 Export 시 2초 폴링이 서버에 과부하 유발 | retryAfterMs를 서버 응답에 포함하여 클라이언트 백오프 유도 (progress >= 80 → 5초). 최대 폴링 5분 제한. 추후 SSE/WebSocket push 전환 | 100명 동시 Export 폴링 시나리오에서 서버 응답 시간 < 200ms 확인. retryAfterMs에 따라 클라이언트 폴링 간격이 실제로 변경되는지 확인 |

---

## 13. Capability 매핑 총괄

> **v2.0 확정**: 5개 Capability
> **v2.1 추가**: ReportType ACL 정책 테이블(§13.2), ReportActionPolicy 훅(§8.1.1), RecipientSelector 제약(§8.1.2)

| Capability | 행위 | 대표 역할 |
|-----------|------|---------|
| `view_reports` | 리포트 카탈로그 조회, 생성된 리포트 열람, 미리보기 | ALL (Sponsor, PMO, PM, DevReader, DEV, Customer PM, 외부 감사) |
| `generate_reports` | 리포트 생성 요청, 파라미터 설정 | PM, PMO |
| `export_reports` | 리포트를 PDF/Excel/PPT로 Export | PM, PMO, 외부 감사 (제한적) |
| `view_stats` | 크로스 프로젝트 통계 화면 접근 | PMO, Sponsor, Customer PM (제한적) |
| `admin_reports` | 예약 리포트 관리 (생성/편집/삭제), 전체 프로젝트 스케줄 관리 | PMO |

### 13.1 역할별 Capability 매핑

| 역할 | view_reports | generate_reports | export_reports | view_stats | admin_reports |
|------|-------------|-----------------|---------------|-----------|--------------|
| **Sponsor** | O | - | - | O (요약) | - |
| **PMO** | O | O | O | O | O |
| **PM** | O | O | O | O (자기 프로젝트) | - |
| **DevReader** | O | - | - | - | - |
| **DEV** | O (번다운/Velocity만) | - | - | - | - |
| **Customer PM** | O | - | - | O (요약) | - |
| **외부 감사** | O | - | O (PDF만) | O | - |

### 13.2 ReportType ACL 정책 테이블 (v2.1)

> **v2.1 추가**: Capability만으로는 리포트 유형별 접근을 통제할 수 없다.
> UI 숨김만으로는 URL 직접 접근(`/reports?type=evm_spi_cpi`)을 막을 수 없으므로,
> **서버 측 ReportType ACL 정책 테이블**을 반드시 둔다.

```typescript
/** 서버 측 정책 테이블 — DB 또는 설정 파일로 관리 */
interface ReportTypePolicy {
  role: ProjectRole;
  reportType: ReportType;
  canView: boolean;
  canGenerate: boolean;
  canExport: boolean;
  canSchedule: boolean;
}
```

**정책 매트릭스 (역할 × 리포트 유형 × 행위)**:

| 역할 | project_status | sprint_burndown | velocity_trend | evm_spi_cpi | quality_dashboard | resource_utilization | risk_heatmap | phase_gate_status | requirement_traceability |
|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **Sponsor** | V | - | - | V | - | - | - | V | - |
| **PMO** | VGES | VGES | VGES | VGES | VGES | VGES | VGES | VGES | VGES |
| **PM** | VGE | VGE | VGE | VGE | VGE | VGE | VGE | VGE | VGE |
| **DEV** | - | V | V | - | - | - | - | - | - |
| **Customer PM** | V | - | - | V | - | - | - | V | - |
| **외부 감사** | V(PDF) | - | - | V(PDF) | V(PDF) | - | V(PDF) | V(PDF) | V(PDF) |

> V=View, G=Generate, E=Export, S=Schedule. `-` = 접근 불가 (서버 403)

**서버 검증 규칙**:

```text
모든 리포트 API 요청 시:
  1. JWT에서 사용자 역할(role) 추출
  2. report_type_policy(role, requestedReportType) 조회
  3. 요청 행위(view/generate/export/schedule)가 허용되지 않으면 → 403 Forbidden
  4. UI는 이 정책을 /api/reports/catalog 응답의 canView/canGenerate/canExport/canSchedule으로 내려받아 렌더링
```

### 13.3 Capability → UI 행위 매핑

```typescript
/** Capability에 따른 UI 요소 표시/비표시 */
const REPORT_CAPABILITY_UI_MAP = {
  view_reports: {
    catalogVisible: true,
    previewEnabled: true,
  },
  generate_reports: {
    generateButtonVisible: true,
    parameterFormEnabled: true,
  },
  export_reports: {
    exportButtonVisible: true,
    formatSelectorEnabled: true,
  },
  view_stats: {
    statsMenuVisible: true,
    healthMatrixEnabled: true,
    comparisonChartEnabled: true,
  },
  admin_reports: {
    scheduleCreateEnabled: true,
    scheduleEditEnabled: true,
    scheduleDeleteEnabled: true,
    recipientManageEnabled: true,
  },
};
```

---

## 14. DoD (본 화면 완료 기준)

### /reports 화면

- [ ] `/reports` 카탈로그에서 Preset별 리포트 유형 분기 동작 (EXEC: 3종, PM/PMO: 9종, DEV: 2종)
- [ ] 9종 표준 리포트 유형별 카탈로그 카드/테이블 행 렌더링
- [ ] FilterSpec 기반 다차원 필터 (reportType/dateRange/phaseId/partId/sprintId/format/scheduled) 동작
- [ ] 리포트 생성 플로우: 다이얼로그 → POST → 폴링 → 완료 → 미리보기 자동 열림
- [ ] 리포트 미리보기 Right Panel: 유형별 차트 렌더링 (Burndown/EVM/Quality/Risk/Traceability 등)
- [ ] Export 플로우: 형식 선택(PDF/Excel/PPT) → 옵션 → POST → 폴링 → 자동 다운로드
- [ ] Export 파일 크기 제한 (50MB) + 초과 시 안내
- [ ] 예약 리포트 CRUD: 생성/편집/중지/삭제 (cap: `admin_reports`)
- [ ] 예약 리포트 수신자 권한 재검증 (실행 시점 검증)
- [ ] KPI Row: 전체 리포트 유형수/예약건수/최근생성건수/Export대기건수 표시
- [ ] PMO Preset: 테이블 형태 카탈로그 + 스케줄 관리 Right Panel
- [ ] PM Preset: 카드 형태 카탈로그 + 미리보기 Right Panel
- [ ] EXEC Preset: 핵심 3종 리포트만 표시 (compact)
- [ ] DEV Preset: Sprint Burndown + Velocity Trend만 표시
- [ ] 동일 리포트 동시 생성 방지 (idempotent)
- [ ] 비동기 리포트 생성: 타임아웃 대응 + 재시도
- [ ] (v2.1) stage 기반 생성 진행 표시: queued→collecting→aggregating→rendering 순서 전이 + UI 텍스트
- [ ] (v2.1) ReportType ACL 서버 검증: role × reportType × 행위(view/generate/export/schedule) 403 처리
- [ ] (v2.1) useReportActionPolicy 훅: 모든 CTA 버튼이 단일 정책 결과로 렌더/disable
- [ ] (v2.1) ReportData 타입 유니온: 유형별 zod 검증, ReportChartRenderer 방어코드 최소화
- [ ] (v2.1) Idempotency asOf bucket(15분): 자동 만료 + forceRegenerate(admin_reports) 동작 확인

### /reports Export (v2.1)

- [ ] (v2.1) downloadUrl 서명 URL: HMAC 서명 + TTL 10~30분 + 만료 시 renew API 동작
- [ ] (v2.1) 다운로드 시점 권한 재검증: 다른 사용자 세션 접근 → 403
- [ ] (v2.1) Export 폴링 백오프: retryAfterMs 서버 응답 기반 (progress < 80 → 2초, >= 80 → 5초)
- [ ] (v2.1) Export 최대 폴링 5분 → "시간 초과" 안내 + statusUrl 수동 확인 링크
- [ ] (v2.1) completeness !== "ok" 시 Export 파일 표지/각주에 경고 자동 삽입

### /stats 화면

- [ ] `/stats` 화면에서 포트폴리오 KPI Row 표시
- [ ] 프로젝트 건강도 매트릭스 테이블 (Health/SPI/CPI/진행률/리스크/리소스)
- [ ] 매트릭스 행 클릭 → Right Panel 상세 분석 표시
- [ ] 진행률 비교 차트 (프로젝트별 바 차트)
- [ ] 리소스 배분 현황 차트 (PMO만)
- [ ] 트렌드 차트 (Health Trend/SPI-CPI Trend)
- [ ] 크로스 프로젝트 데이터 정합성: asOf 표시 + "데이터 오래됨" 배지
- [ ] (v2.1) projectIds scope 교집합: 서버가 사용자 접근 가능 프로젝트와 교집합 처리
- [ ] (v2.1) 접근 불가 프로젝트 제외 시 warnings 표시 ("N건 제외")
- [ ] (v2.1) 프로젝트별 projectAsOf 편차 > 12시간 시 "비교 시점 불일치" 경고 배너
- [ ] (v2.1) 지표 카탈로그: /api/stats/metrics/catalog에서 metricKey 목록 서버 제공 → UI 동적 반영

### 공통

- [ ] **virtualNode 우선 반환**: sprint-burndown/evm/quality/traceability + v2.1: risk-heatmap/resource-utilization/phase-gate (7개)
- [ ] **Preset별 do intent 라우팅**: PM/PMO → reports, Sponsor → reports(읽기), DEV → reports(번다운 고정)
- [ ] **Hub 페널티 스코어링 적용**: 행동/목록 질문 시 리포트 추천 억제, 분석/종합 질문 시 보너스
- [ ] (v2.1) **Hub 페널티 키워드 예외**: 리포트/보고서/export/통계 키워드 포함 시 허브 페널티 면제 + 보너스(+10)
- [ ] AI 네비게이션: nodeId 기반 반환 → UI resolve → capability 필터링
- [ ] 미리보기 내 차트 드릴다운: 차트 영역 클릭 → 관련 화면 딥링크 이동
- [ ] 5분 자동 갱신 (KPI) + 수동 새로고침
- [ ] 반응형 레이아웃 (>=1440 / 1280 / <=1024 / <=768)
- [ ] Capability 기반 3중 방어 (메뉴 노출 / 라우트 가드 / 버튼 활성화)
- [ ] **completeness/warnings 배지**: 데이터 부족 시 리포트에 배지 표시
- [ ] (v2.1) **completeness UI 강제 표시**: 미리보기 상단 asOf + completeness + warnings 항상 노출
- [ ] (v2.1) **provenance 표시**: 디버그 모드 전체, 일반 모드 접이식 토글
- [ ] (v2.1) **RecipientSelector 제약**: 프로젝트 스코프 + reportType view 배지 + 권한 불충족 grey-out

### 권한/보안 테스트 (v2.1)

- [ ] DEV 계정 → `/reports?type=evm_spi_cpi` 직접 접근 → 403 또는 빈 뷰 + 안내
- [ ] Sponsor 계정 → risk_heatmap API 호출 → 정책대로 제한 확인
- [ ] export_reports 없는 계정 → `/api/reports/:id/export` 호출 → 403
- [ ] downloadUrl 서명 만료 후 접근 → 401/403 + renew 안내
- [ ] /stats에서 projectIds에 접근 불가 프로젝트 포함 → 교집합 처리 확인

### 데이터 신뢰성 테스트 (v2.1)

- [ ] /dashboard 진행률 vs /reports(project_status) 진행률 vs /stats 진행률 동일 산식 스냅샷 비교
- [ ] completeness warning 시 Export 파일에도 경고 포함 확인 (표지/각주)
- [ ] provenance policyDecision="filtered" 시 "일부 데이터 필터링됨" 안내 표시 확인

---

## 부록 A. 변경이력

| 버전 | 날짜 | 변경 내용 |
| ------ | ------ | --------- |
| v1.0 | 2026-02-08 | 초안 작성 — 9종 리포트 카탈로그, /reports + /stats 이원 구조, Preset별 분기(EXEC/PMO/PM/DEV), FilterSpec(reportType/dateRange/phaseId/partId/sprintId/format/scheduled), MenuOntology 노드 정의(reports + stats), 4개 virtualNode(sprint-burndown/evm/quality/traceability), 리포트 생성/Export/스케줄 API 계약, 컴포넌트 분해(features/reports/), AI Navigation 연동(virtualNode 우선 + Hub 페널티), 인터랙션 상세(생성 플로우/Export 플로우/드릴다운/스케줄 관리), 반응형 대응(4단계 브레이크포인트), 깨지기 쉬운 5곳 체크리스트(타임아웃/파일 크기/수신자 권한/데이터 정합성/동시 생성), 5개 Capability 매핑(view_reports/generate_reports/export_reports/view_stats/admin_reports), DoD |
| v2.0 | 2026-02-08 | v1.0에서 직접 v2.0으로 확정: (1) MenuOntologyNodeV2 타입 적용 — entities PascalCase, highlight 분리(metricKeys + widgetKeys), suggestedActions 복수 권한(requiredCaps[]), scope 2계층 분리(keys + params), nodeRole: "hub" 명시, (2) FilterSpec URL 직렬화 규칙 포함, (3) 리포트 미리보기 차트 드릴다운 → nodeId 기반 딥링크 명시, (4) 크로스 프로젝트 통계(/stats) 데이터 정합성 규칙(asOf 비교 + "데이터 오래됨" 배지), (5) 비동기 리포트 생성 패턴(POST → pollin → 완료) 확정 |
| v2.1 | 2026-02-08 | 전체 PMS 설계 관점 일관성 점검 + 운영 안정성 보강: (1) **ReportType ACL 정책 테이블** — UI 숨김만이 아닌 서버 측 role×reportType×행위 검증(§13.2), (2) **Hub 페널티 키워드 예외** — 리포트/보고서/export/통계 키워드 포함 시 허브 페널티 면제+보너스(§9.4), (3) **virtualNode 3종 승격** — risk-heatmap/resource-utilization/phase-gate(§2.3, §9.1), (4) **downloadUrl 서명 URL** — HMAC 서명+TTL 10~30분+다운로드 시점 권한 재검증+renew API(§7.1, §7.2.6), (5) **Stage 기반 비동기 생성** — queued/collecting/aggregating/rendering/completed/failed 단계 세분화(§7.1, §7.2.2, §10.1), (6) **Provenance 디버깅 수준 확장** — traceId/rowCount/cacheHit/latencyMs/policyDecision(§7.2.3), (7) **/stats Scope 교집합** — 사용자 접근 가능 프로젝트와 교집합 처리+projectAsOf 편차 경고(§7.3), (8) **지표 카탈로그 API** — /api/stats/metrics/catalog 서버 제공(§7.3.4), (9) **Idempotency asOf bucket** — 15분 자동 만료+PMO 전용 forceRegenerate(§7.2.2), (10) **Export 폴링 백오프** — retryAfterMs 서버 응답 기반+최대 5분 제한(§7.2.5, §10.3), (11) **useReportActionPolicy 훅** — 단일 정책 훅으로 CTA 버튼 일관성 보장(§8.1.1), (12) **RecipientSelector 제약** — 프로젝트 스코프+reportType view 배지+권한 불충족 grey-out(§8.1.2), (13) **ReportData 타입 유니온** — Record<string, unknown> → 유형별 discriminated union+zod 검증(§7.1), (14) **Completeness/Provenance UI 강제 표시** — 미리보기 상단 항상 노출+Export 파일 경고 삽입(§9.5), (15) **산식 불일치 방지** — /dashboard·/reports·/stats 핵심 지표 공통 서비스 단일화(§12 #8), (16) 깨지기 쉬운 곳 §12.6~12.10 추가(ACL 우회/서명 URL/산식 불일치/Hub 키워드/폴링 부하), (17) DoD v2.1 항목 추가(권한 보안 5건+데이터 신뢰성 3건+Export 보강 5건+Stats 보강 4건) |
