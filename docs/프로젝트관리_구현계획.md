# 프로젝트 관리 기능 구현 계획서

> 작성일: 2026-01-17
> **최종 업데이트: 2026-01-17**
> **상태: Phase 0~4 구현 완료 ✅**
> 버전: v2.1 (구현 완료 반영)

---

## 1. 개요

### 1.1 목적
- 멀티 프로젝트 환경 지원
- 계층적 권한 체계 기반 프로젝트 관리
- 프로젝트 → 파트(서브 프로젝트) → 구성원 위임 구조
- 프로젝트별 데이터 분리 및 권한에 따른 접근 제어

### 1.2 현재 상태
| 구성요소 | 상태 | 설명 |
|---------|------|------|
| `ProjectSelector.tsx` | ✅ 구현됨 | 프로젝트 선택 드롭다운 (미사용) |
| `ProjectContext.tsx` | ✅ 구현됨 | 프로젝트 상태 관리 |
| `types/project.ts` | ✅ 구현됨 | Project 타입 정의 |
| 권한 체계 | ⚠️ 단순 | 역할 기반 단일 권한만 존재 |
| Header 프로젝트 표시 | ❌ 하드코딩 | 고정 프로젝트명 표시 |
| 프로젝트 관리 메뉴 | ❌ 미구현 | CRUD 화면 없음 |
| 파트(서브 프로젝트) | ❌ 미구현 | 파트 개념 없음 |
| 프로젝트별 데이터 연동 | ❌ 미구현 | 각 화면이 프로젝트 무관하게 동작 |

---

## 2. 권한 체계 설계 ⭐ NEW

### 2.1 역할 계층 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                      시스템 레벨 역할 (전역)                      │
├─────────────────────────────────────────────────────────────────┤
│  System Admin (시스템 관리자)                                    │
│  ├── 시스템 전체 설정 관리                                       │
│  ├── 프로젝트 관리자(PM) 지정 권한                               │
│  ├── 전체 사용자 관리                                            │
│  └── 전체 권한 관리                                              │
│                                                                  │
│  PMO (프로젝트 관리 오피스)                                       │
│  ├── 전체 프로젝트 조회 (수정 불가)                              │
│  ├── 포트폴리오 대시보드 접근                                    │
│  └── 프로젝트 현황 보고서 조회                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  프로젝트 레벨 역할 (프로젝트별 할당)              │
├─────────────────────────────────────────────────────────────────┤
│  Project Owner (프로젝트 소유자/스폰서)                          │
│  ├── 프로젝트 승인/반려                                          │
│  ├── 예산 결정                                                   │
│  └── 주요 산출물 승인                                            │
│                                                                  │
│  Project Manager (프로젝트 관리자/PM)                            │
│  ├── 본인 생성 프로젝트만 관리 (여러 개 생성 가능)                │
│  ├── 파트(서브 프로젝트) 생성/관리                               │
│  ├── 파트장(Part Leader) 지정                                    │
│  ├── 교육 담당자 지정                                            │
│  ├── 공통 관리 (산출물, 회의, 이슈)                              │
│  └── 대표 프로젝트 설정                                          │
│                                                                  │
│  Part Leader (파트장)                                            │
│  ├── 할당된 파트 내 구성원 추가/관리                             │
│  ├── 파트 내 단계별 관리                                         │
│  ├── 파트 내 칸반보드, 백로그 관리                               │
│  └── 파트 내 공통 관리 (산출물, 회의, 이슈)                      │
│                                                                  │
│  Education Manager (교육 담당자)                                 │
│  └── 프로젝트 교육 관리                                          │
│                                                                  │
│  QA Lead (QA 리드)                                               │
│  └── 품질 관리, 테스트 결과 관리                                 │
│                                                                  │
│  BA (비즈니스 분석가)                                            │
│  └── 요구사항 관리, 백로그 작성                                  │
│                                                                  │
│  Member (구성원)                                                  │
│  └── 할당된 작업 수행, 진행 상태 업데이트                        │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 권한 위임 흐름

```
System Admin
    │
    ├──[PM 지정]──→ Project Manager
    │                    │
    │                    ├──[파트 생성]──→ Part (서브 프로젝트)
    │                    │                    │
    │                    │                    └──[파트장 지정]──→ Part Leader
    │                    │                                           │
    │                    │                                           └──[구성원 추가]──→ Member
    │                    │
    │                    ├──[교육 담당자 지정]──→ Education Manager
    │                    │
    │                    └──[QA/BA 지정]──→ QA Lead, BA
    │
    └──[PMO 지정]──→ PMO (조회만 가능)
```

### 2.3 다중 역할 지원

한 사용자가 여러 역할을 가질 수 있음:

```typescript
// 예시: 김철수의 역할
{
  userId: 'user-001',
  name: '김철수',
  systemRole: 'admin',           // 시스템 레벨: 관리자
  projectRoles: [
    { projectId: 'proj-001', role: 'pm' },           // 프로젝트A: PM
    { projectId: 'proj-002', role: 'part_leader', partId: 'part-003' },  // 프로젝트B: 파트장
    { projectId: 'proj-003', role: 'member', partId: 'part-005' }        // 프로젝트C: 구성원
  ]
}
```

### 2.4 권한별 기능 접근 매트릭스

| 기능 | Admin | PMO | Owner | PM | Part Leader | Edu Mgr | QA | BA | Member |
|------|:-----:|:---:|:-----:|:--:|:-----------:|:-------:|:--:|:--:|:------:|
| **시스템 설정** | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **PM 지정** | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **전체 권한 관리** | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **포트폴리오 조회** | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **프로젝트 생성** | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **파트 생성** | ✅ | ❌ | ❌ | ✅¹ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **파트장 지정** | ✅ | ❌ | ❌ | ✅¹ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **구성원 추가** | ✅ | ❌ | ❌ | ✅¹ | ✅² | ❌ | ❌ | ❌ | ❌ |
| **교육 담당자 지정** | ✅ | ❌ | ❌ | ✅¹ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **단계별 관리** | ✅ | 👁 | 👁 | ✅¹ | ✅² | ❌ | ❌ | 👁 | 👁 |
| **칸반보드** | ✅ | 👁 | ❌ | ✅¹ | ✅² | ❌ | ✅³ | ❌ | ✅⁴ |
| **백로그 관리** | ✅ | 👁 | ❌ | ✅¹ | ✅² | ❌ | ✅³ | ✅³ | 👁 |
| **공통 관리** | ✅ | 👁 | 👁 | ✅¹ | ✅² | ❌ | ❌ | ❌ | 👁 |
| **교육 관리** | ✅ | 👁 | 👁 | 👁 | 👁 | ✅³ | ❌ | ❌ | 👁 |
| **프로젝트 승인** | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

**범례:**
- ✅ 전체 권한
- 👁 조회만 가능
- ❌ 접근 불가
- ¹ 본인 생성 프로젝트만
- ² 할당된 파트만
- ³ 지정된 역할 범위 내
- ⁴ 할당된 작업만

### 2.5 권한 관리 위임 규칙

각 담당자는 자신의 권한 범위 내에서 하위 권한을 관리할 수 있음:

| 역할 | 관리 가능 대상 | 범위 |
|------|---------------|------|
| System Admin | 모든 사용자, 모든 권한 | 시스템 전체 |
| PM | 파트장, 교육 담당자, QA, BA, 구성원 | 본인 프로젝트 |
| Part Leader | 구성원 | 본인 파트 |
| Education Manager | - | 교육 데이터만 |

---

## 3. 데이터 모델 설계

### 3.1 사용자 (User)

```typescript
interface User {
  id: string;
  name: string;
  email: string;
  department: string;
  systemRole: SystemRole;  // 시스템 레벨 역할 (1개)
  createdAt: string;
  updatedAt: string;
}

type SystemRole = 'admin' | 'pmo' | 'user';  // 시스템 레벨 역할
```

### 3.2 프로젝트 (Project)

```typescript
interface Project {
  id: string;
  name: string;
  code: string;               // INS-AI-2025-001
  description: string;
  status: ProjectStatus;
  startDate: string;
  endDate: string;
  budget: number;
  progress: number;
  ownerId: string;            // 프로젝트 소유자 (스폰서)
  managerId: string;          // 프로젝트 관리자 (PM)
  isDefault: boolean;         // 대표 프로젝트 여부
  createdBy: string;          // 생성자 (PM)
  createdAt: string;
  updatedAt: string;
}

type ProjectStatus = 'PLANNING' | 'IN_PROGRESS' | 'ON_HOLD' | 'COMPLETED' | 'CANCELLED';
```

### 3.3 파트/서브 프로젝트 (Part)

```typescript
interface Part {
  id: string;
  projectId: string;          // 상위 프로젝트
  name: string;               // 예: "UI/UX 파트", "백엔드 파트"
  description: string;
  leaderId: string;           // 파트장
  status: PartStatus;
  startDate: string;
  endDate: string;
  progress: number;
  createdAt: string;
  updatedAt: string;
}

type PartStatus = 'ACTIVE' | 'COMPLETED' | 'ON_HOLD';
```

### 3.4 프로젝트 멤버십 (ProjectMembership)

```typescript
interface ProjectMembership {
  id: string;
  userId: string;
  projectId: string;
  partId?: string;            // 파트 소속 (optional)
  role: ProjectRole;          // 프로젝트 내 역할
  assignedBy: string;         // 지정한 사람
  assignedAt: string;
}

type ProjectRole = 
  | 'owner'           // 프로젝트 소유자 (스폰서)
  | 'pm'              // 프로젝트 관리자
  | 'part_leader'     // 파트장
  | 'education_mgr'   // 교육 담당자
  | 'qa_lead'         // QA 리드
  | 'ba'              // 비즈니스 분석가
  | 'member';         // 구성원
```

### 3.5 권한 컨텍스트 (AuthContext)

```typescript
interface AuthContext {
  user: User;
  systemRole: SystemRole;
  
  // 현재 프로젝트에서의 역할
  currentProjectRole?: ProjectRole;
  currentPartId?: string;
  
  // 권한 체크 헬퍼
  canAccessProject(projectId: string): boolean;
  canManageProject(projectId: string): boolean;
  canManagePart(partId: string): boolean;
  hasPermission(permission: Permission): boolean;
}
```

---

## 4. 메뉴 구조 변경

### 4.1 새 메뉴 구조

```
사이드바
├── 📊 통합 대시보드        ← 역할에 따라 포트폴리오/프로젝트 뷰
├── 📁 프로젝트 관리        ← Admin, PM (본인 프로젝트)
│
├── ─── 프로젝트 영역 ───   ← 선택된 프로젝트 + 권한 기반
├── 👥 파트 관리            ← NEW: PM, Part Leader
├── 🔀 단계별 관리
├── 📋 칸반 보드
├── 📝 백로그 관리
├── 📂 공통 관리
├── 🎓 교육 관리
│
├── ─── 시스템 영역 ───     ← 권한 기반
├── 👥 권한 관리            ← Admin (전체), PM/Part Leader (범위 내)
└── ⚙️ 설정
```

### 4.2 역할별 메뉴 접근 권한

| 메뉴 | Admin | PMO | Owner | PM | Part Leader | Edu Mgr | QA | BA | Member |
|------|:-----:|:---:|:-----:|:--:|:-----------:|:-------:|:--:|:--:|:------:|
| 통합 대시보드 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 프로젝트 관리 | ✅ | 👁 | ❌ | ✅¹ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **파트 관리** | ✅ | 👁 | ❌ | ✅¹ | ✅² | ❌ | ❌ | ❌ | ❌ |
| 단계별 관리 | ✅ | 👁 | 👁 | ✅¹ | ✅² | ❌ | ❌ | 👁 | 👁 |
| 칸반 보드 | ✅ | 👁 | ❌ | ✅¹ | ✅² | ❌ | ✅³ | ❌ | ✅⁴ |
| 백로그 관리 | ✅ | 👁 | ❌ | ✅¹ | ✅² | ❌ | ✅³ | ✅³ | 👁 |
| 공통 관리 | ✅ | 👁 | 👁 | ✅¹ | ✅² | ❌ | ❌ | ❌ | 👁 |
| 교육 관리 | ✅ | 👁 | 👁 | 👁 | 👁 | ✅³ | ❌ | ❌ | 👁 |
| 권한 관리 | ✅ | ❌ | ❌ | ✅¹ | ✅² | ❌ | ❌ | ❌ | ❌ |
| 설정 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 5. 기본 프로젝트 선택 로직

### 5.1 우선순위

```
1순위: 대표 프로젝트 (isDefault: true)
   ↓ 없으면
2순위: 사용자 마지막 선택 (localStorage)
   ↓ 없으면
3순위: 사용자가 접근 가능한 진행 중 프로젝트 중 최신
   ↓ 없으면
4순위: 사용자가 접근 가능한 프로젝트 중 첫 번째
   ↓ 없으면
5순위: 프로젝트 선택 안내 표시
```

### 5.2 대표 프로젝트 설정

- PM만 본인 프로젝트를 대표 프로젝트로 설정 가능
- Admin은 모든 프로젝트를 대표로 설정 가능
- 시스템 전체에서 1개만 가능 (새로 설정 시 기존 해제)

---

## 6. 구현 항목 상세

### 6.1 Phase 0: 권한 체계 기반 구축 ⭐ NEW

**신규 파일:**
- `types/auth.ts` - 권한 관련 타입 정의
- `types/part.ts` - 파트 타입 정의
- `contexts/AuthContext.tsx` - 권한 컨텍스트 (기존 확장)
- `hooks/usePermission.ts` - 권한 체크 훅
- `services/authApi.ts` - 권한 관련 API

**수정 파일:**
- `types/project.ts` - ownerId, managerId, isDefault 추가

**핵심 구현:**
```typescript
// hooks/usePermission.ts
export function usePermission() {
  const { user, currentProjectRole, currentPartId } = useAuth();
  
  const canManageProject = (projectId: string) => {
    if (user.systemRole === 'admin') return true;
    return currentProjectRole === 'pm' && isMyProject(projectId);
  };
  
  const canManagePart = (partId: string) => {
    if (user.systemRole === 'admin') return true;
    if (currentProjectRole === 'pm') return true;
    return currentProjectRole === 'part_leader' && currentPartId === partId;
  };
  
  const canEditKanban = () => {
    return ['admin', 'pm', 'part_leader', 'qa_lead', 'member'].includes(
      currentProjectRole || user.systemRole
    );
  };
  
  // ... 기타 권한 체크
  return { canManageProject, canManagePart, canEditKanban, ... };
}
```

---

### 6.2 Phase 1: 프로젝트 관리 메뉴

**수정 파일:**
- `App.tsx` - View 타입에 'projects', 'parts' 추가

**신규 파일:**
- `ProjectManagement.tsx` - 프로젝트 CRUD + 대표 설정

**UI 와이어프레임:**
```
┌─────────────────────────────────────────────────────────────────┐
│ 프로젝트 관리                                    [+ 새 프로젝트] │
├─────────────────────────────────────────────────────────────────┤
│  검색: [________________] 상태: [전체 ▼]                        │
│                                                                  │
│  ┌────┬──────────────────────┬────────┬────────┬───────────────┐│
│  │선택│ 프로젝트명            │ 상태   │ PM     │ 작업          ││
│  ├────┼──────────────────────┼────────┼────────┼───────────────┤│
│  │ ◉  │ AI 기반 손해보험 ⭐  │ 진행중 │ 김철수 │ 편집 파트관리 ││
│  │ ○  │ 차세대 고객관리      │ 계획   │ 이영희 │ 편집 파트관리 ││
│  └────┴──────────────────────┴────────┴────────┴───────────────┘│
│                                                                  │
│  ⭐ = 대표 프로젝트     [이 프로젝트로 작업]  [대표로 설정]     │
└─────────────────────────────────────────────────────────────────┘
```

---

### 6.3 Phase 2: 파트(서브 프로젝트) 관리 ⭐ NEW

**신규 파일:**
- `PartManagement.tsx` - 파트 CRUD + 파트장/구성원 관리

**UI 와이어프레임:**
```
┌─────────────────────────────────────────────────────────────────┐
│ 파트 관리 - AI 기반 손해보험 프로젝트                [+ 새 파트] │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 📁 UI/UX 파트                                    [관리] [▼] ││
│  │    파트장: 박민수                                            ││
│  │    구성원: 4명 | 진행률: 45%                                 ││
│  │    ├── 김지은 (디자이너)                                     ││
│  │    ├── 이준호 (퍼블리셔)                                     ││
│  │    └── [+ 구성원 추가]  ← 파트장도 추가 가능                 ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 📁 백엔드 파트                                   [관리] [▼] ││
│  │    파트장: 최영수                                            ││
│  │    구성원: 6명 | 진행률: 62%                                 ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 🎓 교육 담당: 한미영                             [변경]      ││
│  │ 🔍 QA 담당: 정수진                               [변경]      ││
│  │ 📋 BA 담당: 오현우                               [변경]      ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

---

### 6.4 Phase 3: Header + 대시보드 연동

**수정 파일:**
- `Header.tsx` - 선택된 프로젝트/파트 표시
- `Dashboard.tsx` - 역할별 뷰 분기

**Header 변경:**
```tsx
// 프로젝트 + 파트 표시
<div className="flex items-center gap-2">
  <FolderKanban />
  <div>
    <h2>{currentProject?.name || '프로젝트 선택'}</h2>
    {currentPart && <p className="text-sm">{currentPart.name}</p>}
  </div>
</div>
```

**Dashboard 분기:**
```
Admin/PMO + 프로젝트 미선택 → 포트폴리오 뷰 (전체 프로젝트 현황)
PM + 본인 프로젝트 → 프로젝트 상세 대시보드
Part Leader + 본인 파트 → 파트 상세 대시보드
Member → 본인 작업 현황
```

---

### 6.5 Phase 4: 프로젝트 종속 화면 연동

**수정 파일:**
- `PhaseManagement.tsx` - projectId + partId 기반 필터
- `KanbanBoard.tsx` - 권한 기반 편집/조회 분기
- `BacklogManagement.tsx` - 권한 기반 CRUD
- `CommonManagement.tsx` - 권한 기반 CRUD
- `EducationManagement.tsx` - 교육 담당자만 편집

**공통 패턴:**
```tsx
export default function SomeManagement() {
  const { currentProject } = useProject();
  const { canEdit, canView } = usePermission();

  if (!currentProject) {
    return <ProjectNotSelectedMessage />;
  }

  if (!canView('phase_management')) {
    return <AccessDeniedMessage />;
  }

  return (
    <div>
      {/* 조회 UI */}
      {canEdit('phase_management') && (
        <button>편집</button>
      )}
    </div>
  );
}
```

---

### 6.6 Phase 5: 권한 관리 화면 개선

**수정 파일:**
- `RoleManagement.tsx` - 계층적 권한 관리 UI

**기능:**
- Admin: 전체 사용자 역할 관리
- PM: 본인 프로젝트 내 역할 관리 (파트장, 교육 담당자 등)
- Part Leader: 본인 파트 내 구성원 관리

```
┌─────────────────────────────────────────────────────────────────┐
│ 권한 관리                                                        │
├─────────────────────────────────────────────────────────────────┤
│  [시스템 권한] [프로젝트 권한]  ← 탭 (Admin만 시스템 권한 보임)  │
│                                                                  │
│  프로젝트: [AI 기반 손해보험 ▼]                                  │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 프로젝트 관리자 (PM)                                         ││
│  │   김철수 ✓                                                   ││
│  ├─────────────────────────────────────────────────────────────┤│
│  │ 파트장                                                       ││
│  │   UI/UX: 박민수     [변경]                                   ││
│  │   백엔드: 최영수    [변경]                                   ││
│  ├─────────────────────────────────────────────────────────────┤│
│  │ 교육 담당자                                                  ││
│  │   한미영           [변경]                                    ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

---

### 6.7 Phase 6: API 확장

**수정 파일:**
- `api.ts` - 권한/파트 관련 API 추가

**API 목록:**
```typescript
// 권한 관련
getUserPermissions(userId: string): Promise<UserPermissions>
getProjectMembers(projectId: string): Promise<ProjectMembership[]>
assignProjectRole(projectId: string, userId: string, role: ProjectRole): Promise<void>
removeProjectRole(projectId: string, userId: string): Promise<void>

// 파트 관련
getParts(projectId: string): Promise<Part[]>
createPart(projectId: string, data: Partial<Part>): Promise<Part>
updatePart(partId: string, data: Partial<Part>): Promise<Part>
deletePart(partId: string): Promise<void>
assignPartLeader(partId: string, userId: string): Promise<void>
addPartMember(partId: string, userId: string): Promise<void>
removePartMember(partId: string, userId: string): Promise<void>

// 프로젝트별 데이터 (기존 + partId 파라미터)
getPhases(projectId: string, partId?: string): Promise<Phase[]>
getTasks(projectId: string, partId?: string): Promise<Task[]>
```

---

## 7. 구현 순서

```
Phase 0: 권한 체계 기반 구축 (1.5일) ⭐ NEW
├── types/auth.ts, types/part.ts 생성
├── contexts/AuthContext.tsx 확장
├── hooks/usePermission.ts 생성
└── types/project.ts 수정
    ↓
Phase 1: 프로젝트 관리 메뉴 (1일)
├── App.tsx 수정 (View, 메뉴)
├── ProjectManagement.tsx 생성
└── 권한 기반 접근 제어 적용
    ↓
Phase 2: 파트 관리 (1.5일) ⭐ NEW
├── PartManagement.tsx 생성
├── 파트장/구성원 관리 UI
└── 역할 지정 기능
    ↓
Phase 3: Header + 대시보드 연동 (1일)
├── Header.tsx 수정
├── Dashboard.tsx 역할별 뷰
└── 기본 프로젝트 선택 로직
    ↓
Phase 4: 프로젝트 종속 화면 연동 (2일)
├── 권한 기반 편집/조회 분기
├── PhaseManagement.tsx 수정
├── KanbanBoard.tsx 수정
├── BacklogManagement.tsx 수정
├── CommonManagement.tsx 수정
└── EducationManagement.tsx 수정
    ↓
Phase 5: 권한 관리 화면 개선 (1일)
├── RoleManagement.tsx 확장
└── 계층적 권한 관리 UI
    ↓
Phase 6: API 확장 (1일)
├── api.ts 수정
└── Mock 데이터 권한 기반 분리
```

**총 예상 작업량: 9일** (기존 5일 → 권한 체계 추가로 4일 증가)

---

## 8. 마이그레이션 고려사항

### 8.1 기존 데이터 호환

```
현재 상태:
- 단일 역할 (UserRole: sponsor, pmo_head, pm, ...)
- 프로젝트 구분 없이 전체 데이터 공유

마이그레이션:
1. 기존 UserRole → systemRole 매핑
   - admin → systemRole: 'admin'
   - pmo_head → systemRole: 'pmo'
   - 나머지 → systemRole: 'user'

2. 기존 사용자 → 기본 프로젝트 멤버십 생성
   - pm → projectRole: 'pm'
   - sponsor → projectRole: 'owner'
   - dev, qa, ba → projectRole: 'member'

3. 파트 데이터
   - 초기에는 "기본 파트" 1개 자동 생성
   - 이후 PM이 파트 분리 가능
```

### 8.2 점진적 적용

```
Step 1: 권한 체계 타입만 추가, 기존 로직 유지
Step 2: usePermission 훅 도입, 조회 권한만 적용
Step 3: 편집 권한 적용
Step 4: 파트 기능 활성화
```

---

## 9. 시작 명령어

```
/home/daniel/projects/pms-ic/docs/프로젝트관리_구현계획.md 를 참조하여
Phase 0 권한 체계 기반 구축부터 구현해주세요.
```

---

## 10. 참조 파일

### 기존 구현 (활용 가능):
- `PMS_IC_FrontEnd_v1.2/src/app/components/ProjectSelector.tsx`
- `PMS_IC_FrontEnd_v1.2/src/contexts/ProjectContext.tsx`
- `PMS_IC_FrontEnd_v1.2/src/types/project.ts`

### 수정 대상:
- `PMS_IC_FrontEnd_v1.2/src/app/App.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/Header.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/Dashboard.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/RoleManagement.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/PhaseManagement.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/KanbanBoard.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/BacklogManagement.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/CommonManagement.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/EducationManagement.tsx`
- `PMS_IC_FrontEnd_v1.2/src/services/api.ts`

### 신규 생성:
- `PMS_IC_FrontEnd_v1.2/src/types/auth.ts`
- `PMS_IC_FrontEnd_v1.2/src/types/part.ts`
- `PMS_IC_FrontEnd_v1.2/src/contexts/AuthContext.tsx` (확장)
- `PMS_IC_FrontEnd_v1.2/src/hooks/usePermission.ts`
- `PMS_IC_FrontEnd_v1.2/src/app/components/ProjectManagement.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/PartManagement.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/ProjectNotSelectedMessage.tsx`
- `PMS_IC_FrontEnd_v1.2/src/app/components/AccessDeniedMessage.tsx`

---

## 11. 구현 완료 현황 ✅

### 11.1 완료된 Phase

| Phase | 내용 | 상태 | 완료일 |
|-------|------|:----:|--------|
| **Phase 0** | 권한 체계 기반 구축 | ✅ | 2026-01-17 |
| **Phase 1** | 프로젝트 관리 메뉴 | ✅ | 2026-01-17 |
| **Phase 2** | 파트 관리 | ✅ | 2026-01-17 |
| **Phase 3** | Header + 대시보드 연동 | ✅ | 2026-01-17 |
| **Phase 4** | 공통 컴포넌트 | ✅ | 2026-01-17 |
| **Phase 5** | 권한 관리 화면 개선 | ✅ | 2026-01-17 |
| Phase 6 | 백엔드 API 연동 | 📋 | 추후 (백엔드 구현 필요) |

### 11.2 생성된 파일 목록

```
신규 생성 (7개):
├── src/types/auth.ts                    # 시스템/프로젝트 역할 타입
├── src/types/part.ts                    # 파트 타입
├── src/contexts/AuthContext.tsx         # 인증/권한 컨텍스트
├── src/hooks/usePermission.ts           # 권한 체크 훅
├── src/app/components/ProjectManagement.tsx   # 프로젝트 관리 화면
├── src/app/components/PartManagement.tsx      # 파트 관리 화면
└── src/app/components/ProjectNotSelectedMessage.tsx  # 안내 메시지

수정됨 (6개):
├── src/types/project.ts                 # code, ownerId, isDefault 추가
├── src/app/App.tsx                      # View 타입, 메뉴 추가
├── src/app/components/Header.tsx        # 프로젝트 표시/전환
├── src/app/components/Dashboard.tsx     # 포트폴리오 뷰 추가
├── src/app/components/RoleManagement.tsx # 탭 분리, 프로젝트 권한 관리
└── src/services/api.ts                  # Part/Members API 추가
```

### 11.3 새로운 기능

1. **프로젝트 관리 메뉴**
   - 프로젝트 CRUD (생성, 수정, 삭제)
   - 대표 프로젝트 설정 (⭐)
   - 프로젝트 선택하여 작업

2. **파트 관리 메뉴**
   - 파트(서브 프로젝트) CRUD
   - 파트장 지정 및 구성원 추가/제거
   - 프로젝트 담당자 표시 (교육, QA, BA)

3. **Header 프로젝트 표시**
   - 현재 선택된 프로젝트 동적 표시
   - 클릭하여 빠른 프로젝트 전환 드롭다운

4. **포트폴리오 대시보드**
   - PMO/Admin이 프로젝트 미선택 시 표시
   - 전체 프로젝트 현황 요약
   - 프로젝트별 진행률 차트
   - 클릭하여 프로젝트 선택

### 11.4 메뉴 구조

```
사이드바 (구현 완료)
├── 📊 통합 대시보드        ← 포트폴리오/프로젝트 뷰 자동 전환
├── 📁 프로젝트 관리        ← NEW (Admin, PM, PMO)
├── 🔀 파트 관리            ← NEW (Admin, PM, 파트장)
├── 🔀 단계별 관리
├── 📋 칸반 보드
├── 📝 백로그 관리
├── 📂 공통 관리
├── 🎓 교육 관리
├── 👥 권한 관리
└── ⚙️ 설정
```

### 11.5 권한 체계 (구현됨)

```typescript
// 시스템 역할
type SystemRole = 'admin' | 'pmo' | 'user';

// 프로젝트 역할
type ProjectRole = 'owner' | 'pm' | 'part_leader' | 
                   'education_mgr' | 'qa_lead' | 'ba' | 'member';

// 사용자별 권한 정보
interface UserPermissions {
  userId: string;
  systemRole: SystemRole;
  projectRoles: ProjectMembership[];  // 프로젝트별 역할 목록
}
```

### 11.6 남은 작업

**Phase 5: 권한 관리 화면 개선** ✅ 완료
- 시스템 권한 / 프로젝트 권한 탭 분리
- PM이 자기 프로젝트 내 역할 관리 UI
- 멤버 추가/역할 변경/제거 기능

**Phase 6: 백엔드 API 연동** (백엔드 구현 후)
- 현재 Mock 데이터 → 실제 백엔드 API 호출
- Project, Part, Membership 엔티티 구현
- 권한 기반 API 접근 제어
- 필요 API: `/projects`, `/parts`, `/members`

---

## 12. 테스트 방법

```bash
# 프론트엔드 개발 서버 시작
cd PMS_IC_FrontEnd_v1.2
npm run dev

# 로그인 테스트 계정
# Admin: admin@test.com / test1234
# PM: pm@test.com / test1234
# PMO: pmo@test.com / test1234
```

### 테스트 시나리오

1. **Admin으로 로그인**
   - 프로젝트 관리 메뉴 접근
   - 새 프로젝트 생성
   - 대표 프로젝트 설정

2. **PM으로 로그인**
   - 프로젝트 관리에서 프로젝트 선택
   - 파트 관리에서 파트 생성
   - 구성원 추가

3. **PMO로 로그인**
   - 대시보드에서 포트폴리오 뷰 확인
   - 프로젝트 클릭하여 상세 대시보드 전환
